// Generated from python-foundations-i.md. Edit the Markdown source, then rebuild.

const pythonFoundationIlos = [
  "Open, copy, rename and run a Google Colab notebook, then recognise code-cell output and temporary runtime state.",
  "Explain literals, variables, assignment and constants-by-convention.",
  "Use print() to display readable text, values and calculation results.",
  "Choose valid, meaningful names and avoid Python keywords and important built-in names.",
  "Distinguish expressions from statements and calculate with arithmetic operators and explicit precedence.",
  "Recognise strings, integers, floats, Boolean values and None states, and inspect them with type().",
  "Recognise augmented assignment, implicit integer-to-float conversion and the difference between rounding and display formatting.",
  "Convert suitable values with int(), float() and str() without confusing conversion with validation.",
  "Collect keyboard input and convert it before numerical calculation.",
  "Write comments that explain purpose, units, assumptions and data-quality decisions.",
  "Read and repair common beginner tracebacks and identify logical errors that produce no traceback.",
  "Adapt, explain, test and critically reflect on a complete bounded Python script."
];

const pythonFoundationModules = [
  {
    "id": "1.12",
    "part": "A",
    "title": "Colab and visible output",
    "time": "75 minutes",
    "summary": "Open and run a Colab notebook, understand cell state and communicate results with print().",
    "intro": "Begin practical Python in the browser. You will run code cells, inspect output, understand temporary notebook state and use print() to communicate clear results.",
    "task": "Complete checkpoints A1 and A2. Run, modify, deliberately break and repair at least one example involving cell order and one involving print().",
    "problem": [
      "A cell appears correct but fails because its setup cell was not run, or quotation marks and brackets do not match.",
      "Read the final error line, check execution order and repair the smallest relevant part before rerunning from top to bottom."
    ],
    "check": [
      "I can create and run a Colab code cell.",
      "I can explain why assignment may produce no visible output.",
      "I can display labelled values and calculation results with print()."
    ],
    "reflection": "What information should never be placed in a public or shared teaching notebook?"
  },
  {
    "id": "1.13",
    "part": "B",
    "title": "Values, variables and meaningful names",
    "time": "105 minutes",
    "summary": "Work with literals, constants-by-convention, assignment, augmented assignment, keywords and readable names.",
    "intro": "Names make code understandable. This tutorial distinguishes direct values from variables, shows how assignment and += change state, and develops naming habits that expose rather than hide case assumptions.",
    "task": "Complete checkpoints B1 to B4. Rewrite vague or invalid names, extend a text audit trail, preserve source values and document one assumption behind a derived count.",
    "problem": [
      "A legal-looking name is a keyword, hides a built-in function or differs from the intended name by one character or capital letter.",
      "Use snake case, check keywords, avoid built-in names and update every use consistently when renaming."
    ],
    "check": [
      "I can distinguish a literal, variable, assignment and constant-by-convention.",
      "I avoid Python keywords and important built-in names.",
      "My variable names communicate meaning, units or role."
    ],
    "reflection": "How can a precise variable name reveal a classification or data-cleaning choice that would otherwise remain hidden?"
  },
  {
    "id": "1.14",
    "part": "C",
    "title": "Expressions, arithmetic and precedence",
    "time": "90 minutes",
    "summary": "Build calculations with arithmetic operators and use parentheses to make their meaning explicit.",
    "intro": "Python evaluates expressions according to defined rules. You will calculate counts, rates, batches and scenarios while checking denominators, category overlap and the assumptions encoded in formulas.",
    "task": "Complete checkpoints C1 to C3. Predict each result first, verify it in Python and identify one logical error that produces no traceback.",
    "problem": [
      "A formula runs but reverses part and whole, double-counts categories or applies an operator to the wrong grouped values.",
      "Write the intended formula in words, use explicit parentheses and test a simple case with a known answer."
    ],
    "check": [
      "I can distinguish an expression from an assignment statement.",
      "I can use +, -, *, /, //, %, and ** for their intended purposes.",
      "I can explain and test the grouping in a longer formula."
    ],
    "reflection": "Who should justify a threshold, weight or projection factor that appears in code?"
  },
  {
    "id": "1.15",
    "part": "D",
    "title": "Strings, numbers, Booleans and type()",
    "time": "150 minutes",
    "summary": "Recognise foundational data types, missing states, implicit numeric conversion, rounding and display formatting.",
    "intro": "The same visible characters can have different Python types. Learn how strings, integers, floats, Boolean values and None differ, how compatible numeric types combine, and how type() supports debugging.",
    "task": "Complete checkpoints D1 to D4. Create and inspect each foundation type, distinguish None from zero and empty text, test a Boolean boundary, and explain rounding versus display formatting.",
    "problem": [
      "Numerical-looking text is used in arithmetic, a Boolean is stored as text or display formatting is confused with stored precision.",
      "Inspect values with type(), compare the result with the case meaning and repair the type without hiding information."
    ],
    "check": [
      "I can recognise str, int, float, bool and None states.",
      "I can explain = versus == and produce Booleans with comparisons.",
      "I use type() to investigate values and expressions, including implicit numeric conversion."
    ],
    "reflection": "Why does a technically correct Python type not prove that a measurement or category is valid?"
  },
  {
    "id": "1.16",
    "part": "E",
    "title": "Type conversion and user input",
    "time": "135 minutes",
    "summary": "Convert suitable values, collect input, standardise simple text and transfer the pattern to new cases.",
    "intro": "Keyboard input arrives as text. You will use int(), float() and str() purposefully, preserve raw information, prepare traceable labels and recognise inputs that convert successfully but remain implausible.",
    "task": "Complete checkpoints E1 and E2 plus one transfer example. Build an interactive summary or naming record, test invalid and boundary inputs and document which problems raise errors and which require case validation.",
    "problem": [
      "A conversion discards decimals or leading zeros, or a value converts successfully even though it is impossible in the case.",
      "Decide whether the value is a number, identifier or category before converting, then test plausibility separately."
    ],
    "check": [
      "I can use int(), float() and str() for justified conversions.",
      "I know that input() returns a string.",
      "I distinguish successful conversion from meaningful validation."
    ],
    "reflection": "What user input should be rejected, reviewed or preserved rather than automatically converted?"
  },
  {
    "id": "1.17",
    "part": "F",
    "title": "Comments and beginner debugging",
    "time": "120 minutes",
    "summary": "Document purpose and assumptions, then read and repair six common Python errors and logical mistakes.",
    "intro": "Comments support human understanding and tracebacks support systematic repair. This tutorial develops a repeatable routine for SyntaxError, NameError, TypeError, ValueError, ZeroDivisionError, IndentationError and error-free logical failures.",
    "task": "Complete checkpoints F1 and F2. Build the seven-cell error laboratory and record the cause, smallest repair, repaired output and supporting test for each problem.",
    "problem": [
      "A learner changes several lines at once, reads only the highlighted line or assumes that the absence of a traceback proves correctness.",
      "Read the final traceback line, make one focused repair and compare the result with an expected or boundary case."
    ],
    "check": [
      "My comments explain purpose, unit, assumption or reasoning.",
      "I can interpret the six named Python error types.",
      "I test logical results even when Python reports no error."
    ],
    "reflection": "Which people or situations might be excluded when a script is tested only on a convenient normal case?"
  },
  {
    "id": "1.18",
    "part": "G",
    "title": "Integration, mini-cases and self-test",
    "time": "180 minutes",
    "summary": "Combine the complete foundation, compare numerical and non-mathematical integration, apply it to three cases and check readiness for Python Foundations II.",
    "intro": "Bring the foundation together through complete scripts, text-standardisation work, case transfer, recap and self-assessment. The goal is not memorisation, but the ability to explain, modify, test and repair a bounded Python program.",
    "task": "Complete integration checkpoint G1 and at least one mini-case. Compare a numerical and a non-mathematical integrated script, then finish the self-test, reflection and completion checklist.",
    "problem": [
      "A completed script runs, but the learner cannot explain its assumptions, test a changed input or distinguish calculation from interpretation.",
      "Annotate every value and expression, test normal and boundary cases, and state what the code cannot establish."
    ],
    "check": [
      "I can explain a complete foundation script line by line.",
      "I can adapt learned patterns to a new bounded case.",
      "I know which topics require revision before Foundations II."
    ],
    "reflection": "What can Python calculate in your chosen case, and which judgement remains the responsibility of people?"
  }
];

const pythonFoundationGuide = "<h2>Course files and coding options</h2>\n<p>You do not need previous programming experience. Lecture 3 uses standard Python 3 only: no third-party library, dataset, virtual environment or <code>requirements.txt</code> file is required.</p>\n<h3>The three Lecture 3 notebooks</h3>\n<div class=\"table-wrap\"><table><thead><tr><th>File</th><th>Contents</th><th>Use it for</th></tr></thead><tbody>\n<tr><td><a href=\"https://colab.research.google.com/github/asmrabbi/E26_TAN7_Scripting_CPH/blob/main/notebooks/lecture_03/L03_Tutorial_2_1_to_2_7_Examples.ipynb\" target=\"_blank\" rel=\"noreferrer\">Tutorial Examples</a></td><td>Every website example for Tutorials 2.1–2.7, in the same order and with matching identifiers.</td><td>Lecture demonstrations and review.</td></tr>\n<tr><td><a href=\"https://colab.research.google.com/github/asmrabbi/E26_TAN7_Scripting_CPH/blob/main/notebooks/lecture_03/L03_Tutorial_2_1_to_2_7_Exercises.ipynb\" target=\"_blank\" rel=\"noreferrer\">Exercises</a></td><td>One runnable exercise for each tutorial subsection.</td><td>Work immediately after the matching examples.</td></tr>\n<tr><td><a href=\"https://colab.research.google.com/github/asmrabbi/E26_TAN7_Scripting_CPH/blob/main/notebooks/lecture_03/L03_Tutorial_2_1_to_2_7_Practice_Materials.ipynb\" target=\"_blank\" rel=\"noreferrer\">Practice Materials</a></td><td>Extra case questions, code-logic steps and fully commented examples.</td><td>Additional demonstrations and independent practice.</td></tr>\n</tbody></table></div>\n<p><a href=\"https://github.com/asmrabbi/E26_TAN7_Scripting_CPH\" target=\"_blank\" rel=\"noreferrer\">Open the complete AAU course repository on GitHub</a>. The <code>notebooks/lecture_03</code> folder contains exactly these three student-facing Lecture 3 files.</p>\n<h3>Google Colab — recommended</h3>\n<p>Colab runs a Jupyter notebook in a browser, so Lecture 3 needs no local setup.</p>\n<ol><li>Open one of the three direct Colab links above.</li><li>Choose <strong>File → Save a copy in Drive</strong> before editing.</li><li>Rename your copy so the lecture, tutorial range and your name are clear.</li><li>Run one cell with its play button or <strong>Shift+Enter</strong>.</li><li>Use <strong>Runtime → Run all</strong> to check the complete notebook from top to bottom.</li><li>Remember that live variables and temporarily uploaded files can disappear after a runtime restart.</li></ol>\n<div class=\"tutorial-screenshot-gallery\">\n<figure class=\"tutorial-screenshot\"><img src=\"assets/PF1-01-colab-file-menu.png\" alt=\"The public Lecture 3 notebook with Colab's File menu open.\" loading=\"lazy\"><figcaption>Open the File menu before saving an editable copy.</figcaption></figure>\n<figure class=\"tutorial-screenshot\"><img src=\"assets/PF1-02-colab-save-copy-drive.png\" alt=\"Save a copy in Drive in the Colab File menu.\" loading=\"lazy\"><figcaption>Save a copy in Drive before changing the public notebook.</figcaption></figure>\n<figure class=\"tutorial-screenshot\"><img src=\"assets/PF1-03-colab-notebook-title.png\" alt=\"The Lecture 3 notebook title field in Colab.\" loading=\"lazy\"><figcaption>Rename the notebook copy clearly.</figcaption></figure>\n<figure class=\"tutorial-screenshot\"><img src=\"assets/PF1-04-colab-code-cell-output.png\" alt=\"A Lecture 3 code cell with its Run control and output.\" loading=\"lazy\"><figcaption>Run a code cell and inspect the output directly below it.</figcaption></figure>\n<figure class=\"tutorial-screenshot\"><img src=\"assets/PF1-05-colab-code-text-toolbar.png\" alt=\"The Code and Text controls in Colab.\" loading=\"lazy\"><figcaption>Use + Code for Python and + Text for notes.</figcaption></figure>\n<figure class=\"tutorial-screenshot\"><img src=\"assets/PF1-06-colab-runtime-menu.png\" alt=\"The Colab Runtime menu.\" loading=\"lazy\"><figcaption>Use the Runtime menu to run all cells or restart the session.</figcaption></figure>\n</div>\n<p><a href=\"https://colab.research.google.com/\" target=\"_blank\" rel=\"noreferrer\">Open Google Colab</a> · <a href=\"https://research.google.com/colaboratory/faq.html\" target=\"_blank\" rel=\"noreferrer\">Read the official Colab FAQ</a></p>\n<h3>GitHub — find or download course files</h3>\n<p>Open the repository, select <code>notebooks</code>, then <code>lecture_03</code>, and choose the file ending in <code>Examples</code>, <code>Exercises</code> or <code>Practice_Materials</code>. Use the direct links in this tutorial when you want to run a notebook.</p>\n<ul><li><a href=\"https://docs.github.com/en/get-started/start-your-journey/hello-world\" target=\"_blank\" rel=\"noreferrer\">GitHub Hello World — official beginner guide</a></li><li><a href=\"https://docs.github.com/en/get-started/learning-to-code/getting-started-with-git\" target=\"_blank\" rel=\"noreferrer\">Getting started with Git — official GitHub guide</a></li><li><a href=\"https://www.youtube.com/watch?v=r8jQ9hVA2qs\" target=\"_blank\" rel=\"noreferrer\">A brief introduction to Git for beginners — official GitHub video</a></li></ul>\n<h3>PyCharm, Jupyter and other Python environments</h3>\n<p>All Lecture 3 code uses standard Python 3 and can be copied into a <code>.py</code> file or run from the supplied notebooks. Do not create a virtual environment or install <code>requirements.txt</code> during this lecture; those workflows become relevant when later lectures introduce third-party libraries.</p>\n<ul><li><a href=\"https://docs.python.org/3/using/\" target=\"_blank\" rel=\"noreferrer\">Python setup and usage — official documentation</a></li><li><a href=\"https://www.jetbrains.com/help/pycharm/installation-guide.html\" target=\"_blank\" rel=\"noreferrer\">Install PyCharm — official JetBrains guide</a></li><li><a href=\"https://www.jetbrains.com/help/pycharm/configuring-python-interpreter.html\" target=\"_blank\" rel=\"noreferrer\">Configure a Python interpreter in PyCharm</a></li><li><a href=\"https://jupyter.org/install\" target=\"_blank\" rel=\"noreferrer\">Install JupyterLab or Jupyter Notebook — official guide</a></li><li><a href=\"https://www.w3schools.com/python/default.asp\" target=\"_blank\" rel=\"noreferrer\">W3Schools Python tutorial</a></li></ul>\n<h3>How to use each example</h3>\n<ol><li><strong>Predict</strong> the output or error.</li><li><strong>Run</strong> the unmodified cell.</li><li><strong>Inspect</strong> the result.</li><li><strong>Explain</strong> the important line or symbol.</li><li><strong>Modify</strong> one input or message.</li><li><strong>Break</strong> only the instructed part.</li><li><strong>Repair</strong> the smallest relevant part.</li><li><strong>Reflect</strong> on what changed and what the code cannot establish.</li></ol>";

const pythonFoundationChapters = {
  "1.12": [
    {
      "title": "Colab and visible output",
      "html": "<h3>Suggested book chapters for Part A</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Priority</th>\n<th>Book and chapter</th>\n<th>Suggested focus</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Core companion</td>\n<td><a href=\"https://www.py4e.com/html3/01-intro\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 1: Why should you learn to write programs?</a></td>\n<td>Read <strong>Words and sentences</strong>, <strong>Conversing with Python</strong>, <strong>Writing a program</strong>, <strong>What could possibly go wrong?</strong> and <strong>Debugging</strong>. These sections support first output, execution order and a constructive response to errors.</td>\n</tr>\n<tr>\n<td>Recommended companion</td>\n<td><a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 2: Variables, expressions, and statements</a></td>\n<td>Skim <strong>Statements</strong> to see why assignment may produce no visible output and why scripts execute in sequence.</td>\n</tr>\n</tbody></table></div>\n<h2>Code cells, execution order and visible output</h2>\n<p>The prerequisite panel on the Python Foundations I homepage explains how to open, copy, rename and save a notebook. This tutorial begins with the Python activity itself: run a code cell, inspect its output and understand why notebook execution order matters.</p>\n<h3>Example 2.1.1 - Run your first code cell</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">print(&quot;Python is running&quot;)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Python is running\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>print</code> is the name of a Python function that displays information.</li>\n<li>The round brackets <code>(</code> and <code>)</code> contain the information given to the function.</li>\n<li><code>&quot;Python is running&quot;</code> is text. Python calls a piece of text a <strong>string</strong>.</li>\n<li>The quotation marks tell Python where the string begins and ends. The quotation marks are not displayed.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Typing <code>Print</code> with a capital <code>P</code>. Python distinguishes uppercase and lowercase letters.</li>\n<li>Omitting one quotation mark or one bracket.</li>\n<li>Typing the code into a text cell instead of a code cell.</li>\n<li>Looking for output before running the cell.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the message so the output is <code>Welcome to Python Foundations I</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Deliberately remove the final quotation mark so the cell contains <code>print(&quot;Python is running)</code>.</li>\n<li>Run the cell. Python should report a <code>SyntaxError</code>, often saying that a string literal was not closed.</li>\n<li>Repair the code by restoring the final quotation mark.</li>\n<li>Run it again and confirm that the original output returns.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which characters tell Python that <code>Python is running</code> should be treated as text?</p>\n<h3>Example 2.1.2 - Run cells in sequence</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code cell 1</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">organisation = &quot;North Harbour Council&quot;\n</code></pre></div>\n<p><strong>Expected output from cell 1</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">No visible output\n</code></pre></div>\n<p><strong>Code cell 2</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">print(organisation)\n</code></pre></div>\n<p><strong>Expected output from cell 2</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">North Harbour Council\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>In cell 1, <code>organisation</code> is a variable name.</li>\n<li><code>=</code> assigns the string <code>&quot;North Harbour Council&quot;</code> to that variable.</li>\n<li>Assignment usually creates no visible output. No output does not automatically mean that nothing happened.</li>\n<li>In cell 2, <code>print(organisation)</code> asks Python to display the value currently stored under the name <code>organisation</code>.</li>\n<li>There are no quotation marks around <code>organisation</code> in cell 2 because we want its stored value, not the word <code>organisation</code>.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Running cell 2 before cell 1. Python then does not know the name <code>organisation</code>.</li>\n<li>Writing <code>print(&quot;organisation&quot;)</code>, which displays the word <code>organisation</code> instead of the stored council name.</li>\n<li>Restarting the runtime and expecting old variable values to remain available.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the organisation name in cell 1 to a fictional university unit. Rerun cell 1 and then cell 2. Confirm that the displayed value changes.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Select <strong>Runtime &gt; Restart session</strong> if your Colab interface offers that wording. The exact label may vary.</li>\n<li>Run only cell 2.</li>\n<li>Observe the <code>NameError</code> because the restarted runtime no longer contains the variable.</li>\n<li>Repair the notebook state by running cell 1 before cell 2.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is notebook execution order part of debugging?</p>\n<h3>Exercise 2.1.1</h3>\n<p>Without copying the worked example exactly:</p>\n<ol>\n<li>Create a new code cell.</li>\n<li>Display your first name and programme name on separate output lines using two <code>print()</code> calls.</li>\n<li>Add another code cell that stores a fictional project title in a variable.</li>\n<li>Display the stored title.</li>\n<li>Restart the runtime, run only the display cell, and record the error type.</li>\n<li>Repair the execution order.</li>\n</ol>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_A1_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_A1_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Display information with <code>print()</code></h2>\n<p><code>print()</code> lets a program communicate with a person who runs it. It can display text, numbers, variable values and the results of expressions. Later, <code>print()</code> will help you inspect data and debug scripts.</p>\n<h3>Example 2.1.3 - Display several pieces of information</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">print(&quot;Dataset status&quot;)\nprint(&quot;Rows received:&quot;, 250)\nprint(&quot;Missing rows:&quot;, 18)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Dataset status\nRows received: 250\nMissing rows: 18\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 displays a heading stored directly as a string literal.</li>\n<li>Line 2 gives <code>print()</code> two items: the string <code>&quot;Rows received:&quot;</code> and the integer <code>250</code>.</li>\n<li>The comma separates the two items. <code>print()</code> inserts a space between them in the output.</li>\n<li>Line 3 follows the same structure with the integer <code>18</code>.</li>\n<li>Python executes these statements from top to bottom, so the output appears in the same order.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Putting the number inside the text accidentally. <code>&quot;250&quot;</code> displays correctly, but it is text rather than a number.</li>\n<li>Forgetting the comma between the label and number.</li>\n<li>Using a semicolon when the example uses a comma.</li>\n<li>Assuming <code>print()</code> stores a value. It displays information, but does not create a variable by itself.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a fourth line that displays <code>Duplicate rows: 6</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change line 2 to <code>print(&quot;Rows received:&quot; 250)</code> by removing the comma.</li>\n<li>Run the code and observe the <code>SyntaxError</code>.</li>\n<li>Repair the line by restoring the comma.</li>\n<li>Run the cell and confirm all three lines appear.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What job is the comma performing inside the second <code>print()</code> call?</p>\n<h3>Example 2.1.4 - Display a small organisational summary</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">organisation = &quot;North Harbour Council&quot;\nresponses_received = 480\nresponses_valid = 452\n\nprint(&quot;Organisation:&quot;, organisation)\nprint(&quot;Responses received:&quot;, responses_received)\nprint(&quot;Responses retained:&quot;, responses_valid)\nprint(&quot;Responses excluded:&quot;, responses_received - responses_valid)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Organisation: North Harbour Council\nResponses received: 480\nResponses retained: 452\nResponses excluded: 28\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 stores an organisation name as text.</li>\n<li>Line 2 stores the number of received responses as an integer.</li>\n<li>Line 3 stores the number retained after validation.</li>\n<li>The blank line separates setup from output for human readers. Python does not require that blank line.</li>\n<li>Lines 5 to 7 display labels and stored values.</li>\n<li>The final line contains the expression <code>responses_received - responses_valid</code>.</li>\n<li>Python evaluates the subtraction first and gives the resulting value, <code>28</code>, to <code>print()</code>.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Putting quotation marks around variable names, which prints the names instead of their values.</li>\n<li>Misspelling the same variable differently on different lines.</li>\n<li>Reversing the subtraction and producing <code>-28</code>.</li>\n<li>Treating a displayed total as proof that the underlying records were excluded for a justified reason. Code can calculate a count, but the data decision still requires explanation.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change <code>responses_valid</code> to <code>440</code>. Before running, predict the new number of excluded responses. Then add a line that displays a short source label such as <code>Source: public consultation portal</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the final expression to <code>responses_received - response_valid</code> by removing the <code>s</code> from one variable name.</li>\n<li>Run the cell and read the last line of the traceback.</li>\n<li>Identify <code>NameError</code> and the unrecognised name.</li>\n<li>Repair the spelling so it matches <code>responses_valid</code> exactly.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Does the final subtraction happen before or after <code>print()</code> displays the result?</p>\n<h3>Example 2.1.5 - Format a readable report with newline characters</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">report_title = &quot;Monthly data-quality note&quot;\nrecords_checked = 1250\n\nprint(report_title)\nprint(&quot;\\nRecords checked:&quot;, records_checked)\nprint(&quot;Status:\\nReview required&quot;)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Monthly data-quality note\n\nRecords checked: 1250\nStatus:\nReview required\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 stores the report title as a string.</li>\n<li>Line 2 stores a whole-number count.</li>\n<li>Line 4 displays the title.</li>\n<li>In line 5, <code>\\n</code> is an escape sequence meaning &quot;start a new line&quot;. Because it appears at the beginning of the string, it creates an empty line before <code>Records checked:</code>.</li>\n<li>In line 6, <code>\\n</code> appears inside the string, so <code>Review required</code> begins on the next output line.</li>\n<li>The characters <code>\\</code> and <code>n</code> work together inside a string. They are not displayed literally in this example.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Typing <code>/n</code> with a forward slash. The newline sequence uses a backslash: <code>\\n</code>.</li>\n<li>Placing <code>\\n</code> outside quotation marks.</li>\n<li>Adding many newlines and making output harder to scan.</li>\n<li>Confusing visual formatting with data correctness.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a blank line before the status section and change the status message to <code>Initial checks complete</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the final line to <code>print(&quot;Status:\\nReview required)</code> by deleting its closing quotation mark.</li>\n<li>Run the cell and observe the <code>SyntaxError</code>.</li>\n<li>Repair the missing quotation mark.</li>\n<li>Rerun the cell and verify the two-line status output.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why does <code>\\n</code> change the layout even though <code>print()</code> is called only once on the final line?</p>\n<h3>Exercise 2.1.2</h3>\n<p>Write a four-line printed summary for a fictional dataset:</p>\n<ul>\n<li>dataset title;</li>\n<li>number of rows received;</li>\n<li>number of rows retained;</li>\n<li>number excluded, calculated from the first two counts.</li>\n</ul>\n<p>Requirements:</p>\n<ul>\n<li><p>Store all case-specific values in variables.</p>\n</li>\n<li><p>Use labels that a colleague could understand.</p>\n</li>\n<li><p>Predict the result before running.</p>\n</li>\n<li><p>Create one <code>NameError</code>, then repair it.</p>\n</li>\n<li><p>Explain why a successful calculation does not prove that the exclusion rule was fair or appropriate.</p>\n</li>\n<li><p><a href=\"#\" data-link-placeholder=\"HINTS_A2_URL\">Open checkpoint hints</a></p>\n</li>\n<li><p><a href=\"#\" data-link-placeholder=\"SOLUTIONS_A2_URL\">Open checkpoint solution</a></p>\n</li>\n</ul>\n<hr>\n"
    }
  ],
  "1.13": [
    {
      "title": "Values, variables and meaningful names",
      "html": "<h3>Suggested book chapters for Part B</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Priority</th>\n<th>Book and chapter</th>\n<th>Suggested focus</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Core companion</td>\n<td><a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 2: Variables, expressions, and statements</a></td>\n<td>Read <strong>Values and types</strong>, <strong>Variables</strong>, <strong>Variable names and keywords</strong>, and <strong>Choosing mnemonic variable names</strong>. These sections directly support literals, assignment, reserved words and meaningful names.</td>\n</tr>\n<tr>\n<td>Recommended background</td>\n<td><a href=\"https://www.py4e.com/html3/01-intro\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 1</a></td>\n<td>Revisit <strong>Words and sentences</strong> for Python&#39;s reserved vocabulary and the idea that programmers create their own meaningful variable names.</td>\n</tr>\n<tr>\n<td>Optional text-focused companion</td>\n<td><a href=\"https://www.nltk.org/book/ch01.html\" target=\"_blank\" rel=\"noreferrer\">Bird, Klein and Loper, <em>Natural Language Processing with Python</em>, Chapter 1</a></td>\n<td>Read <strong>Section 2.3, Variables</strong>, and <strong>Section 2.4, Strings</strong>. Stop before the language-analysis sections if lists and corpora are unfamiliar.</td>\n</tr>\n</tbody></table></div>\n<h2>Constants and literals</h2>\n<p>A <strong>literal</strong> is a value written directly in code. Examples include <code>42</code>, <code>3.5</code>, <code>&quot;Copenhagen&quot;</code> and <code>True</code>. Python also has the special constant <code>None</code>, meaning no value is present. Section 13.1 later distinguishes <code>None</code> from zero, empty text and the string <code>&quot;None&quot;</code>.</p>\n<p>A <strong>constant</strong> is a value that should not change while a program is running. Python does not enforce ordinary constants. Programmers usually write a constant name in uppercase letters to communicate: &quot;Treat this as fixed.&quot; This is a convention, not a technical lock.</p>\n<p>Compare these roles:</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Code</th>\n<th>Role</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>250</code></td>\n<td>Integer literal</td>\n</tr>\n<tr>\n<td><code>&quot;Unknown&quot;</code></td>\n<td>String literal</td>\n</tr>\n<tr>\n<td><code>True</code></td>\n<td>Boolean literal</td>\n</tr>\n<tr>\n<td><code>total_records = 250</code></td>\n<td>Assignment of a literal to a variable</td>\n</tr>\n<tr>\n<td><code>MISSING_LABEL = &quot;Unknown&quot;</code></td>\n<td>Constant-by-convention assigned a string literal</td>\n</tr>\n</tbody></table></div>\n<h3>Example 2.2.1 - Use literals directly</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">print(&quot;Consultation responses&quot;)\nprint(320)\nprint(4.5)\nprint(True)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Consultation responses\n320\n4.5\nTrue\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 contains a string literal between quotation marks.</li>\n<li>Line 2 contains the integer literal <code>320</code>, which is a whole number.</li>\n<li>Line 3 contains the float literal <code>4.5</code>, which includes a decimal point.</li>\n<li>Line 4 contains the Boolean literal <code>True</code>. Its capital <code>T</code> is required.</li>\n<li>Each literal is given directly to <code>print()</code> instead of being stored under a variable name first.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Writing <code>true</code> instead of <code>True</code>.</li>\n<li>Assuming <code>&quot;320&quot;</code> and <code>320</code> are the same type because they look similar when printed.</li>\n<li>Using a comma as a decimal separator, such as <code>4,5</code>. Python interprets that as two separate values, not the number four and a half.</li>\n<li>Filling a real data script with unexplained literal numbers, sometimes called magic numbers.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the heading, whole number, decimal number and Boolean value. Predict which changes affect type as well as value.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>True</code> to <code>true</code>.</li>\n<li>Run the cell and observe the <code>NameError</code>.</li>\n<li>Repair it by restoring the capital <code>T</code>.</li>\n<li>Explain why Python treated lowercase <code>true</code> as a possible name rather than a Boolean literal.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which of the four literals requires quotation marks?</p>\n<h3>Example 2.2.2 - Use constants by convention</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">EXPECTED_COLUMNS = 12\nMISSING_LABEL = &quot;Unknown&quot;\nMAX_PREVIEW_ROWS = 10\n\nprint(&quot;Expected columns:&quot;, EXPECTED_COLUMNS)\nprint(&quot;Label for missing category:&quot;, MISSING_LABEL)\nprint(&quot;Maximum preview rows:&quot;, MAX_PREVIEW_ROWS)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Expected columns: 12\nLabel for missing category: Unknown\nMaximum preview rows: 10\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Lines 1 to 3 assign literal values to names written in uppercase.</li>\n<li>The uppercase style tells human readers that these settings should be treated as fixed.</li>\n<li><code>EXPECTED_COLUMNS</code> and <code>MAX_PREVIEW_ROWS</code> store integers.</li>\n<li><code>MISSING_LABEL</code> stores a string.</li>\n<li>Lines 5 to 7 display each constant-by-convention beside an explanatory label.</li>\n<li>Python would still allow a later assignment such as <code>EXPECTED_COLUMNS = 15</code>. The convention relies on programmer discipline.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Believing uppercase names cannot be changed by Python.</li>\n<li>Using a fixed label such as <code>&quot;Unknown&quot;</code> without documenting what it means.</li>\n<li>Repeating the literal <code>12</code> throughout a script instead of defining one clearly named setting.</li>\n<li>Choosing a constant value because it is convenient rather than because the case justifies it.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add <code>REPORTING_MONTH = &quot;October&quot;</code> and display it. Then change <code>MAX_PREVIEW_ROWS</code> to <code>20</code> in its defining line only.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the final line to <code>print(&quot;Maximum preview rows:&quot;, MAX_PREVIEWS_ROWS)</code>.</li>\n<li>Run the cell and observe the <code>NameError</code>.</li>\n<li>Compare the used name with the defined name.</li>\n<li>Repair the extra <code>S</code> so the names match exactly.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Is an uppercase Python name technically protected from reassignment?</p>\n<h3>Example 2.2.3 - Recognise the risk of unexplained literals</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">MINIMUM_ACCEPTABLE_COMPLETION = 80\ncompletion_rate = 76.5\ngap_to_threshold = MINIMUM_ACCEPTABLE_COMPLETION - completion_rate\n\nprint(&quot;Completion rate:&quot;, completion_rate)\nprint(&quot;Gap to stated threshold:&quot;, gap_to_threshold)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Completion rate: 76.5\nGap to stated threshold: 3.5\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 names the stated threshold instead of hiding the literal <code>80</code> inside a later calculation.</li>\n<li>Line 2 stores the observed completion rate as a float.</li>\n<li>Line 3 subtracts the observed rate from the threshold.</li>\n<li>Lines 5 and 6 display the observed rate and numerical gap.</li>\n<li>The calculation describes distance from a threshold. It does not explain who selected the threshold or whether it is appropriate.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Presenting a threshold as objective simply because it appears in code.</li>\n<li>Mixing percentages such as <code>80</code> with proportions such as <code>0.765</code>.</li>\n<li>Calling the result a failure without explaining the context.</li>\n<li>Reversing the subtraction and mislabelling the meaning of the result.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the completion rate to <code>84.0</code>. Predict the output. Then change the label <code>Gap to stated threshold</code> so it accurately describes a negative result.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Put quotation marks around the completion rate: <code>completion_rate = &quot;76.5&quot;</code>.</li>\n<li>Run the cell and observe the <code>TypeError</code> on the subtraction line.</li>\n<li>Repair it by removing the quotation marks or converting the text to a number with <code>float()</code>.</li>\n<li>Explain why numerical-looking text is still text.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What social or organisational question should accompany any coded threshold?</p>\n<h3>Exercise 2.2.1</h3>\n<p>Create three constants-by-convention for a fictional monthly report:</p>\n<ul>\n<li>the expected number of columns;</li>\n<li>the label used for an unreported category;</li>\n<li>the stated minimum number of responses.</li>\n</ul>\n<p>Display all three with clear labels. Then write two sentences explaining why uppercase names communicate intent but do not prevent reassignment.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_B1_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_B1_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Variables and assignment</h2>\n<p>A <strong>variable</strong> is a name that refers to a value. An <strong>assignment statement</strong> connects a name to a value with <code>=</code>.</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">variable_name = value\n</code></pre></div>\n<p>Read <code>=</code> in an assignment as &quot;is assigned&quot; or &quot;now refers to&quot;, not as &quot;is mathematically equal forever&quot;.</p>\n<h3>Example 2.2.4 - Assign and display values</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">city = &quot;Copenhagen&quot;\nsurvey_responses = 275\naverage_rating = 4.2\n\nprint(city)\nprint(survey_responses)\nprint(average_rating)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Copenhagen\n275\n4.2\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 assigns the string <code>&quot;Copenhagen&quot;</code> to <code>city</code>.</li>\n<li>Line 2 assigns the integer <code>275</code> to <code>survey_responses</code>.</li>\n<li>Line 3 assigns the float <code>4.2</code> to <code>average_rating</code>.</li>\n<li>The blank line visually separates assignments from output statements.</li>\n<li>Lines 5 to 7 display the value associated with each variable name.</li>\n<li>The variable names do not appear in the output because <code>print()</code> receives their values.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Reversing assignment as <code>&quot;Copenhagen&quot; = city</code>, which is invalid.</li>\n<li>Writing labels with spaces, such as <code>survey responses</code>.</li>\n<li>Using quotation marks around a variable name when you want its value.</li>\n<li>Assuming the variable contains a copy of its name.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change all three values to describe a different fictional case. Add labels to the three printed lines so another person can understand the output.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change line 2 to <code>275 = survey_responses</code>.</li>\n<li>Run the cell and observe the <code>SyntaxError</code>.</li>\n<li>Repair the order so the name is on the left and the assigned value is on the right.</li>\n<li>Say the repaired line aloud using &quot;is assigned&quot;.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which side of <code>=</code> normally contains the variable name in a basic assignment?</p>\n<h3>Example 2.2.5 - Reassign a variable</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">records_reviewed = 0\nprint(&quot;Initially reviewed:&quot;, records_reviewed)\n\nrecords_reviewed = 25\nprint(&quot;After first batch:&quot;, records_reviewed)\n\nrecords_reviewed = records_reviewed + 10\nprint(&quot;After second batch:&quot;, records_reviewed)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Initially reviewed: 0\nAfter first batch: 25\nAfter second batch: 35\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 creates <code>records_reviewed</code> with the initial integer value <code>0</code>.</li>\n<li>Line 2 displays that initial value.</li>\n<li>Line 4 assigns a new value, <code>25</code>, to the same name. The previous value is replaced.</li>\n<li>Line 5 displays the updated value.</li>\n<li>Line 7 first reads the current value <code>25</code>, adds <code>10</code>, and then assigns the result <code>35</code> back to the same variable.</li>\n<li>Line 8 displays the final value.</li>\n<li>Assignment changes what the name refers to. It is not a permanent mathematical equation.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Reading <code>records_reviewed = records_reviewed + 10</code> as an impossible algebra equation.</li>\n<li>Running only the final two lines in a fresh runtime, causing a <code>NameError</code>.</li>\n<li>Accidentally resetting the variable to <code>0</code> after updating it.</li>\n<li>Losing track of a value because notebook cells were run in a different order.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the first batch to <code>40</code> and the second batch increase to <code>15</code>. Predict all three output values before running.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>In a fresh cell, run only <code>records_reviewed = records_reviewed + 10</code> after restarting the runtime.</li>\n<li>Observe the <code>NameError</code> because no current value exists.</li>\n<li>Repair the state by assigning an initial value before trying to update it.</li>\n<li>Rerun from top to bottom.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why can the same variable name appear on both sides of <code>=</code> during an update?</p>\n<h3>Example 2.2.6 - Extend an audit trail with <code>+=</code></h3>\n<p><strong>Level:</strong> More advanced</p>\n<p>The augmented assignment operator <code>+=</code> updates a variable using its current value. With numbers it can increase a count. With strings it can extend text. This example uses text so that the update records a sequence of data-preparation actions.</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">audit_trail = &quot;File received&quot;\naudit_trail += &quot; -&gt; column names standardised&quot;\naudit_trail += &quot; -&gt; identifiers preserved as text&quot;\n\nprint(audit_trail)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">File received -&gt; column names standardised -&gt; identifiers preserved as text\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 creates the first version of the audit-trail string.</li>\n<li>Line 2 reads the current string, joins another string to its end, and assigns the combined value back to <code>audit_trail</code>.</li>\n<li>For this string example, <code>audit_trail += &quot;...&quot;</code> is a shorter form of <code>audit_trail = audit_trail + &quot;...&quot;</code>.</li>\n<li>Line 3 performs a second update. The order matters because the text is being built from left to right.</li>\n<li>The blank line separates preparation from output.</li>\n<li>The final line displays the completed trail.</li>\n<li>This is only a teaching illustration. A real audit trail should also record dates, responsible people, versions and reproducible operations.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Using <code>=+</code> instead of <code>+=</code>. These are not interchangeable operators.</li>\n<li>Forgetting spaces around the arrow text and producing unreadable joined words.</li>\n<li>Running an update before the initial assignment, which raises <code>NameError</code>.</li>\n<li>Treating a manually written trail as proof that the described work actually happened.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a third action stating that category labels were reviewed. Then replace the arrows with newline characters so that every action appears on its own line.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the second update to <code>audit_trail =+ &quot; -&gt; column names standardised&quot;</code>.</li>\n<li>Run the code. Python accepts the line, but it replaces the earlier text instead of extending it.</li>\n<li>Repair the operator to <code>+=</code>.</li>\n<li>Rerun from the first line and confirm that all stages appear in order.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is this break especially important to test even though Python produces no traceback?</p>\n<h3>Example 2.2.7 - Derive new variables without losing originals</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">total_records = 640\nmissing_records = 32\nduplicate_records = 8\n\nusable_records = total_records - missing_records - duplicate_records\nusable_percentage = usable_records / total_records * 100\n\nprint(&quot;Total records:&quot;, total_records)\nprint(&quot;Usable records:&quot;, usable_records)\nprint(&quot;Usable percentage:&quot;, usable_percentage)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Total records: 640\nUsable records: 600\nUsable percentage: 93.75\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Lines 1 to 3 store the three source counts.</li>\n<li>Line 5 calculates a new value by subtracting missing and duplicate records from the total.</li>\n<li>The result is assigned to a new name, <code>usable_records</code>, so the original counts remain available.</li>\n<li>Line 6 divides usable records by total records and multiplies by <code>100</code> to express the proportion as a percentage.</li>\n<li>Lines 8 to 10 display selected inputs and derived results.</li>\n<li>The code assumes missing records and duplicate records are separate groups. If the same record could belong to both groups, this subtraction could double-count exclusions.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Overwriting <code>total_records</code> with a cleaned count and losing the original total.</li>\n<li>Subtracting overlapping categories as if they were independent.</li>\n<li>Dividing by the wrong count.</li>\n<li>Labelling a calculated value &quot;usable&quot; without defining what usable means.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the counts to <code>1000</code>, <code>65</code> and <code>15</code>. Predict the usable count and percentage. Then rename <code>usable_records</code> to a name that fits a different documented rule.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>total_records</code> to <code>0</code> and run the cell.</li>\n<li>Observe that the count calculation succeeds but the percentage calculation raises <code>ZeroDivisionError</code>.</li>\n<li>Repair the example by restoring a positive total.</li>\n<li>Explain why a zero total needs a case decision, not only a syntactic fix.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which assumption in this calculation should be checked against the real dataset?</p>\n<h3>Exercise 2.2.2</h3>\n<p>Create a short script about a fictional organisation that receives, excludes and retains records. Your script must:</p>\n<ul>\n<li><p>use at least four meaningful variables;</p>\n</li>\n<li><p>use <code>+=</code> once to extend a short text audit trail or update a count;</p>\n</li>\n<li><p>preserve the original total;</p>\n</li>\n<li><p>calculate one new count and one percentage;</p>\n</li>\n<li><p>display labelled results;</p>\n</li>\n<li><p>include one deliberate <code>NameError</code> and its repair in your notes;</p>\n</li>\n<li><p>state one assumption that the arithmetic makes about the categories.</p>\n</li>\n<li><p><a href=\"#\" data-link-placeholder=\"HINTS_B2_URL\">Open checkpoint hints</a></p>\n</li>\n<li><p><a href=\"#\" data-link-placeholder=\"SOLUTIONS_B2_URL\">Open checkpoint solution</a></p>\n</li>\n</ul>\n<hr>\n<h2>Reserved words and built-in names</h2>\n<p>Python reserves certain words for its own grammar. These are usually called <strong>keywords</strong> or <strong>reserved words</strong>. You cannot use a keyword as an ordinary variable name.</p>\n<p>Common Python keywords include:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">False  None  True  and  as  assert  async  await  break  class\ncontinue  def  del  elif  else  except  finally  for  from\nglobal  if  import  in  is  lambda  nonlocal  not  or  pass\nraise  return  try  while  with  yield\n</code></pre></div>\n<p>You do not need to memorise this list now. You will learn the words when they become useful.</p>\n<p>Python also has <strong>built-in names</strong> such as <code>print</code>, <code>input</code>, <code>int</code>, <code>float</code>, <code>str</code> and <code>type</code>. These are not all reserved keywords, so Python may allow you to use them as variable names. Doing so is still a poor choice because your variable can hide the built-in function.</p>\n<p>Current Python versions also have <strong>soft keywords</strong> such as <code>match</code>, <code>case</code>, <code>_</code> and <code>type</code>. A soft keyword has a special role only in a particular grammatical context. You do not need these contexts in Python Foundations I. The <code>keyword</code> module also provides <code>issoftkeyword()</code> for version-appropriate checks. Continue to avoid <code>type</code> as a variable name because it is an important built-in function in this tutorial.</p>\n<h3>Example 2.2.8 - Use a reserved word inside a meaningful name</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">class_size = 28\nreturn_rate = 91.5\nsurvey_from_students = &quot;completed&quot;\n\nprint(&quot;Class size:&quot;, class_size)\nprint(&quot;Return rate:&quot;, return_rate)\nprint(&quot;Survey status:&quot;, survey_from_students)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Class size: 28\nReturn rate: 91.5\nSurvey status: completed\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>class</code> is a keyword, but <code>class_size</code> is a different, valid name.</li>\n<li><code>return</code> is a keyword, but <code>return_rate</code> is valid.</li>\n<li><code>from</code> is a keyword, but it can appear as one part of <code>survey_from_students</code>.</li>\n<li>The underscore joins words without creating spaces.</li>\n<li>Lines 5 to 7 display the values using clear labels.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Using <code>class</code>, <code>return</code> or <code>from</code> alone as a variable name.</li>\n<li>Assuming a word is forbidden anywhere inside a longer name.</li>\n<li>Adding punctuation such as <code>class-size</code> instead of an underscore.</li>\n<li>Choosing an awkward name only to avoid a keyword when a clearer alternative exists.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a valid variable whose name contains the word <code>for</code>, such as <code>records_for_review</code>, and display it.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>class_size = 28</code> to <code>class = 28</code>.</li>\n<li>Run the cell and observe the <code>SyntaxError</code>.</li>\n<li>Repair the name by restoring <code>class_size</code> on both the assignment and print lines.</li>\n<li>Rerun the complete cell.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is <code>class_size</code> allowed even though <code>class</code> is a keyword?</p>\n<h3>Example 2.2.9 - Check whether words are Python keywords</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p>Python includes a small standard module named <code>keyword</code> that can check a word for you.</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">import keyword\n\nprint(keyword.iskeyword(&quot;class&quot;))\nprint(keyword.iskeyword(&quot;records&quot;))\nprint(keyword.iskeyword(&quot;True&quot;))\nprint(keyword.iskeyword(&quot;print&quot;))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">True\nFalse\nTrue\nFalse\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>import keyword</code> makes Python&#39;s standard <code>keyword</code> module available in this cell.</li>\n<li><code>keyword.iskeyword(&quot;class&quot;)</code> asks whether the string <code>&quot;class&quot;</code> is a keyword. The result is <code>True</code>.</li>\n<li><code>&quot;records&quot;</code> is not a keyword, so the result is <code>False</code>.</li>\n<li><code>True</code> is a keyword and literal when written without quotation marks. Here the string <code>&quot;True&quot;</code> is being checked as a possible name.</li>\n<li><code>print</code> is not a keyword. It is a built-in function, so <code>iskeyword()</code> returns <code>False</code> even though using <code>print</code> as a variable name is unwise.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Omitting <code>import keyword</code> and receiving a <code>NameError</code> for <code>keyword</code>.</li>\n<li>Concluding that every non-keyword is a good variable name.</li>\n<li>Writing <code>keyword.iskeyword(class)</code> without quotation marks, which produces invalid syntax.</li>\n<li>Confusing a Boolean result with the string <code>&quot;True&quot;</code> or <code>&quot;False&quot;</code>.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Check <code>if</code>, <code>dataset</code>, <code>input</code> and <code>while</code>. Predict each result before running.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Comment out or delete <code>import keyword</code> and restart the runtime.</li>\n<li>Run the remaining lines and observe the <code>NameError</code>.</li>\n<li>Repair the example by restoring and running the import line.</li>\n<li>Explain why a notebook might work before a restart but fail afterwards if an import cell was skipped.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why does <code>keyword.iskeyword(&quot;print&quot;)</code> return <code>False</code> even though you should avoid using <code>print</code> as a variable name?</p>\n<h3>Example 2.2.10 - Avoid hiding a built-in function</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">record_type = &quot;Consultation&quot;\nrecord_count = 42\n\nprint(&quot;Record type:&quot;, record_type)\nprint(&quot;Type of record_count:&quot;, type(record_count))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Record type: Consultation\nType of record_count: &lt;class &#39;int&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>record_type</code> is a descriptive variable name.</li>\n<li><code>record_count</code> stores an integer.</li>\n<li>Line 4 displays the case label.</li>\n<li>Line 5 calls the built-in <code>type()</code> function to inspect <code>record_count</code>.</li>\n<li>Because the variable is named <code>record_type</code>, the useful built-in name <code>type</code> remains available.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Creating a variable called <code>type</code>, <code>str</code>, <code>int</code>, <code>input</code> or <code>print</code>.</li>\n<li>Assuming Python will warn you immediately when a built-in has been hidden.</li>\n<li>Restarting only one cell mentally. Runtime state persists until it is restarted or overwritten.</li>\n<li>Repairing the assignment but forgetting to rerun it.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a float variable called <code>completion_percentage</code> and use <code>type()</code> to inspect it.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Add <code>type = &quot;Consultation&quot;</code> before the final print line.</li>\n<li>Run the cell. The later call <code>type(record_count)</code> should raise <code>TypeError: &#39;str&#39; object is not callable</code>.</li>\n<li>Repair the code by renaming the variable to <code>record_type</code>.</li>\n<li>If the error remains, restart the runtime or run <code>del type</code>, then rerun the repaired cell. The old assignment may still exist in notebook state.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why can a legal variable name still be a bad variable name?</p>\n<h3>Exercise 2.2.3</h3>\n<p>For each proposed name, classify it as:</p>\n<ul>\n<li>invalid keyword;</li>\n<li>valid but unwise built-in name;</li>\n<li>valid and meaningful;</li>\n<li>valid but unclear.</li>\n</ul>\n<p>Names: <code>if</code>, <code>data</code>, <code>input</code>, <code>missing_record_count</code>, <code>class</code>, <code>class_label</code>, <code>x</code>, <code>float</code>, <code>survey_year</code>.</p>\n<p>Then write and run a short check using the <code>keyword</code> module for at least four names.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_B3_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_B3_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Variable naming rules and meaningful names</h2>\n<p>Python variable names must follow technical rules. Good names must also help humans understand the code.</p>\n<h3>Technical naming rules</h3>\n<p>A basic Python variable name:</p>\n<ul>\n<li>may contain letters, digits and underscores;</li>\n<li>must not begin with a digit;</li>\n<li>must not contain spaces;</li>\n<li>must not contain a hyphen or most other punctuation;</li>\n<li>must not be a Python keyword;</li>\n<li>is case-sensitive, so <code>records</code>, <code>Records</code> and <code>RECORDS</code> are three different names.</li>\n</ul>\n<h3>Style for this course</h3>\n<p>Use <strong>snake case</strong> for ordinary variables: lowercase words joined by underscores.</p>\n<p>Good examples:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">total_records\nmissing_response_count\nreporting_month\naverage_waiting_time\n</code></pre></div>\n<p>Use uppercase snake case for constants-by-convention:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">EXPECTED_COLUMNS\nMISSING_LABEL\n</code></pre></div>\n<p>Avoid names such as <code>x</code>, <code>thing</code>, <code>data1</code>, <code>temp</code> and <code>final_final</code> unless the context genuinely makes them clear.</p>\n<h3>Example 2.2.11 - Use valid snake-case names</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">survey_year = 2026\nresponses_received = 315\nmissing_response_count = 12\n\nprint(&quot;Survey year:&quot;, survey_year)\nprint(&quot;Responses received:&quot;, responses_received)\nprint(&quot;Missing responses:&quot;, missing_response_count)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Survey year: 2026\nResponses received: 315\nMissing responses: 12\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>survey_year</code> uses two lowercase words joined by an underscore.</li>\n<li><code>responses_received</code> describes both what is counted and the stage of the process.</li>\n<li><code>missing_response_count</code> is longer, but it communicates that the value is a count rather than the missing records themselves.</li>\n<li>Each print label uses normal spaces because output is written for people.</li>\n<li>Variable names use underscores because spaces are not permitted inside them.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Writing <code>survey year = 2026</code> with a space.</li>\n<li>Writing <code>2026_survey = 2026</code>, which begins with a digit.</li>\n<li>Writing <code>responses-received</code>, which Python reads as subtraction between two names.</li>\n<li>Making names so long that the code becomes difficult to scan.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add valid variables for the survey title and the number of duplicate responses. Display both.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>survey_year</code> to <code>survey-year</code> on the assignment line.</li>\n<li>Run the cell and inspect the syntax-related error.</li>\n<li>Repair the hyphen by replacing it with an underscore.</li>\n<li>Check that every later use of the variable matches the repaired name.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why can output labels contain spaces while variable names cannot?</p>\n<h3>Example 2.2.12 - Replace vague names with meaningful ones</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">total_responses = 500\ninvalid_responses = 35\nvalid_responses = total_responses - invalid_responses\n\nprint(&quot;Valid responses:&quot;, valid_responses)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Valid responses: 465\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>total_responses</code> communicates the starting count.</li>\n<li><code>invalid_responses</code> communicates what will be subtracted, although a real project should define the validation rule.</li>\n<li><code>valid_responses</code> describes the result of the calculation.</li>\n<li>The calculation can be read almost as an English sentence.</li>\n<li>The output label matches the meaning of the result.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Using <code>a</code>, <code>b</code> and <code>c</code>, which forces readers to remember meanings.</li>\n<li>Calling all tables or values <code>data</code> without distinguishing their roles.</li>\n<li>Using <code>valid_responses</code> before documenting what &quot;valid&quot; means.</li>\n<li>Renaming a variable on its assignment line but not where it is later used.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Rename <code>invalid_responses</code> to a more precise name for one actual rule, such as <code>responses_missing_consent</code>. Update every line that depends on it.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Rename the assignment to <code>responses_missing_consent = 35</code> but leave <code>invalid_responses</code> in the calculation.</li>\n<li>Run the cell and observe the <code>NameError</code>.</li>\n<li>Repair the calculation so it uses the new name consistently.</li>\n<li>Use Colab&#39;s search feature to check that the old name no longer appears.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>How can a precise variable name expose an assumption in a data-cleaning decision?</p>\n<h3>Example 2.2.13 - Notice case sensitivity</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">records = 120\nRecords = 15\nRECORDS = 5\n\nprint(records)\nprint(Records)\nprint(RECORDS)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">120\n15\n5\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Python treats <code>records</code>, <code>Records</code> and <code>RECORDS</code> as three different names.</li>\n<li>Each assignment therefore stores a different integer.</li>\n<li>The print lines retrieve three different values.</li>\n<li>Although the code runs, this naming choice is difficult to read and easy to misuse.</li>\n<li>A better script would use distinct semantic names such as <code>total_records</code>, <code>flagged_records</code> and <code>MAX_RECORDS</code>.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Changing capitalisation accidentally and creating a new variable.</li>\n<li>Assuming uppercase always means Python has created a constant.</li>\n<li>Using names that differ only by case.</li>\n<li>Treating code that runs as automatically well designed.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Replace the three confusing names with <code>total_records</code>, <code>flagged_records</code> and <code>MAX_PREVIEW_RECORDS</code>. Update the output labels to match.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the first print line to <code>print(REcords)</code>.</li>\n<li>Run the cell and observe the <code>NameError</code>.</li>\n<li>Repair the capitalisation or, preferably, complete the meaningful renaming task.</li>\n<li>Explain why autocomplete cannot decide which meaning you intended.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Are two names that differ only in capitalisation the same variable in Python?</p>\n<h3>Exercise 2.2.4</h3>\n<p>Rewrite these unclear or invalid names as valid, meaningful snake-case names:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">1stMonth\nMissing Values\ncase-type\nx\nDATA\nfinal final count\n</code></pre></div>\n<p>For each rewritten name, add a one-sentence definition of what the value represents. A name is not fully meaningful if its underlying concept remains undefined.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_B4_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_B4_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n"
    }
  ],
  "1.14": [
    {
      "title": "Expressions, arithmetic and precedence",
      "html": "<h3>Suggested book chapters for Part C</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Priority</th>\n<th>Book and chapter</th>\n<th>Suggested focus</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Core companion</td>\n<td><a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 2: Variables, expressions, and statements</a></td>\n<td>Read <strong>Statements</strong>, <strong>Operators and operands</strong>, <strong>Expressions</strong>, <strong>Order of operations</strong>, <strong>Modulus operator</strong> and <strong>String operations</strong>. These sections match the Part C sequence closely.</td>\n</tr>\n<tr>\n<td>Optional practice</td>\n<td><a href=\"https://www.nltk.org/book/ch01.html\" target=\"_blank\" rel=\"noreferrer\">Bird, Klein and Loper, <em>Natural Language Processing with Python</em>, Chapter 1</a></td>\n<td>Use <strong>Section 1.1</strong> and Exercises 1 to 3 for extra practice with arithmetic expressions, parentheses and operator behaviour. The later corpus exercises are outside Day 1.</td>\n</tr>\n</tbody></table></div>\n<h2>Expressions and statements</h2>\n<p>An <strong>expression</strong> is code that Python can evaluate to produce a value. Examples include:</p>\n<ul>\n<li>a literal, such as <code>10</code>;</li>\n<li>a variable reference, such as <code>total_records</code>;</li>\n<li>a calculation, such as <code>total_records - missing_records</code>;</li>\n<li>a function call that returns a value, such as <code>type(total_records)</code>.</li>\n</ul>\n<p>A <strong>statement</strong> is an instruction that Python executes. An assignment such as <code>total_records = 250</code> is a statement. A call such as <code>print(total_records)</code> can be used as a statement.</p>\n<p>At beginner level, use this practical distinction:</p>\n<blockquote>\n<p>An expression answers &quot;what value does this produce?&quot; A statement answers &quot;what instruction is Python carrying out?&quot;</p>\n</blockquote>\n<h3>Example 2.3.1 - Put an expression inside a statement</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">received = 90\nexcluded = 7\nprint(received - excluded)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">83\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 is an assignment statement. The literal expression <code>90</code> produces the assigned value.</li>\n<li>Line 2 is another assignment statement.</li>\n<li>In line 3, <code>received - excluded</code> is an arithmetic expression.</li>\n<li>Python evaluates the expression to <code>83</code>.</li>\n<li><code>print(...)</code> then displays that result.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Believing Python displays the result of every assignment automatically.</li>\n<li>Putting the expression in quotation marks, which displays text instead of calculating.</li>\n<li>Calling the expression a variable even though it has no stored name.</li>\n<li>Reversing the operands and changing the meaning.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change both assigned values and predict the expression result. Then assign the result to <code>retained</code> before printing it.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the final line to <code>print(&quot;received - excluded&quot;)</code>.</li>\n<li>Run the cell. It will run without an error but display the expression as text.</li>\n<li>Repair this logical mistake by removing the quotation marks.</li>\n<li>Explain why the absence of an error does not guarantee the intended result.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which part of the final line produces the numerical value?</p>\n<h3>Example 2.3.2 - Store the result of an expression</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">total_records = 300\nmissing_records = 21\ncomplete_records = total_records - missing_records\ncompletion_rate = complete_records / total_records * 100\n\nprint(&quot;Complete records:&quot;, complete_records)\nprint(&quot;Completion rate:&quot;, completion_rate)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Complete records: 279\nCompletion rate: 93.0\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The first two statements establish source values.</li>\n<li>In line 3, the expression on the right produces <code>279</code>.</li>\n<li>The assignment stores that result under <code>complete_records</code>.</li>\n<li>Line 4 contains a longer expression. Python divides <code>279</code> by <code>300</code>, then multiplies by <code>100</code>.</li>\n<li>The result <code>93.0</code> is stored as a float.</li>\n<li>The two print statements display stored results with labels.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Dividing <code>total_records</code> by <code>complete_records</code> and calculating the opposite ratio.</li>\n<li>Forgetting <code>* 100</code> when the label says rate as a percentage.</li>\n<li>Multiplying one source value by <code>100</code> before dividing and losing clarity.</li>\n<li>Confusing a percentage with a percentage point difference.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add <code>duplicate_records = 9</code> and modify the complete-record calculation to subtract both missing and duplicate records. State the assumption you are making.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Put quotation marks around <code>total_records</code> in line 1.</li>\n<li>Run the cell and observe the <code>TypeError</code> on the subtraction line.</li>\n<li>Repair the value by storing it as the integer <code>300</code>.</li>\n<li>Use <code>type(total_records)</code> in a separate cell to confirm the repaired type.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is <code>completion_rate</code> assigned a float even though the source counts are integers?</p>\n<h3>Example 2.3.3 - Turn a messy field label into a consistent name</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">raw_field_label = &quot;  Preferred TRANSPORT mode  &quot;\ntrimmed_label = raw_field_label.strip()\nlowercase_label = trimmed_label.lower()\nfield_name = lowercase_label.replace(&quot; &quot;, &quot;_&quot;)\n\nprint(&quot;Raw label:&quot;, raw_field_label)\nprint(&quot;Trimmed label:&quot;, trimmed_label)\nprint(&quot;Proposed field name:&quot;, field_name)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Raw label:   Preferred TRANSPORT mode  \nTrimmed label: Preferred TRANSPORT mode\nProposed field name: preferred_transport_mode\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 preserves the original label, including its uneven capitalisation and extra spaces.</li>\n<li><code>raw_field_label.strip()</code> is a method-call expression. It produces a new string without spaces at the beginning or end.</li>\n<li><code>trimmed_label.lower()</code> produces another string in lowercase.</li>\n<li><code>lowercase_label.replace(&quot; &quot;, &quot;_&quot;)</code> replaces each ordinary space with an underscore.</li>\n<li>Each expression result is assigned to a new variable, so the transformation can be inspected stage by stage.</li>\n<li>The final three statements display evidence of the original value, the intermediate value and the proposed Python-friendly name.</li>\n<li><code>strip()</code>, <code>lower()</code> and <code>replace()</code> are ready-made string methods. You are calling existing tools, not writing your own function yet.</li>\n<li>This mechanical transformation is only a proposal. A human still needs to check spelling, meaning, abbreviations and whether two distinct source labels would be collapsed into one category.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Overwriting the raw value and losing evidence of what was received.</li>\n<li>Forgetting the parentheses after a method name, such as writing <code>.strip</code> instead of <code>.strip()</code>.</li>\n<li>Assuming one space replacement handles tabs, punctuation or repeated internal spaces perfectly.</li>\n<li>Treating standardisation as a neutral operation when it may merge meaningful distinctions.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the raw label to <code>&quot;  SERVICE   Access? &quot;</code>. Predict the proposed name. Identify what remains imperfect, then add one more documented <code>replace()</code> call to remove the question mark.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>trimmed_label = raw_field_label.strip()</code> to <code>trimmed_label = raw_field_label.strip</code> by removing the parentheses.</li>\n<li>Run the cell and observe that <code>trimmed_label</code> now refers to a method rather than the cleaned string. A later line raises an error because a method object has no <code>.lower()</code> string operation.</li>\n<li>Repair the call by restoring <code>strip()</code>.</li>\n<li>Use <code>type(trimmed_label)</code> before and after the repair to compare the values.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is preserving <code>raw_field_label</code> useful when reviewing a data-cleaning decision?</p>\n<h3>Exercise 2.3.1</h3>\n<p>For each line below, decide whether its main role is an expression, an assignment statement or a print statement containing an expression:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">42\ntotal = 42\ntotal - missing\ncomplete = total - missing\nprint(total - missing)\n</code></pre></div>\n<p>Then write a short script that creates two source variables, derives one new variable through an expression, and displays it.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_C1_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_C1_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Arithmetic operators</h2>\n<p>Python uses arithmetic operators to build numerical expressions.</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Operator</th>\n<th>Meaning</th>\n<th>Example</th>\n<th align=\"right\">Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>+</code></td>\n<td>Addition</td>\n<td><code>7 + 3</code></td>\n<td align=\"right\"><code>10</code></td>\n</tr>\n<tr>\n<td><code>-</code></td>\n<td>Subtraction</td>\n<td><code>7 - 3</code></td>\n<td align=\"right\"><code>4</code></td>\n</tr>\n<tr>\n<td><code>*</code></td>\n<td>Multiplication</td>\n<td><code>7 * 3</code></td>\n<td align=\"right\"><code>21</code></td>\n</tr>\n<tr>\n<td><code>/</code></td>\n<td>Division</td>\n<td><code>7 / 2</code></td>\n<td align=\"right\"><code>3.5</code></td>\n</tr>\n<tr>\n<td><code>//</code></td>\n<td>Floor division</td>\n<td><code>7 // 2</code></td>\n<td align=\"right\"><code>3</code></td>\n</tr>\n<tr>\n<td><code>%</code></td>\n<td>Remainder, modulo</td>\n<td><code>7 % 2</code></td>\n<td align=\"right\"><code>1</code></td>\n</tr>\n<tr>\n<td><code>**</code></td>\n<td>Exponentiation</td>\n<td><code>2 ** 3</code></td>\n<td align=\"right\"><code>8</code></td>\n</tr>\n</tbody></table></div>\n<p>Important points:</p>\n<ul>\n<li><code>/</code> produces a float in ordinary Python 3 division.</li>\n<li><code>//</code> discards the fractional part by rounding down to the next whole-number boundary.</li>\n<li><code>%</code> means remainder. It does not mean percentage.</li>\n<li><code>**</code> means &quot;raised to the power of&quot;. Python does not use <code>^</code> for exponentiation.</li>\n</ul>\n<h3>Example 2.3.4 - Calculate basic data-quality counts</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">received = 240\nmissing = 18\nduplicates = 6\n\nflagged = missing + duplicates\nretained = received - flagged\n\nprint(&quot;Flagged records:&quot;, flagged)\nprint(&quot;Retained records:&quot;, retained)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Flagged records: 24\nRetained records: 216\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The first three lines store source counts.</li>\n<li><code>missing + duplicates</code> uses addition to calculate a combined flagged count.</li>\n<li><code>received - flagged</code> uses subtraction to calculate the remaining count.</li>\n<li>The output statements label both derived values.</li>\n<li>The arithmetic assumes that no record appears in both the missing and duplicate counts.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Adding counts that overlap and double-counting records.</li>\n<li>Subtracting the total from the flagged count.</li>\n<li>Using <code>+</code> where the case requires subtraction.</li>\n<li>Accepting a plausible output without checking the category definitions.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the source values to <code>500</code>, <code>35</code> and <code>10</code>. Predict both derived counts. Then rename <code>retained</code> if the records have not yet passed every quality check.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>flagged = missing + duplicates</code> to <code>flagged = missing - duplicates</code>.</li>\n<li>Run the code. It will produce output without an error, but the flagged total will be logically wrong.</li>\n<li>Repair the operator to <code>+</code>.</li>\n<li>Explain why testing must include expected values, not only checking for error messages.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which assumption determines whether the addition is valid?</p>\n<h3>Example 2.3.5 - Compare division, floor division and remainder</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">records_to_review = 125\nbatch_size = 20\n\nexact_batches = records_to_review / batch_size\nfull_batches = records_to_review // batch_size\nremaining_records = records_to_review % batch_size\n\nprint(&quot;Exact division result:&quot;, exact_batches)\nprint(&quot;Complete batches:&quot;, full_batches)\nprint(&quot;Records left over:&quot;, remaining_records)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Exact division result: 6.25\nComplete batches: 6\nRecords left over: 5\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The source values say that 125 records must be placed into batches of 20.</li>\n<li><code>/</code> calculates the exact numerical division, <code>6.25</code>.</li>\n<li><code>//</code> calculates how many complete batches fit, <code>6</code>.</li>\n<li><code>%</code> calculates the remainder after those complete batches, <code>5</code>.</li>\n<li>The three operations answer different questions about the same values.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Calling <code>6.25</code> a possible number of complete batches.</li>\n<li>Assuming <code>%</code> converts a number to a percentage.</li>\n<li>Using floor division when an accurate average is required.</li>\n<li>Forgetting that <code>//</code> with negative numbers has additional behaviour not needed in this example.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the batch size to <code>12</code>. Predict the exact result, complete batches and remainder before running.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>batch_size</code> to <code>0</code>.</li>\n<li>Run the cell and observe <code>ZeroDivisionError</code> on the first division.</li>\n<li>Repair it with a positive batch size.</li>\n<li>Explain why a zero batch size is a problem in the case, not merely in Python.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which operator tells you how many records remain after creating complete batches?</p>\n<h3>Example 2.3.6 - Calculate a rate and a projected count</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">eligible_residents = 800\nresponses_received = 520\nnext_round_multiplier = 1.10\n\nresponse_rate = responses_received / eligible_residents * 100\nprojected_responses = responses_received * next_round_multiplier\n\nprint(&quot;Response rate:&quot;, response_rate)\nprint(&quot;Projected responses:&quot;, projected_responses)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Response rate: 65.0\nProjected responses: 572.0\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>eligible_residents</code> is the stated denominator for the response rate.</li>\n<li><code>responses_received / eligible_residents</code> calculates a proportion.</li>\n<li>Multiplying by <code>100</code> expresses the proportion as a percentage.</li>\n<li><code>1.10</code> represents 110 per cent of the current response count, which is a 10 per cent increase.</li>\n<li>The projected count is a float because multiplication involves a float.</li>\n<li>A projection is an assumption-based scenario, not a prediction guaranteed by the data.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Using <code>10</code> instead of <code>1.10</code> and multiplying the count tenfold.</li>\n<li>Using the wrong denominator for the response rate.</li>\n<li>Reporting <code>572.0</code> as if people or responses can be fractional without deciding how to round.</li>\n<li>Describing a simple scenario calculation as evidence that growth will occur.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the multiplier to represent a 5 per cent decrease. Hint: the multiplier will be below <code>1</code>. Predict the projected count.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>eligible_residents</code> to <code>&quot;800&quot;</code>.</li>\n<li>Run the cell and observe the <code>TypeError</code> during division.</li>\n<li>Repair the value as an integer or use <code>int(&quot;800&quot;)</code>.</li>\n<li>Explain why data imported from a file may contain numerical-looking text that needs conversion.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What assumption is encoded in <code>next_round_multiplier</code>?</p>\n<h3>Example 2.3.7 - Use exponentiation in a simple growth scenario</h3>\n<p><strong>Level:</strong> Optional advanced example</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">initial_count = 100\ngrowth_factor = 1.05\nnumber_of_periods = 2\n\nprojected_count = initial_count * growth_factor ** number_of_periods\nprint(&quot;Projected count after two periods:&quot;, projected_count)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Projected count after two periods: 110.25\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>initial_count</code> stores the starting value.</li>\n<li><code>growth_factor = 1.05</code> represents a 5 per cent increase per period.</li>\n<li><code>number_of_periods = 2</code> states that the factor is applied twice.</li>\n<li><code>growth_factor ** number_of_periods</code> calculates <code>1.05</code> raised to the power of <code>2</code>.</li>\n<li>Multiplying by <code>initial_count</code> produces the scenario value <code>110.25</code>.</li>\n<li>This mathematical scenario does not establish that real social behaviour follows a constant growth rate.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Using <code>^</code> instead of <code>**</code>. In Python, <code>^</code> has a different meaning.</li>\n<li>Multiplying by <code>1.05 * 2</code>, which is not the same as compounding twice.</li>\n<li>Forgetting that a count may need a documented rounding decision.</li>\n<li>treating a scenario as a forecast without evidence.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the number of periods to <code>3</code> and predict whether the result will be below or above <code>115</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Replace <code>**</code> with <code>^</code>.</li>\n<li>Run the cell and observe a <code>TypeError</code> because bitwise exclusive OR is not defined between these float and integer values.</li>\n<li>Repair the operator to <code>**</code>.</li>\n<li>Add a comment stating the assumption of a constant 5 per cent increase.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is a compounded scenario analytically different from simply adding 5 twice?</p>\n<h3>Exercise 2.3.2</h3>\n<p>An organisation has <code>387</code> records and reviews them in batches of <code>25</code>.</p>\n<p>Calculate and display:</p>\n<ul>\n<li>the exact division result;</li>\n<li>the number of complete batches;</li>\n<li>the number of remaining records;</li>\n<li>the percentage represented by the remainder.</li>\n</ul>\n<p>Predict the output first. Then deliberately replace one numerical value with a string, record the resulting error type, and repair it.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_C2_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_C2_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Operator precedence and parentheses</h2>\n<p>When an expression contains several operators, Python needs rules for deciding which operation happens first. These rules are called <strong>operator precedence</strong>.</p>\n<p>For the arithmetic in this tutorial, use this simplified order:</p>\n<ol>\n<li>Parentheses: <code>(...)</code></li>\n<li>Exponentiation: <code>**</code></li>\n<li>Multiplication, division, floor division and remainder: <code>*</code>, <code>/</code>, <code>//</code>, <code>%</code></li>\n<li>Addition and subtraction: <code>+</code>, <code>-</code></li>\n</ol>\n<p>Operators at the same level are usually evaluated from left to right. Exponentiation has special right-associative behaviour, which is outside the core requirement. Parentheses remain the clearest way to communicate intended grouping.</p>\n<h3>Example 2.3.8 - See how multiplication happens before addition</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">without_parentheses = 1200 + 350 * 4\nwith_parentheses = (1200 + 350) * 4\n\nprint(&quot;Without parentheses:&quot;, without_parentheses)\nprint(&quot;With parentheses:&quot;, with_parentheses)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Without parentheses: 2600\nWith parentheses: 6200\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>In line 1, multiplication has higher precedence than addition.</li>\n<li>Python calculates <code>350 * 4</code> as <code>1400</code>, then adds <code>1200</code>, producing <code>2600</code>.</li>\n<li>In line 2, parentheses take priority.</li>\n<li>Python calculates <code>1200 + 350</code> as <code>1550</code>, then multiplies by <code>4</code>, producing <code>6200</code>.</li>\n<li>The output demonstrates that the same numbers and operators can represent different calculations when grouping changes.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Assuming Python always works strictly from left to right.</li>\n<li>Adding parentheses without checking whether they match the real formula.</li>\n<li>Treating parentheses as decoration rather than part of the expression&#39;s meaning.</li>\n<li>Using a plausible result without comparing it with a hand-calculated expectation.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change <code>4</code> to <code>6</code>. Predict both results. Then write one sentence describing a situation in which each formula could be appropriate.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Remove the closing parenthesis from line 2.</li>\n<li>Run the cell and observe the <code>SyntaxError</code>.</li>\n<li>Repair the unmatched parenthesis.</li>\n<li>Count opening and closing parentheses before running again.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which operation is performed first in <code>1200 + 350 * 4</code>?</p>\n<h3>Example 2.3.9 - Make a percentage formula explicit</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">missing_records = 18\nduplicate_records = 6\ntotal_records = 240\n\nproblem_rate = (missing_records + duplicate_records) / total_records * 100\n\nprint(&quot;Problem rate:&quot;, problem_rate)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Problem rate: 10.0\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Lines 1 to 3 store the counts.</li>\n<li>In line 5, parentheses make Python add the two problem counts first: <code>18 + 6 = 24</code>.</li>\n<li>Python then divides <code>24</code> by <code>240</code>, producing <code>0.1</code>.</li>\n<li>Multiplication by <code>100</code> expresses the result as <code>10.0</code> per cent.</li>\n<li>The parentheses also help a human reader see the intended numerator.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Writing <code>missing_records + duplicate_records / total_records * 100</code>, which applies the percentage calculation only to duplicates before adding missing records.</li>\n<li>Omitting <code>* 100</code> while retaining a percentage label.</li>\n<li>Including overlapping problem categories in the numerator.</li>\n<li>Thinking parentheses make an unjustified metric valid.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add <code>invalid_date_records = 12</code> to the numerator. Predict the new rate and state whether the categories are assumed to be mutually exclusive.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Remove the parentheses around the addition, but leave the rest unchanged.</li>\n<li>Run the cell. It will produce <code>20.5</code>, not an error.</li>\n<li>Repair the grouping and rerun.</li>\n<li>Explain why this is a logical error rather than a syntax error.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What value is the complete numerator in the repaired expression?</p>\n<h3>Example 2.3.10 - Use nested parentheses for a weighted scenario</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">missing_records = 18\nduplicate_records = 6\ntotal_records = 240\n\nweighted_problem_rate = ((missing_records * 2) + duplicate_records) / total_records * 100\n\nprint(&quot;Weighted problem rate:&quot;, weighted_problem_rate)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Weighted problem rate: 17.5\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Missing records receive a weight of <code>2</code> in this illustrative formula.</li>\n<li>The inner parentheses calculate <code>missing_records * 2</code>, producing <code>36</code>.</li>\n<li>The outer parentheses add the duplicate count, producing a weighted numerator of <code>42</code>.</li>\n<li>Division by <code>240</code> and multiplication by <code>100</code> produce <code>17.5</code>.</li>\n<li>The weight is an analytical decision. It does not come from Python and must be justified.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Describing the weighted result as the percentage of records with problems. It is a score, not a direct count percentage.</li>\n<li>Hiding the weight inside a long expression without naming or documenting it.</li>\n<li>Misplacing one parenthesis and changing the numerator.</li>\n<li>Comparing a weighted score directly with an unweighted rate as if they were the same metric.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Create <code>MISSING_WEIGHT = 2</code> and replace the literal weight in the formula with that constant-by-convention. Then test a weight of <code>1.5</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Move the closing parenthesis so the formula becomes <code>(missing_records * (2 + duplicate_records)) / total_records * 100</code>.</li>\n<li>Run it and observe the very different value, <code>60.0</code>.</li>\n<li>Repair the grouping to match the stated metric.</li>\n<li>Explain the difference between weighting the missing count and adding the duplicate count to the weight.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Who is responsible for justifying the value of a weight used in a metric?</p>\n<h3>Example 2.3.11 - Understand a subtle negative-number case</h3>\n<p><strong>Level:</strong> Optional advanced example</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">result_without_grouping = -3 ** 2\nresult_with_grouping = (-3) ** 2\n\nprint(&quot;Without grouping:&quot;, result_without_grouping)\nprint(&quot;With grouping:&quot;, result_with_grouping)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Without grouping: -9\nWith grouping: 9\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>In the first expression, exponentiation happens before the leading negative sign is applied. Python reads it like <code>-(3 ** 2)</code>.</li>\n<li><code>3 ** 2</code> is <code>9</code>, and the leading negative sign produces <code>-9</code>.</li>\n<li>In the second expression, parentheses make <code>-3</code> the complete base.</li>\n<li>Squaring <code>-3</code> produces <code>9</code>.</li>\n<li>Explicit parentheses are especially helpful when negative values and powers appear together.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Assuming the two expressions mean the same thing.</li>\n<li>Memorising the result without understanding the grouping.</li>\n<li>Using negative values in a case where negative counts are impossible.</li>\n<li>Adding parentheses mechanically instead of checking the intended formula.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the exponent from <code>2</code> to <code>3</code>. Predict both results before running.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Remove only the closing parenthesis from <code>(-3) ** 2</code>.</li>\n<li>Observe the <code>SyntaxError</code>.</li>\n<li>Repair the pair of parentheses.</li>\n<li>Add an output label that describes the mathematical grouping rather than only saying &quot;with&quot; or &quot;without&quot;.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What is the base of the exponent in <code>(-3) ** 2</code>?</p>\n<h3>Exercise 2.3.3</h3>\n<p>Predict the values without running Python first:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">10 + 5 * 2\n(10 + 5) * 2\n100 / 5 + 5\n100 / (5 + 5)\n2 ** 3 * 4\n(-2) ** 4\n-2 ** 4\n</code></pre></div>\n<p>Then run each as part of a labelled <code>print()</code> statement. For one expression, explain a realistic case meaning and identify any assumption.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_C3_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_C3_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n"
    }
  ],
  "1.15": [
    {
      "title": "Strings, numbers, Booleans and type()",
      "html": "<h3>Suggested book chapters for Part D</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Priority</th>\n<th>Book and chapter</th>\n<th>Suggested focus</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Core companion</td>\n<td><a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 2: Variables, expressions, and statements</a></td>\n<td>Read <strong>Values and types</strong> and <strong>String operations</strong>. Pay particular attention to the difference between numerical values and digits inside quotation marks.</td>\n</tr>\n<tr>\n<td>Recommended review</td>\n<td><a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 2</a></td>\n<td>Revisit ordinary division and use <code>type()</code> to check the values produced by expressions. The tutorial adds Booleans, <code>None</code>, floating-point representation and formatting in more detail.</td>\n</tr>\n<tr>\n<td>Optional text-focused companion</td>\n<td><a href=\"https://www.nltk.org/book/ch01.html\" target=\"_blank\" rel=\"noreferrer\">Bird, Klein and Loper, <em>Natural Language Processing with Python</em>, Chapter 1</a></td>\n<td>Read <strong>Section 2.4, Strings</strong>, for string assignment, joining and repetition. List indexing and corpus exploration are not required for Python Foundations I.</td>\n</tr>\n</tbody></table></div>\n<h2>Strings: working with text</h2>\n<p>A <strong>string</strong> is a sequence of characters used to represent text. A string literal is normally enclosed in single or double quotation marks.</p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">&quot;Copenhagen&quot;\n&#39;Public consultation&#39;\n</code></pre></div>\n<p>The quotation marks define the value but are not part of the displayed output. Digits inside quotation marks are also text: <code>&quot;250&quot;</code> is a string, not an integer.</p>\n<h3>Example 2.4.1 - Choose quotation marks that fit the text</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">case_title = &quot;Residents&#39; transport survey&quot;\ndepartment = &#39;Urban Mobility Office&#39;\n\nprint(&quot;Case:&quot;, case_title)\nprint(&quot;Department:&quot;, department)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Case: Residents&#39; transport survey\nDepartment: Urban Mobility Office\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 uses double quotation marks around a string that contains an apostrophe.</li>\n<li>The apostrophe in <code>Residents&#39;</code> does not end a double-quoted string.</li>\n<li>Line 2 demonstrates that single quotation marks can also define a string.</li>\n<li>Lines 4 and 5 display labels followed by the stored strings.</li>\n<li>This course usually uses double quotation marks for consistency, but either style is valid when used correctly.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Opening with one quotation style and closing with another.</li>\n<li>Using single quotation marks around text that contains an unescaped apostrophe.</li>\n<li>Typing typographic quotation marks copied from a word processor instead of straight code quotation marks.</li>\n<li>Believing quotation marks will appear in ordinary printed output.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the case title to a phrase containing an apostrophe. Try both quotation styles and keep the clearer working version.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change line 1 to <code>case_title = &#39;Residents&#39; transport survey&#39;</code>.</li>\n<li>Run the cell and observe the <code>SyntaxError</code>.</li>\n<li>Repair it with double quotation marks around the complete string.</li>\n<li>Explain which apostrophe Python incorrectly interpreted as the end of the broken string.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why are double quotation marks convenient for the first string?</p>\n<h3>Example 2.4.2 - Join and repeat strings</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">source = &quot;Consultation portal&quot;\nperiod = &quot;October 2026&quot;\nreport_label = source + &quot; | &quot; + period\nseparator = &quot;-&quot; * 25\n\nprint(report_label)\nprint(separator)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Consultation portal | October 2026\n-------------------------\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Lines 1 and 2 store two strings.</li>\n<li>Line 3 uses <code>+</code> to concatenate, or join, strings.</li>\n<li>The literal <code>&quot; | &quot;</code> includes a vertical bar and spaces for readability.</li>\n<li>Line 4 uses <code>*</code> to repeat the one-character string <code>&quot;-&quot;</code> twenty-five times.</li>\n<li>The output statements display the composed label and separator.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Omitting spaces inside separator strings and producing cramped output.</li>\n<li>Trying to multiply two strings.</li>\n<li>Trying to join a string and integer directly with <code>+</code>.</li>\n<li>Spending more effort decorating console output than checking the underlying result.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a third string for the department and join all three with <code>|</code>. Change the separator to <code>=</code> repeated 35 times.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>separator = &quot;-&quot; * 25</code> to <code>separator = &quot;-&quot; * &quot;25&quot;</code>.</li>\n<li>Run the cell and observe the <code>TypeError</code>.</li>\n<li>Repair the repetition count by storing it as the integer <code>25</code>.</li>\n<li>Use <code>type(25)</code> and <code>type(&quot;25&quot;)</code> to compare the two values.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What does <code>+</code> do when both operands are strings?</p>\n<h3>Example 2.4.3 - Build clear messages with f-strings</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p>An <strong>f-string</strong> lets you place expressions inside a string by writing <code>f</code> before the opening quotation mark and placing expressions inside braces <code>{}</code>.</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">organisation = &quot;North Harbour Council&quot;\nrecords_checked = 640\ncompletion_rate = 93.75\n\nsummary = f&quot;{organisation} checked {records_checked} records. Completion: {completion_rate:.1f}%&quot;\nprint(summary)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">North Harbour Council checked 640 records. Completion: 93.8%\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Lines 1 to 3 store a string, an integer and a float.</li>\n<li>The <code>f</code> before the opening quotation mark activates f-string formatting.</li>\n<li><code>{organisation}</code> inserts the organisation string.</li>\n<li><code>{records_checked}</code> inserts the integer after converting it for display.</li>\n<li><code>{completion_rate:.1f}</code> displays the float with one digit after the decimal point.</li>\n<li>The formatting rounds <code>93.75</code> for display to <code>93.8</code>. It does not change the stored value of <code>completion_rate</code>.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Forgetting the <code>f</code> and seeing braces printed literally.</li>\n<li>Using parentheses instead of braces around inserted expressions.</li>\n<li>Assuming display rounding changes the underlying value.</li>\n<li>Formatting an uncertain metric to look more precise or authoritative than the data allows.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Display the completion rate with two decimal places. Add the reporting month as another variable and insert it into the message.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Remove the <code>f</code> before the string.</li>\n<li>Run the cell. It will not raise an error, but the output will contain the literal braces and variable names.</li>\n<li>Repair the logical formatting error by restoring <code>f</code>.</li>\n<li>Confirm that the output uses values rather than placeholder text.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Does <code>:.1f</code> change the stored value or only its displayed format?</p>\n<h3>Exercise 2.4.1</h3>\n<p>Create a report heading from separate variables for:</p>\n<ul>\n<li>organisation;</li>\n<li>dataset name;</li>\n<li>reporting month;</li>\n<li>number of records.</li>\n</ul>\n<p>Produce one version with concatenation and one with an f-string. Add an apostrophe to one piece of text and choose quotation marks that keep the code readable.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_D1_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_D1_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Integers and floats: working with numbers</h2>\n<p>An <strong>integer</strong>, written <code>int</code> in Python, is a whole number such as <code>0</code>, <code>42</code> or <code>-7</code>.</p>\n<p>A <strong>floating-point number</strong>, written <code>float</code>, contains a decimal point or is produced by an operation such as ordinary division. Examples include <code>3.5</code>, <code>65.0</code> and <code>-0.25</code>.</p>\n<p>Use integers for counts when partial units are impossible. Use floats for measurements, averages, rates and other quantities that can include fractional values. Context matters. A year such as <code>2026</code> may be stored as an integer, but it is an identifier rather than an amount to average.</p>\n<h3>Example 2.4.4 - Use counts and measurements appropriately</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">response_count = 275\naverage_waiting_minutes = 6.5\n\nprint(&quot;Response count:&quot;, response_count)\nprint(&quot;Average waiting time:&quot;, average_waiting_minutes)\nprint(&quot;Count type:&quot;, type(response_count))\nprint(&quot;Average type:&quot;, type(average_waiting_minutes))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Response count: 275\nAverage waiting time: 6.5\nCount type: &lt;class &#39;int&#39;&gt;\nAverage type: &lt;class &#39;float&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>275</code> has no decimal point, so Python stores it as an integer.</li>\n<li><code>6.5</code> has a decimal point, so Python stores it as a float.</li>\n<li>The first two output statements display the values.</li>\n<li><code>type(response_count)</code> returns <code>&lt;class &#39;int&#39;&gt;</code>.</li>\n<li><code>type(average_waiting_minutes)</code> returns <code>&lt;class &#39;float&#39;&gt;</code>.</li>\n<li>The types fit the case meanings: a count of responses and a measured average.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Adding <code>.0</code> to every count without a reason.</li>\n<li>Assuming any whole-looking float is automatically an integer.</li>\n<li>Averaging identifiers such as record IDs or years because they are numerical.</li>\n<li>Confusing measurement precision with data accuracy.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add an integer for the number of service locations and a float for average satisfaction. Inspect both types.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Put quotation marks around <code>6.5</code>.</li>\n<li>Run the cell. Notice that printing still looks similar, but <code>type()</code> now reports <code>&lt;class &#39;str&#39;&gt;</code>.</li>\n<li>Repair it by removing the quotation marks or applying <code>float()</code>.</li>\n<li>Explain why visible output alone can hide a type problem.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why should a count normally be stored as an integer?</p>\n<h3>Example 2.4.5 - Notice that division produces a float</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">total_waiting_minutes = 45\npeople_observed = 6\naverage_waiting_minutes = total_waiting_minutes / people_observed\n\nprint(&quot;Average waiting time:&quot;, average_waiting_minutes)\nprint(&quot;Result type:&quot;, type(average_waiting_minutes))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Average waiting time: 7.5\nResult type: &lt;class &#39;float&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Both source values are integers.</li>\n<li><code>/</code> performs ordinary division.</li>\n<li>The result is <code>7.5</code>, which requires a fractional representation.</li>\n<li>Python 3 ordinary division produces a float.</li>\n<li>The type check confirms the result type.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Using <code>//</code> and silently discarding the fractional part.</li>\n<li>Dividing people by minutes instead of minutes by people.</li>\n<li>Reporting an average without the number of observations.</li>\n<li>Treating an average as if every observed person waited exactly that long.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the total to <code>48</code>. Predict the displayed value and result type.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Replace <code>/</code> with <code>//</code> while keeping the original values.</li>\n<li>Observe that the result becomes <code>7</code>, losing <code>0.5</code> minute.</li>\n<li>Repair the operator to <code>/</code>.</li>\n<li>Explain why this is a silent analytical error rather than a Python error.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Can dividing two integers produce a float?</p>\n<h3>Example 2.4.6 - Understand floating-point representation</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p>Computers store many decimal fractions as binary approximations. This can produce a small representation surprise.</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">first_share = 0.1\nsecond_share = 0.2\ncombined_share = first_share + second_share\n\nprint(&quot;Raw result:&quot;, combined_share)\nprint(f&quot;Formatted result: {combined_share:.2f}&quot;)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Raw result: 0.30000000000000004\nFormatted result: 0.30\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Lines 1 and 2 store two floats.</li>\n<li>Their binary representations are close approximations to the decimal values.</li>\n<li>Adding them exposes a tiny representation difference in the raw output.</li>\n<li>The f-string format <code>.2f</code> displays two digits after the decimal point.</li>\n<li>Formatting is appropriate for presentation here, but it does not remove the underlying representation issue or validate the input data.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Assuming Python has made a large calculation error.</li>\n<li>Comparing floats for exact equality in situations where tiny representation differences matter.</li>\n<li>Using display formatting to hide a genuinely important discrepancy.</li>\n<li>Reporting more decimal places than the measurements support.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Try <code>0.1 + 0.1 + 0.1</code>. Display the raw result and a version formatted to one decimal place.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the format from <code>.2f</code> to <code>.2</code> inside the f-string.</li>\n<li>Run it and observe that the result may use general significant-digit formatting rather than the intended fixed two decimal places.</li>\n<li>Repair the format to <code>.2f</code>.</li>\n<li>Explain the difference between controlling decimal places for display and changing the stored number.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why might a simple decimal calculation display more digits than expected?</p>\n<h3>Example 2.4.7 - Recognise implicit conversion from integer to float</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p>Python can combine an integer and a float in one arithmetic expression. It automatically represents the numerical result as a float so that a fractional part is not lost. This is called <strong>implicit conversion</strong> or <strong>type promotion</strong>.</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">whole_hours = 12\nadditional_hours = 0.5\ntotal_hours = whole_hours + additional_hours\n\nprint(&quot;Total observed hours:&quot;, total_hours)\nprint(&quot;Whole-hours type:&quot;, type(whole_hours))\nprint(&quot;Total-hours type:&quot;, type(total_hours))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Total observed hours: 12.5\nWhole-hours type: &lt;class &#39;int&#39;&gt;\nTotal-hours type: &lt;class &#39;float&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>whole_hours</code> stores the whole-number value <code>12</code> as an integer.</li>\n<li><code>additional_hours</code> stores <code>0.5</code> as a float.</li>\n<li>The addition combines the two numeric types.</li>\n<li>Python produces the float <code>12.5</code> automatically because that type can represent the fractional result.</li>\n<li>The original <code>whole_hours</code> value remains an integer. Python converts for the expression; it does not rewrite the source variable.</li>\n<li>The two <code>type()</code> calls make the before-and-after types visible.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Assuming the integer variable itself has permanently changed type.</li>\n<li>Calling this automatic behaviour the same as explicit conversion with <code>float()</code>.</li>\n<li>Mixing numerical values with numerical-looking strings, which Python does not convert automatically for arithmetic.</li>\n<li>Ignoring whether two values use the same unit before adding them.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change <code>additional_hours</code> to <code>2.0</code>. Predict both the displayed value and its type even though the result looks whole.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>additional_hours = 0.5</code> to <code>additional_hours = &quot;0.5&quot;</code>.</li>\n<li>Run the code and observe <code>TypeError</code> on the addition line.</li>\n<li>Repair it by storing <code>0.5</code> as a float or by explicitly converting suitable text with <code>float()</code>.</li>\n<li>Explain why Python promotes compatible numeric types but does not guess the intended meaning of text.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>After the calculation, what is the type of <code>whole_hours</code>, and what is the type of <code>total_hours</code>?</p>\n<h3>Example 2.4.8 - Separate rounding a value from formatting its display</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p>Rounding and display formatting answer different questions. <code>round()</code> creates a rounded numerical value. An f-string can control how a number looks without changing the stored source value.</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">raw_completion_rate = 93.756\nrounded_rate = round(raw_completion_rate, 1)\ndisplay_line = f&quot;Completion rate: {raw_completion_rate:.1f}%&quot;\n\nprint(&quot;Raw value:&quot;, raw_completion_rate)\nprint(&quot;Rounded numerical value:&quot;, rounded_rate)\nprint(display_line)\nprint(&quot;Raw value is still:&quot;, raw_completion_rate)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Raw value: 93.756\nRounded numerical value: 93.8\nCompletion rate: 93.8%\nRaw value is still: 93.756\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 stores the source float with three decimal places.</li>\n<li><code>round(raw_completion_rate, 1)</code> produces the numerical value <code>93.8</code> and assigns it to a new variable.</li>\n<li>The f-string formats the original value to one decimal place inside a display string.</li>\n<li>The first output preserves the unrounded evidence.</li>\n<li>The second output shows a rounded number that could be used in later numerical work if that rule is justified.</li>\n<li>The third output is presentation text.</li>\n<li>The final output proves that f-string formatting did not change <code>raw_completion_rate</code>.</li>\n<li>Neither approach creates more accurate evidence. The chosen precision should match the data and the reporting purpose.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Believing <code>:.1f</code> permanently changes the variable.</li>\n<li>Overwriting the raw measurement before checking whether the rounding rule is appropriate.</li>\n<li>Reporting many decimal places merely because Python can display them.</li>\n<li>Assuming <code>round()</code> is a data-quality correction rather than a representation decision.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Create a second display line with two decimal places. Then round the numerical value to two decimal places and inspect both results with <code>type()</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the f-string format from <code>.1f</code> to <code>.1</code>.</li>\n<li>Run the code and observe that general significant-digit formatting may produce a different representation from one fixed decimal place.</li>\n<li>Repair the format to <code>.1f</code>.</li>\n<li>State whether your goal is a rounded number for later calculation or a readable string for communication.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which line creates a new rounded number, and which line only controls presentation?</p>\n<h3>Exercise 2.4.2</h3>\n<p>For each value, decide whether <code>int</code>, <code>float</code> or <code>str</code> best represents its intended role, and explain why:</p>\n<ul>\n<li>number of interviews;</li>\n<li>interview duration in minutes;</li>\n<li>postcode;</li>\n<li>satisfaction score with half-points;</li>\n<li>reporting year;</li>\n<li>participant identifier with leading zeros;</li>\n<li>completion percentage.</li>\n</ul>\n<p>Then create and inspect at least one example of each of the three types.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_D2_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_D2_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Boolean values and comparisons</h2>\n<p>A <strong>Boolean</strong> value is either <code>True</code> or <code>False</code>. The capital letters are required. Boolean values can store flags directly or result from comparisons.</p>\n<p>Common comparison operators are:</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Operator</th>\n<th>Meaning</th>\n<th>Example</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>==</code></td>\n<td>Equal to</td>\n<td><code>status == &quot;complete&quot;</code></td>\n</tr>\n<tr>\n<td><code>!=</code></td>\n<td>Not equal to</td>\n<td><code>missing != 0</code></td>\n</tr>\n<tr>\n<td><code>&gt;</code></td>\n<td>Greater than</td>\n<td><code>responses &gt; 100</code></td>\n</tr>\n<tr>\n<td><code>&lt;</code></td>\n<td>Less than</td>\n<td><code>error_rate &lt; 5</code></td>\n</tr>\n<tr>\n<td><code>&gt;=</code></td>\n<td>Greater than or equal to</td>\n<td><code>score &gt;= 80</code></td>\n</tr>\n<tr>\n<td><code>&lt;=</code></td>\n<td>Less than or equal to</td>\n<td><code>file_size &lt;= 10</code></td>\n</tr>\n</tbody></table></div>\n<p>Do not confuse assignment <code>=</code> with equality comparison <code>==</code>.</p>\n<h3>Example 2.4.9 - Store Boolean flags directly</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">consent_recorded = True\ncontains_personal_data = False\n\nprint(&quot;Consent recorded:&quot;, consent_recorded)\nprint(&quot;Contains personal data:&quot;, contains_personal_data)\nprint(&quot;Consent flag type:&quot;, type(consent_recorded))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Consent recorded: True\nContains personal data: False\nConsent flag type: &lt;class &#39;bool&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 stores the Boolean literal <code>True</code>.</li>\n<li>Line 2 stores the Boolean literal <code>False</code>.</li>\n<li>The output labels describe what each flag means.</li>\n<li><code>type(consent_recorded)</code> confirms the Boolean type.</li>\n<li>A Boolean flag simplifies a complex situation, so its definition still needs documentation.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Writing <code>true</code> or <code>false</code> in lowercase.</li>\n<li>Writing <code>&quot;True&quot;</code>, which creates a string rather than a Boolean.</li>\n<li>Assuming a Boolean field captures uncertainty or partial consent.</li>\n<li>Using a negatively worded flag that is difficult to interpret.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a Boolean variable called <code>requires_manual_review</code> and display it. Write one sentence defining when it should be <code>True</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>True</code> to <code>&quot;True&quot;</code>.</li>\n<li>Run the cell. No error occurs, but <code>type()</code> reports <code>&lt;class &#39;str&#39;&gt;</code>.</li>\n<li>Repair it by removing the quotation marks.</li>\n<li>Explain why a silent type change can be dangerous in later decision logic.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What are the only two Boolean literal values in Python?</p>\n<h3>Example 2.4.10 - Produce Booleans with comparisons</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">total_records = 300\nmissing_records = 0\nminimum_required = 250\n\nhas_no_missing_records = missing_records == 0\nmeets_minimum_size = total_records &gt;= minimum_required\nis_exactly_minimum = total_records == minimum_required\n\nprint(&quot;No missing records:&quot;, has_no_missing_records)\nprint(&quot;Meets minimum size:&quot;, meets_minimum_size)\nprint(&quot;Exactly minimum size:&quot;, is_exactly_minimum)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">No missing records: True\nMeets minimum size: True\nExactly minimum size: False\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The first three lines store numerical values.</li>\n<li><code>missing_records == 0</code> asks a question and produces <code>True</code>.</li>\n<li><code>total_records &gt;= minimum_required</code> is <code>True</code> because <code>300</code> is at least <code>250</code>.</li>\n<li><code>total_records == minimum_required</code> is <code>False</code> because <code>300</code> is not exactly <code>250</code>.</li>\n<li>The three Boolean results are stored under question-like, readable names.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Using <code>=</code> instead of <code>==</code> for comparison.</li>\n<li>Confusing <code>&gt;</code> with <code>&gt;=</code> at the boundary value.</li>\n<li>Treating a minimum sample size as proof of representative data.</li>\n<li>Choosing a threshold without documenting who decided it and why.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Set <code>total_records</code> to exactly <code>250</code>. Predict which Boolean results change.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>has_no_missing_records = missing_records == 0</code> to <code>has_no_missing_records = missing_records = 0</code>.</li>\n<li>Run the cell and observe the <code>SyntaxError</code>.</li>\n<li>Repair the comparison operator to <code>==</code>.</li>\n<li>Point to the separate assignment operator earlier in the script and explain the difference.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why are <code>&gt;= 250</code> and <code>== 250</code> different questions?</p>\n<h3>Example 2.4.11 - Combine Boolean conditions</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">has_required_columns = True\nmissing_record_count = 0\nduplicate_record_count = 0\n\nready_for_initial_analysis = (\n    has_required_columns\n    and missing_record_count == 0\n    and duplicate_record_count == 0\n)\n\nprint(&quot;Ready for initial analysis:&quot;, ready_for_initial_analysis)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Ready for initial analysis: True\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The first three lines store two counts and one Boolean flag.</li>\n<li>Parentheses allow the longer expression to span several readable lines.</li>\n<li><code>and</code> requires every connected Boolean expression to be <code>True</code>.</li>\n<li><code>has_required_columns</code> is already Boolean, so it does not need comparison with <code>True</code>.</li>\n<li>Both count comparisons are <code>True</code> because the counts equal zero.</li>\n<li>The final variable stores the combined Boolean result.</li>\n<li>&quot;Ready&quot; here means only that these three checks passed. Other quality and ethical checks may still be needed.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Believing <code>and</code> checks concepts that were not coded.</li>\n<li>Using vague readiness labels without defining their scope.</li>\n<li>Comparing a Boolean as <code>has_required_columns == &quot;True&quot;</code>, which compares it with a string.</li>\n<li>Omitting parentheses and indenting a continued expression incorrectly.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change <code>duplicate_record_count</code> to <code>2</code>. Predict the combined result. Then rename the variable to clarify that it represents only initial technical readiness.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Delete the final closing parenthesis after the Boolean expression.</li>\n<li>Run the cell and observe the <code>SyntaxError</code>.</li>\n<li>Repair the parenthesis.</li>\n<li>Count and align the opening and closing parentheses visually.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Does a <code>True</code> result prove that the dataset is reliable, representative or ethically appropriate?</p>\n<h3>Exercise 2.4.3</h3>\n<p>Create Boolean variables that answer these questions for a fictional dataset:</p>\n<ul>\n<li>Are there more than 500 records?</li>\n<li>Are there exactly zero duplicate records?</li>\n<li>Is the completion rate at least 80 per cent?</li>\n<li>Is consent recorded?</li>\n<li>Do all four conditions hold together?</li>\n</ul>\n<p>Display every Boolean with a clear label. Test at least one boundary value, such as exactly <code>500</code> or exactly <code>80.0</code>.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_D3_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_D3_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Inspect values with <code>type()</code></h2>\n<p>The built-in <code>type()</code> function tells you the type of a value. It is especially useful when code looks correct but Python reports that two values cannot be combined.</p>\n<p>Common results in this tutorial are:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">&lt;class &#39;str&#39;&gt;\n&lt;class &#39;int&#39;&gt;\n&lt;class &#39;float&#39;&gt;\n&lt;class &#39;bool&#39;&gt;\n</code></pre></div>\n<h3>Example 2.4.12 - Inspect the four foundation types</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">case_name = &quot;Green Mobility Debate&quot;\nrecord_count = 420\naverage_engagement = 37.5\nreview_complete = False\n\nprint(type(case_name))\nprint(type(record_count))\nprint(type(average_engagement))\nprint(type(review_complete))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">&lt;class &#39;str&#39;&gt;\n&lt;class &#39;int&#39;&gt;\n&lt;class &#39;float&#39;&gt;\n&lt;class &#39;bool&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The first four lines assign one value of each foundational type.</li>\n<li>Each <code>type(...)</code> call receives a variable&#39;s value.</li>\n<li>The four print statements display the returned type objects.</li>\n<li><code>str</code> means string, <code>int</code> means integer, <code>float</code> means floating-point number and <code>bool</code> means Boolean.</li>\n<li>Type describes how Python treats a value, not whether the underlying data is valid or truthful.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Writing <code>print(&quot;type(record_count)&quot;)</code>, which displays text.</li>\n<li>Writing <code>Type()</code> with a capital letter.</li>\n<li>Expecting <code>type()</code> to convert a value.</li>\n<li>Treating a correct Python type as proof of good measurement.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change one value so its type changes without changing its visible characters when printed. Example: compare <code>420</code> with <code>&quot;420&quot;</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the final call to <code>print(typo(review_complete))</code>.</li>\n<li>Observe the <code>NameError</code> for <code>typo</code>.</li>\n<li>Repair the function name to <code>type</code>.</li>\n<li>Confirm that the result is <code>&lt;class &#39;bool&#39;&gt;</code>.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Does <code>type()</code> report the role of a value in your research design or only its Python type?</p>\n<h3>Example 2.4.13 - Use <code>type()</code> to investigate a failed calculation</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">records_received = &quot;250&quot;\nrecords_missing = 18\n\nprint(&quot;records_received type:&quot;, type(records_received))\nprint(&quot;records_missing type:&quot;, type(records_missing))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">records_received type: &lt;class &#39;str&#39;&gt;\nrecords_missing type: &lt;class &#39;int&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>records_received</code> looks numerical when printed, but quotation marks make it a string.</li>\n<li><code>records_missing</code> is an integer.</li>\n<li>The labelled <code>type()</code> calls expose the mismatch before subtraction is attempted.</li>\n<li>This is a useful debugging pattern when values may come from keyboard input or a data file.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Checking only the type of the final result rather than the input values.</li>\n<li>Assuming a column or input is numerical because its characters are digits.</li>\n<li>Converting values automatically without checking whether conversion is appropriate.</li>\n<li>Forgetting that empty text and labels such as <code>&quot;Unknown&quot;</code> cannot be converted directly to numbers.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add <code>records_received_number = int(records_received)</code> and inspect its type. Then calculate the retained count.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Add <code>print(records_received - records_missing)</code> before conversion.</li>\n<li>Observe the <code>TypeError</code> explaining that subtraction is not supported between <code>str</code> and <code>int</code>.</li>\n<li>Repair the calculation by using the converted integer.</li>\n<li>Keep the original string variable so you can explain the transformation.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What evidence in the code tells you that <code>&quot;250&quot;</code> is not an integer?</p>\n<h3>Example 2.4.14 - Inspect expressions, not only variables</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">print(type(10 + 5))\nprint(type(10 / 5))\nprint(type(&quot;10&quot; + &quot;5&quot;))\nprint(type(10 &gt; 5))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">&lt;class &#39;int&#39;&gt;\n&lt;class &#39;float&#39;&gt;\n&lt;class &#39;str&#39;&gt;\n&lt;class &#39;bool&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>10 + 5</code> produces the integer <code>15</code>.</li>\n<li><code>10 / 5</code> produces the float <code>2.0</code>, even though the mathematical result is whole.</li>\n<li><code>&quot;10&quot; + &quot;5&quot;</code> concatenates two strings and produces <code>&quot;105&quot;</code>.</li>\n<li><code>10 &gt; 5</code> is a comparison and produces the Boolean <code>True</code>.</li>\n<li><code>type()</code> can inspect the value produced by a complete expression.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Predicting that <code>&quot;10&quot; + &quot;5&quot;</code> produces <code>15</code>.</li>\n<li>Predicting that ordinary division produces an integer when the result is whole.</li>\n<li>Confusing a comparison result with the text <code>&quot;True&quot;</code>.</li>\n<li>Checking type after converting or overwriting the value and losing evidence of the original problem.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Predict and inspect the types of <code>10 // 5</code>, <code>10 == 5</code>, <code>&quot;10&quot; * 2</code> and <code>10 * 2.0</code>.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the first line to <code>print(type(10 + &quot;5&quot;))</code>.</li>\n<li>Observe the <code>TypeError</code> caused by adding an integer and string.</li>\n<li>Repair it according to intended meaning: use <code>10 + int(&quot;5&quot;)</code> for arithmetic or <code>str(10) + &quot;5&quot;</code> for text joining.</li>\n<li>State which meaning you selected and why.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is intended meaning necessary when repairing a type mismatch?</p>\n<h3><code>None</code>, zero and empty text mean different things</h3>\n<p>Python uses the special value <code>None</code> to represent the absence of a value. It is not the integer <code>0</code>, the empty string <code>&quot;&quot;</code>, the Boolean <code>False</code>, or the text <code>&quot;None&quot;</code>. This distinction is important in later data work because &quot;not recorded&quot; is different from a recorded value of zero.</p>\n<h3>Example 2.4.15 - Preserve three different states</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">waiting_time_minutes = None\ncomplaint_count = 0\noptional_comment = &quot;&quot;\n\nprint(&quot;Waiting time:&quot;, waiting_time_minutes)\nprint(&quot;Complaint count:&quot;, complaint_count)\nprint(&quot;Optional comment:&quot;, optional_comment)\nprint(&quot;Waiting-time type:&quot;, type(waiting_time_minutes))\nprint(&quot;Complaint-count type:&quot;, type(complaint_count))\nprint(&quot;Comment type:&quot;, type(optional_comment))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Waiting time: None\nComplaint count: 0\nOptional comment: \nWaiting-time type: &lt;class &#39;NoneType&#39;&gt;\nComplaint-count type: &lt;class &#39;int&#39;&gt;\nComment type: &lt;class &#39;str&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>waiting_time_minutes = None</code> records that no waiting-time value is available in this teaching case.</li>\n<li><code>complaint_count = 0</code> records an observed or reported count of zero complaints.</li>\n<li><code>optional_comment = &quot;&quot;</code> stores a string containing no characters. It may mean that the optional field was left blank, but the code alone cannot prove why.</li>\n<li>The first three output statements look different because the stored values differ.</li>\n<li><code>type(None)</code> is <code>NoneType</code>, the zero is an integer, and the empty value remains a string.</li>\n<li>Keeping these states distinct prevents a missing measurement from being silently interpreted as a genuine zero.</li>\n<li>A real dataset may use other missing-value markers. Those conventions must be documented and handled consistently later.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Writing <code>&quot;None&quot;</code>, which stores four characters as text rather than the special value.</li>\n<li>Replacing all missing measurements with zero and changing the meaning of the data.</li>\n<li>Assuming an empty comment means the participant had no opinion.</li>\n<li>Writing <code>none</code> in lowercase, which Python treats as an ordinary undefined name.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add <code>review_note = &quot;Not yet checked&quot;</code>. Compare its meaning and type with <code>None</code> and <code>&quot;&quot;</code>. Explain which state best represents a value that has not yet been entered.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>waiting_time_minutes = None</code> to <code>waiting_time_minutes = none</code>.</li>\n<li>Run the code and observe <code>NameError</code> because <code>none</code> is not the special Python literal.</li>\n<li>Repair the capitalisation to <code>None</code>.</li>\n<li>Confirm the repaired type with <code>type()</code>.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why would replacing a missing waiting time with <code>0</code> change the meaning of the record?</p>\n<h3>Exercise 2.4.4</h3>\n<p>Before running, predict the type produced by each expression:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">25\n25.0\n&quot;25&quot;\nNone\n&quot;&quot;\n25 / 5\n25 // 5\n10 + 0.5\nround(93.756, 1)\nf&quot;{93.756:.1f}&quot;\n&quot;A&quot; + &quot;B&quot;\n25 &gt;= 20\n</code></pre></div>\n<p>Then verify with <code>type()</code>. For every incorrect prediction, write one sentence explaining the rule you had misunderstood.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_D4_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_D4_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n"
    }
  ],
  "1.16": [
    {
      "title": "Type conversion and user input",
      "html": "<h3>Suggested book chapters for Part E</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Priority</th>\n<th>Book and chapter</th>\n<th>Suggested focus</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Core companion</td>\n<td><a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 2: Variables, expressions, and statements</a></td>\n<td>Read <strong>Asking the user for input</strong> and the surrounding type-conversion discussion. This directly supports <code>input()</code>, <code>int()</code> and the <code>ValueError</code> produced by unsuitable text.</td>\n</tr>\n<tr>\n<td>Recommended transfer practice</td>\n<td><a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 2 exercises</a></td>\n<td>Complete Exercises 2, 3 and 5 after the tutorial examples. Exercises 3 and 5 are the sources for the explicitly adapted gross-pay and temperature activities in this part.</td>\n</tr>\n<tr>\n<td>NLTK Book</td>\n<td>No Day 1 reading required</td>\n<td>The NLTK Book is not the best companion for keyboard input and type conversion. Return to it during the later text-processing lecture.</td>\n</tr>\n</tbody></table></div>\n<h2>Convert values with <code>int()</code>, <code>float()</code> and <code>str()</code></h2>\n<p>Type conversion creates a value of a different type when the conversion is possible and meaningful.</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Function</th>\n<th>Intended result</th>\n<th>Example</th>\n<th>Result</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>int()</code></td>\n<td>Integer</td>\n<td><code>int(&quot;42&quot;)</code></td>\n<td><code>42</code></td>\n</tr>\n<tr>\n<td><code>float()</code></td>\n<td>Float</td>\n<td><code>float(&quot;42.5&quot;)</code></td>\n<td><code>42.5</code></td>\n</tr>\n<tr>\n<td><code>str()</code></td>\n<td>String</td>\n<td><code>str(42)</code></td>\n<td><code>&quot;42&quot;</code></td>\n</tr>\n</tbody></table></div>\n<p>Conversion is not the same as data validation. Successfully converting <code>&quot;999&quot;</code> to <code>999</code> does not prove that 999 is a plausible age, count or score.</p>\n<h3>Example 2.5.1 - Convert whole-number text to an integer</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">records_text = &quot;250&quot;\nrecords_number = int(records_text)\nremaining_records = records_number - 18\n\nprint(&quot;Original type:&quot;, type(records_text))\nprint(&quot;Converted type:&quot;, type(records_number))\nprint(&quot;Remaining records:&quot;, remaining_records)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Original type: &lt;class &#39;str&#39;&gt;\nConverted type: &lt;class &#39;int&#39;&gt;\nRemaining records: 232\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 stores the characters <code>250</code> as a string because they are inside quotation marks.</li>\n<li><code>int(records_text)</code> reads suitable whole-number text and produces the integer <code>250</code>.</li>\n<li>The converted value is stored under a new name, preserving the original text for comparison.</li>\n<li>The integer can be used in subtraction.</li>\n<li>The two <code>type()</code> calls verify the transformation.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Writing <code>int = records_text</code>, which hides the function instead of calling it.</li>\n<li>Forgetting the parentheses in <code>int(records_text)</code>.</li>\n<li>Converting every identifier made of digits, including values where leading zeros matter.</li>\n<li>Assuming conversion proves that the number is plausible.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the text to <code>&quot;1000&quot;</code> and the subtraction to <code>65</code>. Predict the new result. Then display the original and converted values beside their types.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>&quot;250&quot;</code> to <code>&quot;250 records&quot;</code>.</li>\n<li>Run the cell and observe <code>ValueError</code> because the complete string is not a valid integer representation.</li>\n<li>Repair the source text to <code>&quot;250&quot;</code> for now.</li>\n<li>Explain why silently deleting words from real data would require a documented cleaning rule.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which variable retains the original string after conversion?</p>\n<h3>Example 2.5.2 - Convert decimal text to a float</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">completion_text = &quot;93.5&quot;\ncompletion_number = float(completion_text)\ngap_to_full_completion = 100 - completion_number\n\nprint(&quot;Completion:&quot;, completion_number)\nprint(&quot;Gap to 100:&quot;, gap_to_full_completion)\nprint(&quot;Converted type:&quot;, type(completion_number))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Completion: 93.5\nGap to 100: 6.5\nConverted type: &lt;class &#39;float&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>completion_text</code> contains decimal characters as a string.</li>\n<li><code>float(completion_text)</code> converts suitable decimal text to a floating-point number.</li>\n<li>The converted number can be subtracted from integer <code>100</code>; Python produces a float result.</li>\n<li>The output displays the value, derived gap and type.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Using <code>int(&quot;93.5&quot;)</code>, which raises <code>ValueError</code> because the string is not a whole-number representation.</li>\n<li>Writing a comma decimal such as <code>&quot;93,5&quot;</code> without an explicit cleaning rule.</li>\n<li>Calling <code>float()</code> but continuing to calculate with the original string.</li>\n<li>Interpreting a completion percentage without knowing its denominator.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the text to <code>&quot;87.25&quot;</code>. Display the gap with two decimal places using an f-string.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the conversion to <code>completion_number = int(completion_text)</code>.</li>\n<li>Observe the <code>ValueError</code>.</li>\n<li>Repair the conversion by using <code>float()</code>.</li>\n<li>Explain why converting the text first to float and then to int would discard information.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is <code>float()</code> the appropriate converter for <code>&quot;93.5&quot;</code>?</p>\n<h3>Example 2.5.3 - Convert numbers to text for concatenation</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">report_year = 2026\nrecord_count = 420\n\nfile_label = &quot;report_&quot; + str(report_year) + &quot;_records_&quot; + str(record_count)\nprint(file_label)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">report_2026_records_420\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The year and record count begin as integers.</li>\n<li>String concatenation with <code>+</code> requires string operands.</li>\n<li><code>str(report_year)</code> produces the string <code>&quot;2026&quot;</code>.</li>\n<li><code>str(record_count)</code> produces the string <code>&quot;420&quot;</code>.</li>\n<li>The joined result is stored as a label and displayed.</li>\n<li>The source variables remain integers. <code>str()</code> creates converted values for this expression.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Joining integers directly with strings by <code>+</code>.</li>\n<li>Overwriting a numerical source variable with its string version before calculations are finished.</li>\n<li>Using a human-readable label as a file name without checking allowed characters.</li>\n<li>Treating a year as an amount just because it is stored as an integer.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a string variable for the reporting month and include it in the label. Use lowercase text without spaces for the file-friendly month value.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Remove <code>str()</code> from <code>record_count</code> in the concatenation.</li>\n<li>Run the cell and observe <code>TypeError</code>.</li>\n<li>Repair it by restoring <code>str(record_count)</code> or rewriting the label as an f-string.</li>\n<li>Explain why <code>print(&quot;Records:&quot;, record_count)</code> does not need this explicit conversion.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Does calling <code>str(report_year)</code> change the type stored under <code>report_year</code>?</p>\n<h3>Example 2.5.4 - Understand that <code>int()</code> truncates floats</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">projected_responses = 572.9\nwhole_responses = int(projected_responses)\n\nprint(&quot;Original projection:&quot;, projected_responses)\nprint(&quot;Converted integer:&quot;, whole_responses)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Original projection: 572.9\nConverted integer: 572\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The original projection is a float.</li>\n<li><code>int(projected_responses)</code> removes the fractional part by truncating towards zero.</li>\n<li>It does not perform ordinary rounding to the nearest integer.</li>\n<li>The original and converted values are displayed separately.</li>\n<li>A real project needs an explicit decision about rounding, ceiling, flooring or reporting the decimal scenario.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Assuming <code>int(572.9)</code> produces <code>573</code>.</li>\n<li>Converting a measurement to an integer and losing meaningful precision.</li>\n<li>Applying a rounding rule without documenting it.</li>\n<li>Treating a projected fractional count as an observed count.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Test <code>572.1</code>, <code>572.5</code> and <code>572.9</code>. Record the integer produced in each case. Then describe why truncation may systematically reduce positive projected counts.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the source value to <code>&quot;572.9&quot;</code> and keep <code>int(projected_responses)</code>.</li>\n<li>Observe <code>ValueError</code> because the string contains a decimal point.</li>\n<li>Repair it with <code>int(float(projected_responses))</code> only if truncation is genuinely intended.</li>\n<li>Prefer keeping the float when the fractional value is analytically meaningful.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What important decision is hidden if you convert every projected count with <code>int()</code>?</p>\n<h3>Conversion decision guide</h3>\n<p>Before converting, ask:</p>\n<ol>\n<li>What type is the value now?</li>\n<li>What type does the next operation require?</li>\n<li>Does the text represent a number, an identifier or a category?</li>\n<li>Could leading zeros or decimal places carry meaning?</li>\n<li>What should happen to missing, empty or invalid text?</li>\n<li>Does conversion change the interpretation of the data?</li>\n</ol>\n<h3>Exercise 2.5.1</h3>\n<p>For each source string, predict whether <code>int()</code>, <code>float()</code> or neither converter will work directly:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">&quot;42&quot;\n&quot;42.0&quot;\n&quot;42.5&quot;\n&quot; 42 &quot;\n&quot;forty-two&quot;\n&quot;&quot;\n&quot;0042&quot;\n&quot;93,5&quot;\n</code></pre></div>\n<p>Run each prediction in a separate cell so one error does not prevent later tests. Record the error type for failed conversions. Then explain which values should possibly remain strings even if conversion is technically possible.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_E1_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_E1_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Collect user input with <code>input()</code></h2>\n<p>The built-in <code>input()</code> function pauses the program, displays a prompt and waits for the user to type something. When the user presses Enter, <code>input()</code> returns the typed content as a <strong>string</strong>.</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">variable_name = input(&quot;Prompt shown to the user: &quot;)\n</code></pre></div>\n<p>Even when the user types digits, the returned value is a string. Numerical input must normally be converted before arithmetic.</p>\n<h3>Example 2.5.5 - Collect and display text input</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">organisation = input(&quot;Enter the organisation name: &quot;)\nprint(&quot;Organisation recorded:&quot;, organisation)\nprint(&quot;Input type:&quot;, type(organisation))\n</code></pre></div>\n<p><strong>Expected output for this sample interaction</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Enter the organisation name: North Harbour Council\nOrganisation recorded: North Harbour Council\nInput type: &lt;class &#39;str&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>input(...)</code> displays the prompt inside its parentheses.</li>\n<li>Python waits until the user types a response and presses Enter.</li>\n<li>The response is assigned to <code>organisation</code> as a string.</li>\n<li>The second line displays the stored response.</li>\n<li>The final line confirms that keyboard input is text.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Clicking the next cell while the current cell is still waiting for input.</li>\n<li>Expecting quotation marks to be needed around a typed response.</li>\n<li>Assuming input has been checked merely because it was collected.</li>\n<li>Using a vague prompt such as <code>Enter value:</code> without context or units.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the prompt to request a dataset title. Add a second input for the reporting month and display both in one sentence.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the first line to <code>organisation = Input(&quot;Enter the organisation name: &quot;)</code>.</li>\n<li>Run the cell and observe the <code>NameError</code> because Python is case-sensitive.</li>\n<li>Repair <code>Input</code> to lowercase <code>input</code>.</li>\n<li>Rerun and complete the prompt.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What type does <code>input()</code> return?</p>\n<h3>Example 2.5.6 - Convert numerical input before calculating</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">records_text = input(&quot;Enter the number of records received: &quot;)\nrecords_received = int(records_text)\nrecords_missing = 8\nrecords_complete = records_received - records_missing\n\nprint(&quot;Complete records:&quot;, records_complete)\n</code></pre></div>\n<p><strong>Expected output for this sample interaction</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Enter the number of records received: 120\nComplete records: 112\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 collects the typed characters and stores them as <code>records_text</code>.</li>\n<li>Line 2 converts suitable whole-number text to an integer.</li>\n<li>Line 3 stores another integer.</li>\n<li>Line 4 performs subtraction using two integers.</li>\n<li>The final line displays the derived count.</li>\n<li>Separate names make the conversion visible for beginners.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Subtracting from <code>records_text</code> before conversion.</li>\n<li>Typing decimal text when <code>int()</code> expects a whole-number representation.</li>\n<li>Entering a unit such as <code>120 records</code>.</li>\n<li>Accepting a negative or implausible value without later validation.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Collect the missing-record count from the user as well. Convert both inputs and calculate the complete count.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Comment out the conversion line and change the calculation to use <code>records_text</code>.</li>\n<li>Enter <code>120</code> and observe <code>TypeError</code> when Python tries to subtract an integer from a string.</li>\n<li>Repair the conversion and calculate with <code>records_received</code>.</li>\n<li>Use <code>type()</code> to confirm both numerical inputs before subtraction.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why does typing <code>120</code> not automatically create an integer?</p>\n<h3>Example 2.5.7 - Turn messy text input into a traceable file label</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">organisation_raw = input(&quot;Organisation: &quot;)\ndataset_raw = input(&quot;Dataset title: &quot;)\nperiod_raw = input(&quot;Reporting period: &quot;)\n\norganisation = organisation_raw.strip().title()\norganisation_label = organisation.lower().replace(&quot; &quot;, &quot;_&quot;)\ndataset_label = dataset_raw.strip().lower().replace(&quot; &quot;, &quot;_&quot;)\nperiod_label = period_raw.strip().lower().replace(&quot; &quot;, &quot;_&quot;)\nfile_name = f&quot;{organisation_label}_{dataset_label}_{period_label}.csv&quot;\n\nprint(f&quot;\\nOrganisation: {organisation}&quot;)\nprint(&quot;Dataset label:&quot;, dataset_label)\nprint(&quot;Reporting-period label:&quot;, period_label)\nprint(&quot;Draft file name:&quot;, file_name)\n</code></pre></div>\n<p><strong>Expected output for this sample interaction</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Organisation:   north harbour council  \nDataset title:   Mobility Feedback  \nReporting period: OCTOBER 2026\n\nOrganisation: North Harbour Council\nDataset label: mobility_feedback\nReporting-period label: october_2026\nDraft file name: north_harbour_council_mobility_feedback_october_2026.csv\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The first three lines preserve exactly what the user typed, including extra spaces and inconsistent capitalisation.</li>\n<li><code>strip()</code> removes spaces from the beginning and end of a new string.</li>\n<li><code>title()</code> prepares a human-readable organisation name for this fictional example.</li>\n<li><code>lower()</code> and <code>replace(&quot; &quot;, &quot;_&quot;)</code> prepare simple lowercase labels with underscores instead of spaces.</li>\n<li>The transformations are chained from left to right. Each method returns a new string for the next method.</li>\n<li>The raw variables are not overwritten, so a reviewer can compare input and transformation.</li>\n<li>The f-string combines three prepared components with underscores and adds a <code>.csv</code> suffix.</li>\n<li>The output separates a human-readable label from file-oriented labels.</li>\n<li>This is not complete file-name validation. Punctuation, repeated spaces, non-English characters, duplicated names and the organisation&#39;s own naming policy still require decisions.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Overwriting raw input and losing evidence of the original characters.</li>\n<li>Assuming <code>.title()</code> produces correct capitalisation for every personal, organisational or place name.</li>\n<li>Treating text standardisation as neutral when labels may carry social or organisational meaning.</li>\n<li>Assuming a plausible file name is automatically unique, permitted or safe on every system.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a fourth input for a version label such as <code>v01</code>. Include it before <code>.csv</code>. Then create a separate human-readable report title without underscores.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change <code>.replace(&quot; &quot;, &quot;_&quot;)</code> in <code>dataset_label</code> to <code>.replace(&quot; &quot;, &quot;&quot;)</code>.</li>\n<li>Run the program. Python reports no error, but <code>Mobility Feedback</code> becomes the harder-to-read <code>mobilityfeedback</code>.</li>\n<li>Repair the replacement string to <code>&quot;_&quot;</code>.</li>\n<li>Explain why naming conventions require expected-output tests even when the code runs successfully.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why does this script keep both <code>dataset_raw</code> and <code>dataset_label</code>?</p>\n<h3>Example 2.5.8 - Compare text joining before and after conversion</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">first_text = input(&quot;First count: &quot;)\nsecond_text = input(&quot;Second count: &quot;)\n\njoined_text = first_text + second_text\nnumerical_total = int(first_text) + int(second_text)\n\nprint(&quot;Text joined:&quot;, joined_text)\nprint(&quot;Numbers added:&quot;, numerical_total)\n</code></pre></div>\n<p><strong>Expected output for this sample interaction</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">First count: 12\nSecond count: 8\nText joined: 128\nNumbers added: 20\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Both input values begin as strings.</li>\n<li>Adding two strings concatenates their characters, so <code>&quot;12&quot; + &quot;8&quot;</code> becomes <code>&quot;128&quot;</code>.</li>\n<li>Converting each string with <code>int()</code> changes the operation to numerical addition.</li>\n<li>The output makes the two meanings visible.</li>\n<li>Python follows types and operators. It cannot infer that a user intended arithmetic merely because the strings contain digits.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Seeing <code>128</code> and assuming the input values were added.</li>\n<li>Converting after joining, which would produce the integer <code>128</code>, not <code>20</code>.</li>\n<li>Using unclear variable names that hide whether values are text or numbers.</li>\n<li>Forgetting to test with values of different lengths.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Try inputs <code>5</code> and <code>10</code>. Predict both lines of output. Rename the source variables to make their string type even more obvious.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change only the second conversion to <code>float(second_text)</code>.</li>\n<li>Run with <code>12</code> and <code>8</code>. The total becomes <code>20.0</code>, which is valid but changes the result type.</li>\n<li>Repair both conversions to <code>int()</code> when the case represents whole-number counts.</li>\n<li>Explain when a float result would instead be appropriate.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is converting the joined text not equivalent to adding converted inputs?</p>\n<h3>Exercise 2.5.2</h3>\n<p>Create an interactive script for a fictional project that asks for:</p>\n<ul>\n<li>project title;</li>\n<li>number of invited participants;</li>\n<li>number of completed responses;</li>\n<li>average completion time in minutes.</li>\n</ul>\n<p>The script must display a labelled summary and calculate a completion percentage. Record what happens when you enter:</p>\n<ul>\n<li>valid values;</li>\n<li>a word where an integer is expected;</li>\n<li>zero invited participants;</li>\n<li>completed responses greater than invited participants.</li>\n</ul>\n<p>Do not hide these problems. Explain which ones produce Python errors and which ones produce logically questionable results.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_E2_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_E2_URL\">Open checkpoint solution</a></li>\n</ul>\n<h3>Example 2.5.9 - Transfer the input pattern to a gross-pay estimate</h3>\n<p><strong>Level:</strong> Intermediate transfer</p>\n<p><strong>Source note:</strong> Adapted from Exercise 3 in Charles R. Severance&#39;s open textbook <a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\"><em>Python for Everybody</em>, Chapter 2: Variables, expressions, and statements</a>. The variable names, reporting context, formatting, explanation and critical questions below are course adaptations. This Day 1 version intentionally excludes overtime rules because decisions belong in Python Foundations II.</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">role_label = input(&quot;Role label: &quot;)\nhours_worked = float(input(&quot;Hours worked: &quot;))\nhourly_rate_dkk = float(input(&quot;Hourly rate in DKK: &quot;))\n\ngross_pay_dkk = hours_worked * hourly_rate_dkk\npay_note = f&quot;{role_label}: estimated gross pay is {gross_pay_dkk:.2f} DKK&quot;\n\nprint(pay_note)\n</code></pre></div>\n<p><strong>Expected output for this sample interaction</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Role label: Student assistant\nHours worked: 12.5\nHourly rate in DKK: 145\nStudent assistant: estimated gross pay is 1812.50 DKK\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The role label remains a string because it is descriptive text.</li>\n<li><code>float(input(...))</code> collects text and converts suitable decimal input to a float.</li>\n<li>Using a float permits half-hours and rates containing øre.</li>\n<li>Multiplication produces a gross-pay estimate before tax, pension, supplements or other rules.</li>\n<li>The f-string combines the label and value while displaying exactly two decimal places.</li>\n<li>The calculation is sequential Day 1 code. Overtime or role-dependent rates require later decision logic.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Forgetting to convert input and then multiplying text in an unintended way.</li>\n<li>Using a comma as a decimal separator even though <code>float()</code> expects a full stop in this simple example.</li>\n<li>Treating two displayed decimal places as proof that the payroll rules are correct.</li>\n<li>Calling the result net pay or final salary when the code only multiplies hours by one rate.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a string input for the reporting period and include it in the final note. Then test <code>7.25</code> hours at <code>152.50</code> DKK and predict the result before running.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Remove <code>float()</code> from <code>hours_worked</code> but leave the rate as a float.</li>\n<li>Run the program and enter the sample values. Observe <code>TypeError</code> when Python tries to multiply a string by a float.</li>\n<li>Restore the conversion.</li>\n<li>Confirm both numeric types before calculating.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why does this program calculate only a basic estimate rather than a complete payroll result?</p>\n<h3>Example 2.5.10 - Transfer the input pattern to a temperature conversion</h3>\n<p><strong>Level:</strong> More advanced transfer</p>\n<p><strong>Source note:</strong> Adapted from Exercise 5 in Charles R. Severance&#39;s <a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\"><em>Python for Everybody</em>, Chapter 2</a>. The sensor-check scenario, traceable raw value, formatted report and data-quality discussion are original course adaptations.</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">sensor_id = input(&quot;Sensor identifier: &quot;)\ncelsius_text = input(&quot;Temperature in degrees Celsius: &quot;)\ncelsius = float(celsius_text)\n\nfahrenheit = celsius * 9 / 5 + 32\nreport_line = f&quot;Sensor {sensor_id}: {celsius:.1f} °C equals {fahrenheit:.1f} °F&quot;\n\nprint(report_line)\nprint(&quot;Original temperature text:&quot;, celsius_text)\n</code></pre></div>\n<p><strong>Expected output for this sample interaction</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Sensor identifier: CPH-07\nTemperature in degrees Celsius: 21.5\nSensor CPH-07: 21.5 °C equals 70.7 °F\nOriginal temperature text: 21.5\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li><code>sensor_id</code> remains a string because letters, punctuation and leading zeros are meaningful parts of the identifier.</li>\n<li><code>celsius_text</code> preserves the input as typed.</li>\n<li><code>float(celsius_text)</code> creates a numerical value for conversion.</li>\n<li><code>celsius * 9 / 5 + 32</code> applies the temperature-scale formula. Multiplication and division occur before addition.</li>\n<li>The f-string reports both values to one decimal place and includes their units.</li>\n<li>The final line preserves visible evidence of the original text input.</li>\n<li>The calculation cannot establish that the sensor was calibrated, correctly positioned or associated with the right location.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Converting the sensor identifier to a number and losing letters or a leading zero.</li>\n<li>Omitting the <code>+ 32</code> part of the formula.</li>\n<li>Reporting a temperature without its scale or unit.</li>\n<li>Treating a successfully converted sensor reading as automatically trustworthy.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the sample input to <code>0</code> °C and then <code>-10</code> °C. Predict the Fahrenheit results. Add a string variable for the non-sensitive sensor location and include it in the report.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Change the formula to <code>fahrenheit = celsius * (9 / (5 + 32))</code>.</li>\n<li>Run the code. Python produces a number without a traceback, but the grouping changes the formula.</li>\n<li>Repair the formula to <code>celsius * 9 / 5 + 32</code>.</li>\n<li>Use the known boundary test <code>0 °C = 32 °F</code> to verify the repair.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which value should remain text even though it contains digits, and why?</p>\n<hr>\n"
    }
  ],
  "1.17": [
    {
      "title": "Comments and beginner debugging",
      "html": "<h3>Suggested book chapters for Part F</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Priority</th>\n<th>Book and chapter</th>\n<th>Suggested focus</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Core debugging companion</td>\n<td><a href=\"https://www.py4e.com/html3/01-intro\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 1</a></td>\n<td>Read <strong>What could possibly go wrong?</strong> and <strong>Debugging</strong>. The chapter distinguishes syntax, logic and semantic problems and presents debugging as a normal activity.</td>\n</tr>\n<tr>\n<td>Core comments companion</td>\n<td><a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 2</a></td>\n<td>Read <strong>Comments</strong> and compare comments that repeat syntax with comments that explain purpose, units or assumptions.</td>\n</tr>\n<tr>\n<td>Optional text-focused example</td>\n<td><a href=\"https://www.nltk.org/book/ch01.html\" target=\"_blank\" rel=\"noreferrer\">Bird, Klein and Loper, <em>Natural Language Processing with Python</em>, Chapter 1</a></td>\n<td>Revisit the <code>SyntaxError</code> example in <strong>Section 1.1</strong>. Use the current tutorial for modern traceback wording and the complete repair routine.</td>\n</tr>\n</tbody></table></div>\n<h2>Write useful comments</h2>\n<p>A comment begins with <code>#</code>. Python ignores the remainder of that line. Comments are written for humans.</p>\n<p>Useful comments can explain:</p>\n<ul>\n<li>the purpose of a section;</li>\n<li>the source or meaning of a fixed value;</li>\n<li>an assumption in a calculation;</li>\n<li>a data-quality decision;</li>\n<li>why an apparently unusual step is necessary;</li>\n<li>a task that must be completed later.</li>\n</ul>\n<p>Avoid comments that merely repeat obvious syntax.</p>\n<h3>Example 2.6.1 - Explain purpose and assumptions</h3>\n<p><strong>Level:</strong> Simple</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\"># Summarise the first technical check of a monthly file.\ntotal_records = 240\nmissing_records = 18\n\n# Assumption: missing_records is a non-overlapping count.\ncomplete_records = total_records - missing_records\n\nprint(&quot;Complete records:&quot;, complete_records)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Complete records: 222\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 is a comment describing the purpose of the small code section.</li>\n<li>Lines 2 and 3 assign source counts.</li>\n<li>The second comment records an assumption relevant to interpreting the subtraction.</li>\n<li>Line 6 performs the calculation.</li>\n<li>The final line displays the result.</li>\n<li>Neither comment appears in the output because Python ignores comment text.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Writing <code># subtract missing records</code> when the code already makes that obvious.</li>\n<li>Allowing a comment to become outdated after the code changes.</li>\n<li>Using comments to justify a questionable decision without evidence.</li>\n<li>Writing essential student instructions only inside code comments where they may be missed.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add <code>duplicate_records = 4</code>, update the calculation, and revise the assumption comment to describe whether the categories overlap.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Place <code>#</code> at the beginning of the assignment <code>total_records = 240</code>.</li>\n<li>Restart the runtime and run the cell.</li>\n<li>Observe <code>NameError</code> because the assignment has become a comment.</li>\n<li>Repair it by removing the unintended <code>#</code> and rerunning the cell.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is the assumption comment more useful than a comment saying <code># subtract</code>?</p>\n<h3>Example 2.6.2 - Use end-of-line comments carefully</h3>\n<p><strong>Level:</strong> Intermediate</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">MINIMUM_REQUIRED = 80  # Stated project threshold, pending review.\ncompletion_rate = 76.5  # Percentage, not a proportion.\n\ngap = MINIMUM_REQUIRED - completion_rate\nprint(&quot;Gap to stated threshold:&quot;, gap)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Gap to stated threshold: 3.5\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The first end-of-line comment records that the threshold is stated but still subject to review.</li>\n<li>The second comment clarifies the unit of <code>76.5</code>.</li>\n<li>The blank line separates inputs from calculation.</li>\n<li>The gap calculation uses values expressed on the same percentage scale.</li>\n<li>The output label retains the careful wording &quot;stated threshold&quot;.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Adding long end-of-line comments that make code hard to scan.</li>\n<li>Treating <code>80</code> as natural or neutral because it is uppercase.</li>\n<li>Mixing a proportion such as <code>0.765</code> with a percentage such as <code>80</code>.</li>\n<li>Updating the threshold without updating its comment or documentation.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Move one long end-of-line comment to its own line above the related code. Change the observed rate and update any wording that no longer fits.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Insert <code>#</code> immediately before <code>- completion_rate</code> in the calculation line.</li>\n<li>Run the code. The calculation becomes only <code>gap = MINIMUM_REQUIRED</code>, producing a plausible but wrong result.</li>\n<li>Repair the misplaced comment marker.</li>\n<li>Explain why comments can create logical errors without producing a traceback.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What unit must both operands use for the gap calculation to be meaningful?</p>\n<h3>Example 2.6.3 - Do not confuse strings with comments</h3>\n<p><strong>Level:</strong> More advanced</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">note = &quot;Missing categories are labelled Unknown in this teaching example.&quot;\n# The string above is stored data. This line is a comment.\n\nprint(note)\nprint(type(note))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Missing categories are labelled Unknown in this teaching example.\n&lt;class &#39;str&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>Line 1 assigns a string to <code>note</code>. Python stores the text as data.</li>\n<li>Line 2 is a comment. Python does not store it as the value of a variable.</li>\n<li>The print statement displays the stored string.</li>\n<li><code>type(note)</code> confirms that the stored note is a string.</li>\n<li>Triple-quoted text is also a string literal, not a special general-purpose comment syntax. It may be used as a documentation string in specific contexts introduced later.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Calling every block of triple-quoted text a comment.</li>\n<li>Expecting a comment to be available as program data.</li>\n<li>Printing internal notes that should not appear in user-facing output.</li>\n<li>Storing sensitive information in comments or notebook output.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the stored note to a statement that should appear in a report. Add a separate comment explaining why the note is included.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Put <code>#</code> before <code>note = ...</code> and restart the runtime.</li>\n<li>Run the cell and observe <code>NameError</code> at <code>print(note)</code>.</li>\n<li>Repair the assignment by removing <code>#</code>.</li>\n<li>Explain the difference between documentation for a programmer and text intended as program output.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which line creates data that can later be printed?</p>\n<h3>Comment quality checklist</h3>\n<p>Before keeping a comment, ask:</p>\n<ul>\n<li>Does it explain purpose, context, unit, source, assumption or reasoning?</li>\n<li>Would a clearer variable name make the comment unnecessary?</li>\n<li>Is the comment still accurate after the latest code change?</li>\n<li>Does it reveal confidential or personal information?</li>\n<li>Does it distinguish a confirmed fact from an assumption or pending decision?</li>\n</ul>\n<h3>Exercise 2.6.1</h3>\n<p>Add comments to a short script of your own. Include:</p>\n<ul>\n<li>one purpose comment;</li>\n<li>one unit comment;</li>\n<li>one assumption comment;</li>\n<li>one pending-review comment.</li>\n</ul>\n<p>Then delete any comment that merely repeats an obvious assignment. Ask another student whether the remaining comments help them explain the code.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_F1_URL\">Open checkpoint hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_F1_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Read and repair beginner error messages</h2>\n<p>An error message is information. It normally tells you:</p>\n<ul>\n<li>where Python noticed a problem;</li>\n<li>what category of problem occurred;</li>\n<li>what Python could not do.</li>\n</ul>\n<p>When Python reports an error during execution, Colab often displays a <strong>traceback</strong>. Read it from the bottom upwards at first.</p>\n<h3>A beginner traceback-reading routine</h3>\n<ol>\n<li>Read the final line.</li>\n<li>Identify the error type, such as <code>NameError</code> or <code>TypeError</code>.</li>\n<li>Read the short explanation after the error type.</li>\n<li>Locate the highlighted line or line number.</li>\n<li>Compare names, quotation marks, brackets, operators and types.</li>\n<li>Fix the smallest likely cause.</li>\n<li>Rerun the corrected cell.</li>\n<li>If the cell depends on earlier variables, rerun the relevant earlier cells from top to bottom.</li>\n<li>Compare the output with an expected result. A missing traceback does not prove that the logic is correct.</li>\n</ol>\n<blockquote>\n<p>[Screenshot placeholder PF1-07: A Colab traceback annotated with line number, code line, error type and explanatory message.]</p>\n</blockquote>\n<h3>Example 2.6.4 - Error clinic: <code>SyntaxError</code></h3>\n<p><code>SyntaxError</code> means Python cannot understand the code as valid Python grammar. Common causes include missing quotation marks, missing brackets, invalid variable names and incomplete expressions.</p>\n<p><strong>Deliberately broken code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">case_title = &quot;Mobility survey\nprint(case_title)\n</code></pre></div>\n<p><strong>Typical error output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">SyntaxError: unterminated string literal\n</code></pre></div>\n<p>The exact wording may include a line number or say <code>detected at line 1</code>.</p>\n<p><strong>Repaired code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">case_title = &quot;Mobility survey&quot;\nprint(case_title)\n</code></pre></div>\n<p><strong>Expected repaired output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Mobility survey\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The broken first line opens a string with <code>&quot;</code> but never closes it.</li>\n<li>Python reaches the end of the line while still expecting the string to continue.</li>\n<li>The repaired first line adds the matching closing quotation mark.</li>\n<li>The second line can then display the assigned string.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Fixing the line highlighted by Python without checking the line immediately above it.</li>\n<li>Adding another quotation mark in the wrong place.</li>\n<li>Replacing straight quotation marks with typographic quotation marks.</li>\n<li>Assuming a syntax repair also guarantees correct case logic.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Change the title to a phrase containing an apostrophe and choose quotation marks that keep the string valid.</p>\n<p><strong>Break it again, then repair it</strong></p>\n<ol>\n<li>Change the second line to <code>print(case_title</code> by removing the closing parenthesis.</li>\n<li>Observe the new <code>SyntaxError</code>.</li>\n<li>Restore the parenthesis.</li>\n<li>Compare this bracket error with the original quotation-mark error.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What unfinished structure caused the first syntax error?</p>\n<h3>Example 2.6.5 - Error clinic: <code>NameError</code></h3>\n<p><code>NameError</code> means Python does not currently know a name that the code tried to use. The name may be misspelt, never defined, deleted, or lost after a runtime restart.</p>\n<p><strong>Deliberately broken code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">total_records = 250\nmissing_records = 18\ncomplete_records = total_records - missing_record\nprint(complete_records)\n</code></pre></div>\n<p><strong>Typical error output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">NameError: name &#39;missing_record&#39; is not defined\n</code></pre></div>\n<p><strong>Repaired code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">total_records = 250\nmissing_records = 18\ncomplete_records = total_records - missing_records\nprint(complete_records)\n</code></pre></div>\n<p><strong>Expected repaired output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">232\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The source variable is defined as plural <code>missing_records</code>.</li>\n<li>The broken calculation uses singular <code>missing_record</code>.</li>\n<li>Python treats these as different names.</li>\n<li>The repaired calculation uses the exact defined spelling.</li>\n<li>The final print statement then receives the derived integer <code>232</code>.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Checking only spelling while overlooking capitalisation.</li>\n<li>Correcting one occurrence but leaving other mismatches.</li>\n<li>Running a dependent cell before its setup cell.</li>\n<li>Creating several similar names that differ by one character.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Rename <code>missing_records</code> to <code>records_with_missing_values</code> everywhere. Use search to verify that the old name is gone.</p>\n<p><strong>Break it again, then repair it</strong></p>\n<ol>\n<li>Restart the runtime and run only the final <code>print(complete_records)</code> line.</li>\n<li>Observe another <code>NameError</code>, this time caused by missing notebook state rather than spelling.</li>\n<li>Rerun the repaired code from top to bottom.</li>\n<li>Record the two different causes of the same error type.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why can correctly spelt code still raise <code>NameError</code> in a notebook?</p>\n<h3>Example 2.6.6 - Error clinic: <code>TypeError</code></h3>\n<p><code>TypeError</code> means an operation received a type of value it cannot use in that way.</p>\n<p><strong>Deliberately broken code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">total_records = &quot;250&quot;\nmissing_records = 18\ncomplete_records = total_records - missing_records\nprint(complete_records)\n</code></pre></div>\n<p><strong>Typical error output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">TypeError: unsupported operand type(s) for -: &#39;str&#39; and &#39;int&#39;\n</code></pre></div>\n<p><strong>Repaired code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">total_records = int(&quot;250&quot;)\nmissing_records = 18\ncomplete_records = total_records - missing_records\nprint(complete_records)\n</code></pre></div>\n<p><strong>Expected repaired output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">232\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>In the broken code, <code>total_records</code> is a string.</li>\n<li><code>missing_records</code> is an integer.</li>\n<li>Subtraction is not defined between a string and integer.</li>\n<li>The repaired first line converts suitable whole-number text to an integer.</li>\n<li>The subtraction then operates on two integers.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Converting the wrong variable.</li>\n<li>Using <code>str(missing_records)</code> and turning both values into text even though subtraction is intended.</li>\n<li>Removing quotation marks without considering where the value came from.</li>\n<li>Assuming all values in a CSV column share the same clean type.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Print the type of each source value before and after the repair. Explain why the converter matches the intended arithmetic.</p>\n<p><strong>Break it again, then repair it</strong></p>\n<ol>\n<li>Change the calculation to <code>complete_records = total_records + &quot;18&quot;</code>.</li>\n<li>Observe a <code>TypeError</code> because the repaired <code>total_records</code> is an integer and <code>&quot;18&quot;</code> is a string.</li>\n<li>Repair the second value as the integer <code>18</code>.</li>\n<li>Verify the expected result rather than accepting any error-free <code>+</code> operation.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>How does the final error line identify the two incompatible types?</p>\n<h3>Example 2.6.7 - Error clinic: <code>ValueError</code></h3>\n<p><code>ValueError</code> means a function received the right broad type but an unsuitable value. A common beginner case is asking <code>int()</code> to convert text that is not a valid whole-number representation.</p>\n<p><strong>Deliberately broken code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">completion_text = &quot;93.5&quot;\ncompletion_number = int(completion_text)\nprint(completion_number)\n</code></pre></div>\n<p><strong>Typical error output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">ValueError: invalid literal for int() with base 10: &#39;93.5&#39;\n</code></pre></div>\n<p><strong>Repaired code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">completion_text = &quot;93.5&quot;\ncompletion_number = float(completion_text)\nprint(completion_number)\n</code></pre></div>\n<p><strong>Expected repaired output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">93.5\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The source is correctly a string for a conversion function to inspect.</li>\n<li>Its content includes a decimal point.</li>\n<li><code>int()</code> cannot directly interpret that string as a whole-number representation.</li>\n<li><code>float()</code> matches the decimal content and produces <code>93.5</code>.</li>\n<li>The repair preserves the decimal information.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Using <code>int(float(completion_text))</code> and unintentionally truncating the result.</li>\n<li>Deleting non-numerical content without documenting the cleaning decision.</li>\n<li>Assuming every <code>ValueError</code> has the same cause.</li>\n<li>Writing a bare decimal comma and expecting Python&#39;s default converters to interpret local formatting.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Test <code>&quot;93&quot;</code>, <code>&quot;93.0&quot;</code>, <code>&quot;93,5&quot;</code> and <code>&quot;ninety-three&quot;</code> separately with <code>int()</code> and <code>float()</code>. Record which conversions succeed.</p>\n<p><strong>Break it again, then repair it</strong></p>\n<ol>\n<li>Change the source string to an empty string <code>&quot;&quot;</code>.</li>\n<li>Observe <code>ValueError</code> from <code>float()</code>.</li>\n<li>Repair the teaching example with a valid value.</li>\n<li>Explain why a real empty value should be handled as missing data rather than invented as zero.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is <code>float()</code> a better repair than converting first to float and then to integer here?</p>\n<h3>Example 2.6.8 - Error clinic: <code>ZeroDivisionError</code></h3>\n<p><code>ZeroDivisionError</code> occurs when a numerical expression tries to divide by zero.</p>\n<p><strong>Deliberately broken code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">completed_responses = 0\ninvited_participants = 0\ncompletion_rate = completed_responses / invited_participants * 100\nprint(completion_rate)\n</code></pre></div>\n<p><strong>Typical error output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">ZeroDivisionError: division by zero\n</code></pre></div>\n<p><strong>Repaired teaching example</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">completed_responses = 72\ninvited_participants = 90\ncompletion_rate = completed_responses / invited_participants * 100\nprint(completion_rate)\n</code></pre></div>\n<p><strong>Expected repaired output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">80.0\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The broken example uses zero as the denominator.</li>\n<li>A rate relative to zero invited participants is not defined by this formula.</li>\n<li>The repaired teaching values use a positive denominator.</li>\n<li><code>72 / 90 * 100</code> produces <code>80.0</code>.</li>\n<li>In real work, replacing a zero with an invented positive number is not an acceptable repair. The case requires a rule for missing or not-applicable rates.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Adding a tiny number only to avoid the error without analytical justification.</li>\n<li>replacing zero with one and producing a misleading rate.</li>\n<li>Confusing a zero numerator, which is valid with a positive denominator, with a zero denominator.</li>\n<li>Hiding the affected record instead of documenting the decision.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Use <code>0</code> completed responses and <code>90</code> invited participants. Predict the valid result and explain why no error occurs.</p>\n<p><strong>Break it again, then repair it</strong></p>\n<ol>\n<li>Set only <code>invited_participants</code> back to <code>0</code>.</li>\n<li>Observe the same error.</li>\n<li>Restore a justified positive teaching value.</li>\n<li>Write a note describing how later conditional logic could mark the rate as not applicable when the denominator is zero.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which part of a division expression must not be zero?</p>\n<h3>Example 2.6.9 - Error clinic: <code>IndentationError</code></h3>\n<p>Python uses indentation to group lines in structures such as conditions, loops and functions. Those structures are taught later, but accidental leading spaces can already cause an error.</p>\n<p><strong>Deliberately broken code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">    print(&quot;Dataset loaded&quot;)\nprint(&quot;Initial check started&quot;)\n</code></pre></div>\n<p><strong>Typical error output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">IndentationError: unexpected indent\n</code></pre></div>\n<p><strong>Repaired code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">print(&quot;Dataset loaded&quot;)\nprint(&quot;Initial check started&quot;)\n</code></pre></div>\n<p><strong>Expected repaired output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Dataset loaded\nInitial check started\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The broken first line begins with spaces even though no surrounding structure requires indentation.</li>\n<li>Python reports the indentation as unexpected.</li>\n<li>The repaired lines begin at the same left margin.</li>\n<li>Python executes them in sequence.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Mixing tabs and spaces after copying code.</li>\n<li>Removing all indentation from code that actually belongs inside a later condition or loop.</li>\n<li>Looking only at visible text and overlooking leading whitespace.</li>\n<li>Pasting code from a formatted document that changes spacing.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a third unindented print line describing a completed check. Confirm that all three align.</p>\n<p><strong>Break it again, then repair it</strong></p>\n<ol>\n<li>Add spaces before only the second line.</li>\n<li>Observe <code>IndentationError</code> again.</li>\n<li>Repair the alignment.</li>\n<li>Use Colab&#39;s code formatting or visible indentation guides if available.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is indentation part of Python syntax rather than only visual style?</p>\n<h3>Example 2.6.10 - Error clinic: A logical error with no traceback</h3>\n<p>Some of the most important errors do not produce an error message. Python can execute the wrong formula perfectly.</p>\n<p><strong>Logically broken code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">complete_records = 180\ntotal_records = 200\ncompletion_rate = total_records / complete_records * 100\n\nprint(&quot;Completion rate:&quot;, completion_rate)\n</code></pre></div>\n<p><strong>Wrong but valid output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Completion rate: 111.11111111111111\n</code></pre></div>\n<p><strong>Repaired code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">complete_records = 180\ntotal_records = 200\ncompletion_rate = complete_records / total_records * 100\n\nprint(&quot;Completion rate:&quot;, completion_rate)\n</code></pre></div>\n<p><strong>Expected repaired output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Completion rate: 90.0\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The broken formula divides the total by the complete count.</li>\n<li>Python is allowed to perform that division, so no traceback appears.</li>\n<li>The value above 100 per cent is a clue that the formula may be reversed.</li>\n<li>The repaired formula places the part in the numerator and the whole in the denominator.</li>\n<li>The expected result is confirmed by a simple hand calculation: 180 is 90 per cent of 200.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Treating &quot;the code ran&quot; as proof that the calculation is correct.</li>\n<li>Ignoring impossible or implausible values.</li>\n<li>Changing the label instead of fixing the calculation.</li>\n<li>Assuming every rate must be below 100 without checking whether the metric can genuinely exceed it.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Test <code>complete_records = 0</code>, <code>100</code> and <code>200</code> with a total of <code>200</code>. Predict the three rates and use them as boundary tests.</p>\n<p><strong>Break it again, then repair it</strong></p>\n<ol>\n<li>Change <code>* 100</code> to <code>/ 100</code>.</li>\n<li>Observe the error-free but incorrect result <code>0.009</code>.</li>\n<li>Repair the operator.</li>\n<li>Write an expected-output test in a comment before running.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>What evidence helped you recognise the wrong formula without a traceback?</p>\n<h3>Common beginner-error reference</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Error type</th>\n<th>Plain-language meaning</th>\n<th>First things to check</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>SyntaxError</code></td>\n<td>Python cannot parse the code</td>\n<td>Quotes, brackets, colons, incomplete lines, invalid names</td>\n</tr>\n<tr>\n<td><code>NameError</code></td>\n<td>Python does not know a name</td>\n<td>Spelling, capitalisation, execution order, runtime restart</td>\n</tr>\n<tr>\n<td><code>TypeError</code></td>\n<td>An operation received incompatible types</td>\n<td><code>type()</code>, quotation marks, conversion, intended operation</td>\n</tr>\n<tr>\n<td><code>ValueError</code></td>\n<td>The type is acceptable but the value is unsuitable</td>\n<td>Actual text content, decimal format, empty value, converter choice</td>\n</tr>\n<tr>\n<td><code>ZeroDivisionError</code></td>\n<td>The denominator is zero</td>\n<td>Source data, formula, not-applicable rule</td>\n</tr>\n<tr>\n<td><code>IndentationError</code></td>\n<td>Leading spaces do not fit Python&#39;s structure</td>\n<td>Tabs, spaces, copied code, block alignment</td>\n</tr>\n<tr>\n<td>No traceback</td>\n<td>Python ran, but the logic may be wrong</td>\n<td>Expected output, units, formula direction, boundaries, assumptions</td>\n</tr>\n</tbody></table></div>\n<h3>Exercise 2.6.2 - Error laboratory</h3>\n<p>Create a fresh notebook section with seven deliberately broken cells, one for each error clinic above. For each cell, record:</p>\n<ol>\n<li>your prediction;</li>\n<li>the exact error type or wrong output;</li>\n<li>the likely cause;</li>\n<li>the smallest repair;</li>\n<li>the repaired output;</li>\n<li>one test that supports the repair.</li>\n</ol>\n<p>Do not paste personal data into error screenshots.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"COLAB_ERROR_LAB_URL\">Open error-laboratory starter notebook</a></li>\n<li><a href=\"#\" data-link-placeholder=\"HINTS_F2_URL\">Open error-laboratory hints</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_F2_URL\">Open error-laboratory solution</a></li>\n</ul>\n<hr>\n"
    }
  ],
  "1.18": [
    {
      "title": "Integration, mini-cases and self-test",
      "html": "<h3>Suggested book chapters for Part G</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Priority</th>\n<th>Book and chapter</th>\n<th>Suggested focus</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Core consolidation</td>\n<td><a href=\"https://www.py4e.com/html3/01-intro\" target=\"_blank\" rel=\"noreferrer\">Severance, <em>Python for Everybody</em>, Chapter 1</a> and <a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">Chapter 2</a></td>\n<td>Review only the sections connected to a self-test error or uncertainty. Then use the Chapter 2 exercises as additional transfer practice rather than rereading both chapters from beginning to end.</td>\n</tr>\n<tr>\n<td>Optional language-data transfer</td>\n<td><a href=\"https://www.nltk.org/book/ch01.html\" target=\"_blank\" rel=\"noreferrer\">Bird, Klein and Loper, <em>Natural Language Processing with Python</em>, Chapter 1</a></td>\n<td>Revisit <strong>Sections 2.3 and 2.4</strong> and explain which variables, assignments, expressions and string operations you now recognise. Lists, corpora and frequency analysis belong to later tutorials.</td>\n</tr>\n<tr>\n<td>Looking ahead, not Day 1</td>\n<td><a href=\"https://www.nltk.org/book/ch03.html\" target=\"_blank\" rel=\"noreferrer\">NLTK Book, Chapter 3: Processing Raw Text</a></td>\n<td>Bookmark this chapter for the later text-processing lecture. Do not treat it as required Python Foundations I reading.</td>\n</tr>\n</tbody></table></div>\n<h2>Read complete foundation scripts</h2>\n<p>The next examples combine the ideas from the whole tutorial. Do not rush to run them. First annotate every literal, variable, assignment, expression, operator, type conversion, comparison, comment and output statement that you recognise.</p>\n<h3>Example 2.7.1 - Produce a monthly data-quality summary</h3>\n<p><strong>Level:</strong> Intermediate integration</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\"># Report settings\nREPORT_TITLE = &quot;Monthly consultation data check&quot;\ntotal_records = 750\nmissing_records = 27\nduplicate_records = 9\n\n# Initial technical summary. Problem counts are assumed not to overlap.\nproblem_records = missing_records + duplicate_records\nusable_records = total_records - problem_records\nusable_rate = usable_records / total_records * 100\nhas_identified_problems = problem_records &gt; 0\n\nprint(REPORT_TITLE)\nprint(&quot;Total records:&quot;, total_records)\nprint(&quot;Problem records:&quot;, problem_records)\nprint(&quot;Usable records:&quot;, usable_records)\nprint(f&quot;Usable rate: {usable_rate:.1f}%&quot;)\nprint(&quot;Identified problems present:&quot;, has_identified_problems)\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Monthly consultation data check\nTotal records: 750\nProblem records: 36\nUsable records: 714\nUsable rate: 95.2%\nIdentified problems present: True\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The first comment labels a settings section.</li>\n<li><code>REPORT_TITLE</code> is uppercase because it is treated as a constant setting.</li>\n<li>The next three assignments store integer source counts.</li>\n<li>The second comment documents that the two problem categories are assumed not to overlap.</li>\n<li><code>problem_records</code> adds the two problem counts.</li>\n<li><code>usable_records</code> subtracts the combined problem count from the total.</li>\n<li><code>usable_rate</code> divides part by whole and multiplies by <code>100</code>.</li>\n<li><code>problem_records &gt; 0</code> produces a Boolean value.</li>\n<li>The first four output statements display a heading and counts.</li>\n<li>The f-string displays the rate to one decimal place.</li>\n<li>The final output statement displays the Boolean flag.</li>\n<li>The script provides an initial technical summary. It does not establish data representativeness, reliability or fairness.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Double-counting records that are both missing and duplicated.</li>\n<li>Naming records &quot;usable&quot; before defining every relevant quality rule.</li>\n<li>Believing one decimal place makes the rate more accurate.</li>\n<li>Treating the Boolean flag as a complete data-quality judgement.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add <code>invalid_date_records = 14</code>. Update the formula, comment and output. Then replace one literal count with keyboard input and apply the correct conversion.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Reverse the rate formula to <code>total_records / usable_records * 100</code>.</li>\n<li>Run the cell. It will produce an error-free value above 100 per cent.</li>\n<li>Repair the part-over-whole order.</li>\n<li>Use the expected range and a hand calculation as tests.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which line creates the Boolean, and which line merely displays it?</p>\n<h3>Example 2.7.2 - Compare expected and received file structure</h3>\n<p><strong>Level:</strong> More advanced integration</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\"># Expected structure agreed for this fictional teaching case.\nEXPECTED_COLUMNS = 12\n\ndataset_name = &quot;October service log&quot;\nreceived_columns_text = &quot;10&quot;\nreceived_columns = int(received_columns_text)\n\nmissing_column_count = EXPECTED_COLUMNS - received_columns\nstructure_matches_expectation = received_columns == EXPECTED_COLUMNS\n\nsummary = (\n    f&quot;{dataset_name}: received {received_columns} of &quot;\n    f&quot;{EXPECTED_COLUMNS} expected columns.&quot;\n)\n\nprint(summary)\nprint(&quot;Missing column count:&quot;, missing_column_count)\nprint(&quot;Structure matches expectation:&quot;, structure_matches_expectation)\nprint(&quot;Original input type:&quot;, type(received_columns_text))\nprint(&quot;Converted input type:&quot;, type(received_columns))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">October service log: received 10 of 12 expected columns.\nMissing column count: 2\nStructure matches expectation: False\nOriginal input type: &lt;class &#39;str&#39;&gt;\nConverted input type: &lt;class &#39;int&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The first comment explains the origin of the expectation within the fictional case.</li>\n<li><code>EXPECTED_COLUMNS</code> stores the fixed structural expectation.</li>\n<li><code>dataset_name</code> is a string label.</li>\n<li><code>received_columns_text</code> simulates numerical-looking text from an input or file.</li>\n<li><code>int()</code> converts the suitable text to an integer.</li>\n<li>Subtraction calculates a difference of <code>2</code>.</li>\n<li>Equality comparison produces <code>False</code> because 10 does not equal 12.</li>\n<li>Parentheses allow the long <code>summary</code> assignment to span two source lines.</li>\n<li>Adjacent f-strings inside the parentheses form one string.</li>\n<li>The output displays the summary, count difference, Boolean result and evidence of conversion.</li>\n<li>A matching number of columns would still not prove that the names, types or values are correct.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Treating a matching column count as complete schema validation.</li>\n<li>Converting an invalid or empty value without a plan.</li>\n<li>Receiving more columns than expected and labelling a negative difference as &quot;missing&quot;.</li>\n<li>Hiding the original text before investigating conversion problems.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Test received counts of <code>12</code> and <code>14</code>. For <code>14</code>, rename or redesign the difference variable so the output remains truthful. Do not use <code>if</code> yet.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Remove <code>int()</code> and assign <code>received_columns = received_columns_text</code>.</li>\n<li>Run the cell and observe <code>TypeError</code> during subtraction.</li>\n<li>Restore the conversion.</li>\n<li>Confirm the types with the existing diagnostic output.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Why is a correct column count only one part of a structural check?</p>\n<h3>Example 2.7.3 - Create an interactive organisational intake note</h3>\n<p><strong>Level:</strong> More advanced integration</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\"># Collect a small, non-sensitive teaching example.\norganisation = input(&quot;Organisation: &quot;)\nreporting_period = input(&quot;Reporting period: &quot;)\nrecords_received = int(input(&quot;Records received: &quot;))\nrecords_excluded = int(input(&quot;Records excluded: &quot;))\n\nrecords_retained = records_received - records_excluded\nretention_rate = records_retained / records_received * 100\nhas_exclusions = records_excluded &gt; 0\n\nprint(f&quot;\\n{organisation} | {reporting_period}&quot;)\nprint(&quot;Records retained:&quot;, records_retained)\nprint(f&quot;Retention rate: {retention_rate:.1f}%&quot;)\nprint(&quot;Has exclusions:&quot;, has_exclusions)\n</code></pre></div>\n<p><strong>Expected output for this sample interaction</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Organisation: Community Services Unit\nReporting period: October 2026\nRecords received: 320\nRecords excluded: 16\n\nCommunity Services Unit | October 2026\nRecords retained: 304\nRetention rate: 95.0%\nHas exclusions: True\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The comment limits the example to non-sensitive teaching information.</li>\n<li>The first two inputs remain strings.</li>\n<li>The next two inputs are converted immediately to integers.</li>\n<li>Subtraction derives the retained count.</li>\n<li>The rate expression uses retained records as the part and received records as the whole.</li>\n<li>The comparison produces a Boolean indicating whether any exclusions exist.</li>\n<li><code>\\n</code> separates input interaction from the displayed note.</li>\n<li>The final statements display a heading, count, formatted rate and flag.</li>\n<li>The reason, legitimacy and possible overlap of exclusions still require documentation.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Entering zero received records and causing division by zero.</li>\n<li>Entering exclusions greater than received records and accepting a negative result.</li>\n<li>Entering identifying or sensitive information into a teaching notebook.</li>\n<li>Treating a high retention rate as proof of high-quality or representative data.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add a string input describing the exclusion rule. Display it in the final note. Then test a case with zero exclusions and predict the Boolean result.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Enter <code>sixteen</code> instead of <code>16</code> for excluded records.</li>\n<li>Observe <code>ValueError</code> from <code>int()</code>.</li>\n<li>Rerun and enter a valid whole number.</li>\n<li>Record how later exception handling and validation could improve the interaction.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which types are returned by the four <code>input()</code> calls before conversion is applied?</p>\n<h3>Example 2.7.4 - Preserve raw metadata and publish a standardisation note</h3>\n<p><strong>Level:</strong> More advanced non-mathematical integration</p>\n<p>This example combines assignment, strings, method-call expressions, <code>None</code>, Booleans, <code>type()</code> and an f-string. It does not use conditions, loops or custom functions.</p>\n<p><strong>Code</strong></p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\"># Raw metadata copied from a fictional intake form.\nraw_project_title = &quot;  neighbourhood ACCESS study  &quot;\nraw_field_label = &quot;Service Access?&quot;\nraw_missing_marker = &quot; n/a &quot;\nreviewer_note = None\n\nproject_title = raw_project_title.strip().title()\nfield_name = raw_field_label.strip().lower().replace(&quot; &quot;, &quot;_&quot;).replace(&quot;?&quot;, &quot;&quot;)\nmissing_marker = raw_missing_marker.strip().upper()\nreview_complete = False\n\nstandardisation_note = (\n    f&quot;Project: {project_title}\\n&quot;\n    f&quot;Proposed field name: {field_name}\\n&quot;\n    f&quot;Preserved missing marker: {missing_marker}\\n&quot;\n    f&quot;Review complete: {review_complete}\\n&quot;\n    f&quot;Reviewer note: {reviewer_note}&quot;\n)\n\nprint(standardisation_note)\nprint(&quot;Raw project title retained:&quot;, raw_project_title)\nprint(&quot;Reviewer-note type:&quot;, type(reviewer_note))\n</code></pre></div>\n<p><strong>Expected output</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Project: Neighbourhood Access Study\nProposed field name: service_access\nPreserved missing marker: N/A\nReview complete: False\nReviewer note: None\nRaw project title retained:   neighbourhood ACCESS study  \nReviewer-note type: &lt;class &#39;NoneType&#39;&gt;\n</code></pre></div>\n<p><strong>Line-by-line explanation</strong></p>\n<ul>\n<li>The comment identifies the provenance of the first group of values.</li>\n<li>The four <code>raw_...</code> variables preserve received metadata rather than overwriting it.</li>\n<li><code>reviewer_note = None</code> states that no review note is available yet. It does not claim that a reviewer wrote an empty note.</li>\n<li><code>strip()</code> removes outer spaces and <code>title()</code> creates a presentation version of the project title.</li>\n<li>The field-name expression standardises case, replaces spaces with underscores and removes the question mark.</li>\n<li>The missing marker is preserved as text and displayed in uppercase. It is not automatically converted to <code>None</code>, because that would be a separate data-cleaning decision.</li>\n<li><code>review_complete = False</code> stores the current workflow state explicitly.</li>\n<li>Parentheses let one f-string expression span several source lines. Each <code>\\n</code> creates a line break in the stored note.</li>\n<li>The final outputs show the prepared note, the untouched raw title and the special type of <code>None</code>.</li>\n<li>The code prepares metadata for review. It does not decide whether <code>N/A</code> means missing, not applicable, refused or something else.</li>\n</ul>\n<p><strong>Common mistakes</strong></p>\n<ul>\n<li>Replacing every source marker immediately and losing information about how missingness was encoded.</li>\n<li>Assuming <code>.title()</code> handles every organisation, place or personal name correctly.</li>\n<li>Changing <code>review_complete</code> to <code>True</code> merely because the script ran.</li>\n<li>Confusing the string <code>&quot;None&quot;</code> with the special value <code>None</code>.</li>\n</ul>\n<p><strong>Modify it</strong></p>\n<p>Add <code>raw_source_label = &quot;  MOBILE portal &quot;</code>. Produce a cleaned presentation label and include both the raw and cleaned versions in the standardisation note.</p>\n<p><strong>Break it, observe it, repair it</strong></p>\n<ol>\n<li>Remove <code>.replace(&quot;?&quot;, &quot;&quot;)</code> from the field-name expression.</li>\n<li>Run the code. Python produces no traceback, but the proposed field name ends with punctuation.</li>\n<li>Restore the documented replacement.</li>\n<li>Compare both outputs and explain why a naming policy is a human requirement, not a Python syntax rule.</li>\n</ol>\n<p><strong>Check your understanding</strong></p>\n<p>Which variables preserve evidence, which variables contain proposed standardisations, and which variable records workflow state?</p>\n<h3>Exercise 2.7.1 - Integrate the foundation</h3>\n<p>Choose one of the four integrated scripts. Without looking at the explanation, annotate:</p>\n<ul>\n<li>every literal;</li>\n<li>every variable;</li>\n<li>every assignment statement;</li>\n<li>every arithmetic expression;</li>\n<li>every Boolean expression;</li>\n<li>every conversion;</li>\n<li>every comment;</li>\n<li>every <code>print()</code> call;</li>\n<li>every assumption that Python cannot verify.</li>\n</ul>\n<p>Then explain the script aloud in no more than two minutes.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"ANNOTATION_WORKSHEET_URL\">Open annotation worksheet</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SOLUTIONS_G1_URL\">Open checkpoint solution</a></li>\n</ul>\n<hr>\n<h2>Mini-case activities</h2>\n<p>The mini-cases are student activities, not additional worked solutions. Use the techniques in this tutorial, but keep your completed code in your own notebook. Solutions belong on the separate solution page.</p>\n<h3>Mini-case 2.7.1 - Municipal consultation intake</h3>\n<p><strong>Estimated time:</strong> 40 to 60 minutes</p>\n<p><strong>Context</strong></p>\n<p>A fictional municipality has received a file containing public consultation responses about a mobility plan. Before loading the future CSV file, the project team wants a small script that documents the monthly intake counts.</p>\n<p><strong>Given values</strong></p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Item</th>\n<th align=\"right\">Value</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Responses received</td>\n<td align=\"right\">640</td>\n</tr>\n<tr>\n<td>Responses missing consent information</td>\n<td align=\"right\">18</td>\n</tr>\n<tr>\n<td>Duplicate responses</td>\n<td align=\"right\">7</td>\n</tr>\n<tr>\n<td>Expected columns</td>\n<td align=\"right\">11</td>\n</tr>\n<tr>\n<td>Received columns</td>\n<td align=\"right\">10</td>\n</tr>\n</tbody></table></div>\n<p><strong>Your task</strong></p>\n<p>Create a script that:</p>\n<ol>\n<li>stores the expected column count as a constant-by-convention;</li>\n<li>uses meaningful variables for all other values;</li>\n<li>calculates the number of retained responses;</li>\n<li>calculates and displays a retention percentage to one decimal place;</li>\n<li>calculates the difference between expected and received columns;</li>\n<li>creates Boolean values for &quot;has exclusions&quot; and &quot;structure matches expectation&quot;;</li>\n<li>includes a purpose comment and an assumption comment;</li>\n<li>displays a readable report;</li>\n<li>uses <code>type()</code> to inspect at least two values;</li>\n<li>documents whether exclusion categories are assumed to overlap.</li>\n</ol>\n<p><strong>Target output content</strong></p>\n<p>Your wording may differ, but the report should communicate these results:</p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Retained responses: 615\nRetention rate: 96.1%\nColumn difference: 1\nHas exclusions: True\nStructure matches expectation: False\n</code></pre></div>\n<p><strong>Common mistakes to watch for</strong></p>\n<ul>\n<li>subtracting only one exclusion category;</li>\n<li>dividing the total by the retained count;</li>\n<li>confusing <code>=</code> and <code>==</code>;</li>\n<li>treating a column-count match as full validation;</li>\n<li>failing to state whether categories overlap.</li>\n</ul>\n<p><strong>Modification task</strong></p>\n<p>Change the case so the file has all expected columns and no duplicates. Predict every affected output before running.</p>\n<p><strong>Break-and-repair requirement</strong></p>\n<p>Create and repair one <code>NameError</code>, one <code>TypeError</code> and one logical error. Record the broken line, the observed message or output, and the repaired line in a text cell.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"MINI_CASE_1_BRIEF_URL\">Download mini-case 1 brief</a></li>\n<li><a href=\"#\" data-link-placeholder=\"MINI_CASE_1_COLAB_URL\">Open mini-case 1 starter notebook</a></li>\n<li><a href=\"#\" data-link-placeholder=\"MINI_CASE_1_DATASET_URL\">Open mini-case 1 dataset placeholder</a></li>\n<li><a href=\"#\" data-link-placeholder=\"MINI_CASE_1_SOLUTION_URL\">Open mini-case 1 solution</a></li>\n</ul>\n<h3>Mini-case 2.7.2 - University feedback preparation</h3>\n<p><strong>Estimated time:</strong> 45 to 70 minutes</p>\n<p><strong>Context</strong></p>\n<p>A fictional university unit wants a simple interactive script that records non-sensitive aggregate information about a feedback round. The script will later be replaced by a CSV workflow.</p>\n<p><strong>Required user inputs</strong></p>\n<ul>\n<li>course label as text;</li>\n<li>reporting period as text;</li>\n<li>invited students as a whole number;</li>\n<li>completed responses as a whole number;</li>\n<li>average completion time as a decimal number.</li>\n</ul>\n<p><strong>Your task</strong></p>\n<p>Create a script that:</p>\n<ol>\n<li>collects all five values with clear prompts;</li>\n<li>applies <code>int()</code> or <code>float()</code> only where appropriate;</li>\n<li>calculates the response rate;</li>\n<li>creates a Boolean showing whether completed responses equal invited students;</li>\n<li>displays a heading built with an f-string;</li>\n<li>displays all numerical results with units;</li>\n<li>adds a comment defining the denominator;</li>\n<li>checks at least three types;</li>\n<li>avoids collecting names, email addresses or other personal data.</li>\n</ol>\n<p><strong>Test cases</strong></p>\n<p>Use all four test cases and record what happens:</p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Test</th>\n<th align=\"right\">Invited</th>\n<th align=\"right\">Completed</th>\n<th align=\"right\">Average minutes</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Normal</td>\n<td align=\"right\">100</td>\n<td align=\"right\">72</td>\n<td align=\"right\">8.5</td>\n<td>Expected ordinary result</td>\n</tr>\n<tr>\n<td>Boundary</td>\n<td align=\"right\">100</td>\n<td align=\"right\">100</td>\n<td align=\"right\">7.0</td>\n<td>Equality boundary</td>\n</tr>\n<tr>\n<td>Invalid denominator</td>\n<td align=\"right\">0</td>\n<td align=\"right\">0</td>\n<td align=\"right\">0.0</td>\n<td>Division-by-zero case</td>\n</tr>\n<tr>\n<td>Impossible count</td>\n<td align=\"right\">100</td>\n<td align=\"right\">105</td>\n<td align=\"right\">6.5</td>\n<td>Logical validation problem</td>\n</tr>\n</tbody></table></div>\n<p><strong>Expected result for the normal test</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Response rate: 72.0%\nAll invited students responded: False\n</code></pre></div>\n<p><strong>Modification task</strong></p>\n<p>Change the prompts and labels so the same script could support a fictional community workshop instead of a university course.</p>\n<p><strong>Break-and-repair requirement</strong></p>\n<p>Enter a word for one numerical prompt, capture the <code>ValueError</code>, and repair the interaction by rerunning with suitable input. Do not add exception handling yet.</p>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"MINI_CASE_2_BRIEF_URL\">Download mini-case 2 brief</a></li>\n<li><a href=\"#\" data-link-placeholder=\"MINI_CASE_2_COLAB_URL\">Open mini-case 2 starter notebook</a></li>\n<li><a href=\"#\" data-link-placeholder=\"MINI_CASE_2_SOLUTION_URL\">Open mini-case 2 solution</a></li>\n</ul>\n<h3>Mini-case 2.7.3 - Communicating a societal indicator</h3>\n<p><strong>Estimated time:</strong> 50 to 75 minutes</p>\n<p><strong>Context</strong></p>\n<p>A fictional public report uses a threshold to describe neighbourhood access to a service. The technical team needs a transparent calculation, while the research team wants the threshold and limitations to remain visible.</p>\n<p><strong>Given values</strong></p>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Item</th>\n<th align=\"right\">Value</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Residents surveyed</td>\n<td align=\"right\">800</td>\n</tr>\n<tr>\n<td>Residents reporting access</td>\n<td align=\"right\">596</td>\n</tr>\n<tr>\n<td>Stated threshold</td>\n<td align=\"right\">75.0 per cent</td>\n</tr>\n</tbody></table></div>\n<p><strong>Your task</strong></p>\n<p>Create a script that:</p>\n<ol>\n<li>calculates the reported-access percentage;</li>\n<li>calculates the difference from the stated threshold;</li>\n<li>creates a Boolean comparison using <code>&gt;=</code>;</li>\n<li>displays the result to one decimal place;</li>\n<li>uses comments to identify the threshold as a stated project choice;</li>\n<li>uses variable names that distinguish counts, rates and thresholds;</li>\n<li>prints a short limitation note stored as a string;</li>\n<li>explains why a percentage does not show whose experiences are absent.</li>\n</ol>\n<p><strong>Expected numerical values</strong></p>\n<div class=\"md-code md-output\"><div class=\"md-code-label\">Output or reference</div><pre><code class=\"language-text\">Reported access rate: 74.5%\nDifference from threshold: -0.5 percentage points\nMeets stated threshold: False\n</code></pre></div>\n<p><strong>Modification task</strong></p>\n<p>Change the access count to <code>600</code>. Predict what happens at the exact threshold boundary. Then test <code>604</code>.</p>\n<p><strong>Break-and-repair requirement</strong></p>\n<p>Reverse the comparison or rate formula deliberately, inspect the plausible but incorrect output, and repair it using a boundary test.</p>\n<p><strong>Critical reflection prompts</strong></p>\n<ul>\n<li><p>Who chose the threshold?</p>\n</li>\n<li><p>Is the survey sample representative?</p>\n</li>\n<li><p>Does &quot;access&quot; have a clear and consistent definition?</p>\n</li>\n<li><p>Can a single percentage hide differences between groups or locations?</p>\n</li>\n<li><p>What evidence is missing from the script?</p>\n</li>\n<li><p><a href=\"#\" data-link-placeholder=\"MINI_CASE_3_BRIEF_URL\">Download mini-case 3 brief</a></p>\n</li>\n<li><p><a href=\"#\" data-link-placeholder=\"MINI_CASE_3_COLAB_URL\">Open mini-case 3 starter notebook</a></p>\n</li>\n<li><p><a href=\"#\" data-link-placeholder=\"MINI_CASE_3_SOLUTION_URL\">Open mini-case 3 solution</a></p>\n</li>\n</ul>\n<hr>\n<h2>Recap and quick reference</h2>\n<h3>The foundation mental model</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Concept</th>\n<th>Question to ask</th>\n<th>Example</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Literal</td>\n<td>What value is written directly?</td>\n<td><code>250</code>, <code>&quot;October&quot;</code>, <code>True</code></td>\n</tr>\n<tr>\n<td>Variable</td>\n<td>What meaningful name refers to a value?</td>\n<td><code>total_records</code></td>\n</tr>\n<tr>\n<td>Assignment</td>\n<td>What name receives what value?</td>\n<td><code>total_records = 250</code></td>\n</tr>\n<tr>\n<td>Augmented assignment</td>\n<td>How is the current value updated?</td>\n<td><code>audit_trail += &quot; -&gt; reviewed&quot;</code></td>\n</tr>\n<tr>\n<td>Constant-by-convention</td>\n<td>Which setting should remain fixed?</td>\n<td><code>EXPECTED_COLUMNS = 12</code></td>\n</tr>\n<tr>\n<td>Expression</td>\n<td>What value will this code produce?</td>\n<td><code>complete / total * 100</code></td>\n</tr>\n<tr>\n<td>Statement</td>\n<td>What instruction is Python executing?</td>\n<td><code>print(total_records)</code></td>\n</tr>\n<tr>\n<td>Type</td>\n<td>How does Python treat this value?</td>\n<td><code>str</code>, <code>int</code>, <code>float</code>, <code>bool</code></td>\n</tr>\n<tr>\n<td>Missing-value state</td>\n<td>Is a value absent rather than zero or empty text?</td>\n<td><code>None</code></td>\n</tr>\n<tr>\n<td>Conversion</td>\n<td>Does the intended operation require another type?</td>\n<td><code>int(&quot;250&quot;)</code></td>\n</tr>\n<tr>\n<td>Input</td>\n<td>What text did the user type?</td>\n<td><code>input(&quot;Total: &quot;)</code></td>\n</tr>\n<tr>\n<td>Comment</td>\n<td>What does a human reader need to know?</td>\n<td><code># Categories are assumed not to overlap.</code></td>\n</tr>\n<tr>\n<td>Error message</td>\n<td>What could Python not understand or do?</td>\n<td><code>NameError</code>, <code>TypeError</code></td>\n</tr>\n<tr>\n<td>Logical test</td>\n<td>Does the result match an expected case?</td>\n<td>Part divided by whole</td>\n</tr>\n</tbody></table></div>\n<h3>Essential syntax reference</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Purpose</th>\n<th>Pattern</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Display text</td>\n<td><code>print(&quot;Message&quot;)</code></td>\n</tr>\n<tr>\n<td>Display label and value</td>\n<td><code>print(&quot;Total:&quot;, total_records)</code></td>\n</tr>\n<tr>\n<td>Assign a string</td>\n<td><code>case_name = &quot;Mobility survey&quot;</code></td>\n</tr>\n<tr>\n<td>Assign an integer</td>\n<td><code>record_count = 250</code></td>\n</tr>\n<tr>\n<td>Assign a float</td>\n<td><code>completion_rate = 93.5</code></td>\n</tr>\n<tr>\n<td>Assign a Boolean</td>\n<td><code>review_complete = False</code></td>\n</tr>\n<tr>\n<td>Assign an absent value</td>\n<td><code>reviewer_note = None</code></td>\n</tr>\n<tr>\n<td>Update an existing value</td>\n<td><code>audit_trail += &quot; -&gt; checked&quot;</code></td>\n</tr>\n<tr>\n<td>Inspect type</td>\n<td><code>type(record_count)</code></td>\n</tr>\n<tr>\n<td>Convert to integer</td>\n<td><code>int(&quot;250&quot;)</code></td>\n</tr>\n<tr>\n<td>Convert to float</td>\n<td><code>float(&quot;93.5&quot;)</code></td>\n</tr>\n<tr>\n<td>Convert to string</td>\n<td><code>str(2026)</code></td>\n</tr>\n<tr>\n<td>Collect input</td>\n<td><code>name = input(&quot;Name: &quot;)</code></td>\n</tr>\n<tr>\n<td>Collect integer input</td>\n<td><code>count = int(input(&quot;Count: &quot;))</code></td>\n</tr>\n<tr>\n<td>Equality comparison</td>\n<td><code>count == 0</code></td>\n</tr>\n<tr>\n<td>At-least comparison</td>\n<td><code>rate &gt;= threshold</code></td>\n</tr>\n<tr>\n<td>Format a value</td>\n<td><code>f&quot;Rate: {rate:.1f}%&quot;</code></td>\n</tr>\n<tr>\n<td>Round to a new number</td>\n<td><code>round(rate, 1)</code></td>\n</tr>\n<tr>\n<td>Standardise simple text</td>\n<td><code>label.strip().lower().replace(&quot; &quot;, &quot;_&quot;)</code></td>\n</tr>\n<tr>\n<td>Write a comment</td>\n<td><code># Explain purpose or assumption.</code></td>\n</tr>\n</tbody></table></div>\n<h3>Arithmetic reference</h3>\n<div class=\"table-wrap\"><table>\n<thead>\n<tr>\n<th>Operation</th>\n<th>Syntax</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Add</td>\n<td><code>a + b</code></td>\n</tr>\n<tr>\n<td>Subtract</td>\n<td><code>a - b</code></td>\n</tr>\n<tr>\n<td>Multiply</td>\n<td><code>a * b</code></td>\n</tr>\n<tr>\n<td>Divide</td>\n<td><code>a / b</code></td>\n</tr>\n<tr>\n<td>Complete whole groups</td>\n<td><code>a // b</code></td>\n</tr>\n<tr>\n<td>Remainder</td>\n<td><code>a % b</code></td>\n</tr>\n<tr>\n<td>Power</td>\n<td><code>a ** b</code></td>\n</tr>\n<tr>\n<td>Force grouping</td>\n<td><code>(a + b) * c</code></td>\n</tr>\n</tbody></table></div>\n<h3>A six-question code-reading routine</h3>\n<p>When you encounter unfamiliar beginner code, ask:</p>\n<ol>\n<li>What values are written directly?</li>\n<li>Which names store those values?</li>\n<li>What types do the values have?</li>\n<li>Which expressions create new values?</li>\n<li>What assumptions and units are attached to the calculations?</li>\n<li>What output should appear, and how can I test it?</li>\n</ol>\n<h3>A seven-step debugging routine</h3>\n<ol>\n<li>Predict what should happen.</li>\n<li>Run the smallest relevant cell.</li>\n<li>Read the last traceback line.</li>\n<li>Inspect the highlighted line and the line above.</li>\n<li>Check names, types, quotation marks, brackets and operators.</li>\n<li>Make one small repair and rerun.</li>\n<li>Test the repaired result with a simple or boundary case.</li>\n</ol>\n<hr>\n<h2>Self-test</h2>\n<p>Complete this test without running Python first. Then use Python to check only after recording your answer. Keep the answer key separate.</p>\n<h3>Concepts</h3>\n<ol>\n<li>What is the difference between a literal and a variable?</li>\n<li>Why is <code>=</code> not best read as permanent mathematical equality?</li>\n<li>What does uppercase naming communicate in <code>EXPECTED_COLUMNS = 12</code>?</li>\n<li>Does Python technically prevent that uppercase value from changing?</li>\n<li>What is the difference between an expression and an assignment statement?</li>\n<li>Why is <code>&quot;250&quot;</code> different from <code>250</code>?</li>\n<li>What does <code>input()</code> always return before conversion?</li>\n<li>What is the difference between <code>=</code> and <code>==</code>?</li>\n<li>Why might <code>type()</code> be useful before subtraction?</li>\n<li>Why can code be wrong even when no traceback appears?</li>\n<li>Why are <code>None</code>, <code>0</code>, <code>&quot;&quot;</code> and <code>&quot;None&quot;</code> four different values?</li>\n<li>What happens to the result type when an integer and a float are added?</li>\n<li>What is the difference between <code>round(rate, 1)</code> and <code>{rate:.1f}</code> inside an f-string?</li>\n<li>What longer assignment is equivalent to <code>count += 5</code>?</li>\n</ol>\n<h3>Predict the output or error</h3>\n<ol start=\"15\">\n<li>What does <code>print(&quot;Rows:&quot;, 25)</code> display?</li>\n<li>What does <code>print(&quot;10&quot; + &quot;5&quot;)</code> display?</li>\n<li>What does <code>print(10 + 5)</code> display?</li>\n<li>What type is produced by <code>10 / 5</code>?</li>\n<li>What values are produced by <code>17 // 5</code> and <code>17 % 5</code>?</li>\n<li>What does <code>2 + 3 * 4</code> produce?</li>\n<li>What does <code>(2 + 3) * 4</code> produce?</li>\n<li>What happens when Python runs <code>int(&quot;4.5&quot;)</code>?</li>\n<li>What happens when Python runs <code>float(&quot;4.5&quot;)</code>?</li>\n<li>What does <code>type(True)</code> report?</li>\n</ol>\n<h3>Diagnose and repair</h3>\n<ol start=\"25\">\n<li>Repair <code>print(&quot;Dataset ready)</code>.</li>\n<li>Repair <code>total-records = 250</code>.</li>\n<li>Explain the likely cause of <code>NameError: name &#39;totl&#39; is not defined</code>.</li>\n<li>Repair the arithmetic intention in <code>&quot;250&quot; - 18</code>.</li>\n<li>Explain why <code>int(&quot;twenty&quot;)</code> fails.</li>\n<li>Explain why <code>10 / 0</code> fails.</li>\n<li>Repair the percentage formula <code>total / complete * 100</code> when complete is the part.</li>\n<li>Explain why <code>print(&quot;variable_name&quot;)</code> and <code>print(variable_name)</code> may display different things.</li>\n<li>Explain why <code>0.1 + 0.2</code> may display a long decimal representation.</li>\n<li>Explain why <code>int(8.9)</code> is not ordinary rounding.</li>\n</ol>\n<h3>Apply and reflect</h3>\n<ol start=\"35\">\n<li>Write meaningful variable names for a total, a missing count and a completion rate.</li>\n<li>Write a clear prompt that states both the required value and its unit.</li>\n<li>Write a comment that documents an assumption rather than repeating syntax.</li>\n<li>Give one reason a threshold in code should be critically reviewed.</li>\n<li>Give one example of a plausible but logically wrong output.</li>\n<li>Describe one boundary test for a completion-rate script.</li>\n<li>Explain why a participant ID containing digits may need to remain a string.</li>\n<li>Explain why a <code>True</code> readiness flag does not prove general data quality.</li>\n<li>Name one piece of information that should not be entered into a public teaching notebook.</li>\n<li>Explain a complete break-and-repair cycle in your own words.</li>\n</ol>\n<ul>\n<li><a href=\"#\" data-link-placeholder=\"SELF_TEST_FORM_URL\">Open the self-test answer form</a></li>\n<li><a href=\"#\" data-link-placeholder=\"SELF_TEST_SOLUTION_URL\">Open the separate self-test answer key</a></li>\n</ul>\n<hr>\n<h2 id=\"python-glossary\">22. Glossary</h2>\n<p><strong>Argument</strong><br>A value supplied to a function inside parentheses. In <code>print(total)</code>, <code>total</code> is an argument.</p>\n<p><strong>Arithmetic operator</strong><br>A symbol that performs a numerical operation, such as <code>+</code>, <code>-</code>, <code>*</code> or <code>/</code>.</p>\n<p><strong>Assignment</strong><br>An instruction that makes a name refer to a value, such as <code>count = 10</code>.</p>\n<p><strong>Augmented assignment</strong><br>A shorter update form that uses a value&#39;s current state, such as <code>count += 5</code> or <code>note += &quot; reviewed&quot;</code>.</p>\n<p><strong>Boolean</strong><br>A type with the two values <code>True</code> and <code>False</code>.</p>\n<p><strong>Built-in function</strong><br>A function Python makes available without importing a separate library, such as <code>print()</code>, <code>type()</code> or <code>input()</code>.</p>\n<p><strong>Case-sensitive</strong><br>Treating uppercase and lowercase letters as different. <code>Records</code> and <code>records</code> are different Python names.</p>\n<p><strong>Code cell</strong><br>A notebook area that contains executable code.</p>\n<p><strong>Comment</strong><br>Human-readable text beginning with <code>#</code> that Python ignores during execution.</p>\n<p><strong>Comparison operator</strong><br>An operator that compares values and produces a Boolean, such as <code>==</code>, <code>&gt;=</code> or <code>!=</code>.</p>\n<p><strong>Concatenation</strong><br>Joining strings together, often with <code>+</code>.</p>\n<p><strong>Constant-by-convention</strong><br>A value assigned to an uppercase name to communicate that it should not change. Ordinary Python does not enforce this rule.</p>\n<p><strong>Conversion</strong><br>Creating a value of another type, such as converting <code>&quot;25&quot;</code> to <code>25</code> with <code>int()</code>.</p>\n<p><strong>Data type</strong><br>A category that determines how Python represents and operates on a value, such as string, integer, float or Boolean.</p>\n<p><strong>Debugging</strong><br>The systematic process of finding, understanding, testing and repairing a problem in code or logic.</p>\n<p><strong>Denominator</strong><br>The value below the division line. In <code>complete / total</code>, <code>total</code> is the denominator.</p>\n<p><strong>Escape sequence</strong><br>A character combination inside a string with a special meaning. <code>\\n</code> starts a new line.</p>\n<p><strong>Expression</strong><br>Code that produces a value, such as <code>total - missing</code>.</p>\n<p><strong>Float</strong><br>Python&#39;s floating-point number type, used for values such as <code>3.5</code>.</p>\n<p><strong>Floor division</strong><br>Division with <code>//</code>, producing the whole-number floor of the result.</p>\n<p><strong>Function</strong><br>Reusable code that performs a task. A function is called with parentheses, such as <code>print()</code>.</p>\n<p><strong>f-string</strong><br>A string beginning with <code>f</code> that can insert expressions inside braces, such as <code>f&quot;Total: {total}&quot;</code>.</p>\n<p><strong>Input</strong><br>Information supplied to a program. <code>input()</code> collects keyboard input as a string.</p>\n<p><strong>Implicit conversion</strong><br>An automatic compatible type change performed by Python during an expression, such as producing a float when an integer and a float are added.</p>\n<p><strong>Integer</strong><br>Python&#39;s whole-number type, written <code>int</code>.</p>\n<p><strong>Keyword</strong><br>A word reserved for Python&#39;s grammar, such as <code>if</code>, <code>class</code>, <code>True</code> or <code>return</code>.</p>\n<p><strong>Literal</strong><br>A value written directly in code, such as <code>42</code>, <code>&quot;text&quot;</code> or <code>False</code>.</p>\n<p><strong>Logical error</strong><br>Code that runs but produces a result that does not match the intended logic.</p>\n<p><strong>Modulo</strong><br>The <code>%</code> remainder operator. <code>17 % 5</code> produces <code>2</code>.</p>\n<p><strong>Notebook</strong><br>A document containing code cells, text cells and outputs.</p>\n<p><strong>None</strong><br>Python&#39;s special value for the absence of a value. It is different from zero, empty text, <code>False</code> and the string <code>&quot;None&quot;</code>.</p>\n<p><strong>Operand</strong><br>A value on which an operator acts. In <code>a + b</code>, <code>a</code> and <code>b</code> are operands.</p>\n<p><strong>Operator precedence</strong><br>Rules that determine which operations Python evaluates first.</p>\n<p><strong>Output</strong><br>Information a program produces or displays.</p>\n<p><strong>Parentheses</strong><br>Round brackets used for function calls and explicit expression grouping.</p>\n<p><strong>Prompt</strong><br>Text shown by <code>input()</code> to tell the user what to enter.</p>\n<p><strong>Reserved word</strong><br>Another common term for a Python keyword that cannot be used as an ordinary variable name.</p>\n<p><strong>Runtime</strong><br>The active environment in which notebook code executes and variables exist temporarily.</p>\n<p><strong>String method</strong><br>A ready-made operation associated with string values, called with dot notation, such as <code>label.strip()</code> or <code>label.lower()</code>.</p>\n<p><strong>Snake case</strong><br>A naming style using lowercase words joined by underscores, such as <code>missing_record_count</code>.</p>\n<p><strong>Statement</strong><br>An instruction Python executes, such as an assignment or a print statement.</p>\n<p><strong>String</strong><br>Python&#39;s text type, written <code>str</code>.</p>\n<p><strong>Syntax</strong><br>The grammar and structural rules for writing valid code.</p>\n<p><strong>Traceback</strong><br>Python&#39;s report showing where an execution error occurred and what error type was raised.</p>\n<p><strong>Type conversion</strong><br>See <strong>Conversion</strong>.</p>\n<p><strong>Variable</strong><br>A name that refers to a value.</p>\n<hr>\n<h2>Completion checklist</h2>\n<p>You are ready to continue when you can honestly tick these items:</p>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\"> I can open, copy, rename and run a Colab notebook.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I understand that notebook runtime state can disappear.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can explain a literal, variable, assignment and constant-by-convention.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can use <code>+=</code> to update a number or extend a string, and I can explain the equivalent longer assignment.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can display labelled output with <code>print()</code>.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can choose valid, meaningful snake-case names.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can avoid keywords and avoid hiding useful built-in names.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can distinguish a basic expression from an assignment statement.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can use all arithmetic operators covered here.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can use parentheses to make calculation order explicit.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can recognise <code>str</code>, <code>int</code>, <code>float</code> and <code>bool</code> values.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can distinguish <code>None</code>, zero, empty text and the string <code>&quot;None&quot;</code>.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can recognise when Python implicitly produces a float from compatible numerical values.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can distinguish rounding a stored value from formatting a displayed value.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can inspect a value with <code>type()</code>.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can use <code>int()</code>, <code>float()</code> and <code>str()</code> for justified conversions.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I understand that successful conversion is not the same as validation.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can collect text and numerical input.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can write comments about purpose, units and assumptions.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can read the last line of a traceback.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can repair the six named Python error types in this tutorial.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can recognise that a logical error may produce no traceback.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can predict, run, modify, break and repair a complete foundation script.</li>\n<li><input disabled=\"\" type=\"checkbox\"> I can explain what the code does and what it cannot establish about the case.</li>\n</ul>\n<p>If several boxes remain unticked, return to the relevant checkpoint rather than trying to memorise the whole tutorial.</p>\n<hr>\n<h2>What comes next</h2>\n<p>Python Foundations II will build on this tutorial with:</p>\n<ul>\n<li>comparison and Boolean expressions in decisions;</li>\n<li><code>if</code>, <code>elif</code> and <code>else</code>;</li>\n<li>indentation inside code blocks;</li>\n<li>logical operators in more depth;</li>\n<li><code>try</code> and <code>except</code> for handling expected input problems;</li>\n<li><code>for</code> and <code>while</code> loops;</li>\n<li>simple functions, parameters and return values;</li>\n<li>imports and libraries;</li>\n<li>more systematic traceback reading.</li>\n</ul>\n<p>Later tutorials will connect the same foundation concepts to CSV files and pandas. For example:</p>\n<div class=\"md-code md-python\"><div class=\"md-code-label\"><span>Python</span><button type=\"button\" class=\"md-copy-button\" data-copy-python aria-label=\"Copy this Python code to the clipboard\">Copy code</button></div><pre><code class=\"language-python\">data = pd.read_csv(&quot;main_case_raw.csv&quot;)\n</code></pre></div>\n<p>You will be able to recognise:</p>\n<ul>\n<li><code>data</code> as a variable;</li>\n<li><code>=</code> as assignment;</li>\n<li><code>pd</code> as a name referring to an imported library;</li>\n<li><code>read_csv()</code> as a function call;</li>\n<li><code>&quot;main_case_raw.csv&quot;</code> as a string argument;</li>\n<li>the returned table as a value assigned to <code>data</code>.</li>\n</ul>\n<p>Do not worry if <code>pd</code> and CSV loading are not yet familiar. Their full explanations belong in the later data tutorial.</p>\n<hr>\n<h2>End-of-tutorial reflection</h2>\n<p>Write 150 to 250 words responding to these prompts:</p>\n<ol>\n<li>Which code pattern can you now explain confidently?</li>\n<li>Which error type is easiest for you to recognise?</li>\n<li>Which type conversion still requires careful thought?</li>\n<li>What is one example where Python can calculate a result but cannot decide whether the underlying category, threshold or assumption is justified?</li>\n<li>What will you do first when a future code cell fails?</li>\n</ol>\n<p>Save the reflection in a text cell at the end of your notebook.</p>\n"
    }
  ]
};
const pythonFoundationReferences = {
  "1.12": [
    "Google. (n.d.). <em>Google Colab frequently asked questions</em>. <a href=\"https://research.google.com/colaboratory/intl/en-GB/faq.html\" target=\"_blank\" rel=\"noreferrer\">https://research.google.com/colaboratory/intl/en-GB/faq.html</a>"
  ],
  "1.13": [
    "Python Software Foundation. (2026). Names, keywords and soft keywords. In <em>Python 3 documentation: Lexical analysis</em>. <a href=\"https://docs.python.org/3/reference/lexical_analysis.html#names-identifiers-and-keywords\" target=\"_blank\" rel=\"noreferrer\">https://docs.python.org/3/reference/lexical_analysis.html#names-identifiers-and-keywords</a>"
  ],
  "1.14": [
    "Python Software Foundation. (2026). Using Python as a calculator. In <em>The Python tutorial</em>. <a href=\"https://docs.python.org/3/tutorial/introduction.html#using-python-as-a-calculator\" target=\"_blank\" rel=\"noreferrer\">https://docs.python.org/3/tutorial/introduction.html#using-python-as-a-calculator</a>"
  ],
  "1.15": [
    "Python Software Foundation. (2026). Built-in types. In <em>Python 3 documentation</em>. <a href=\"https://docs.python.org/3/library/stdtypes.html\" target=\"_blank\" rel=\"noreferrer\">https://docs.python.org/3/library/stdtypes.html</a>"
  ],
  "1.16": [
    "Python Software Foundation. (2026). Built-in functions. In <em>Python 3 documentation</em>. <a href=\"https://docs.python.org/3/library/functions.html\" target=\"_blank\" rel=\"noreferrer\">https://docs.python.org/3/library/functions.html</a>",
    "Severance, C. R. (n.d.). Variables, expressions, and statements. In <em>Python for Everybody</em>. Exercises 3 and 5 inspired the two explicitly labelled transfer examples. <a href=\"https://www.py4e.com/html3/02-variables\" target=\"_blank\" rel=\"noreferrer\">https://www.py4e.com/html3/02-variables</a>"
  ],
  "1.17": [
    "Python Software Foundation. (2026). Errors and exceptions. In <em>The Python tutorial</em>. <a href=\"https://docs.python.org/3/tutorial/errors.html\" target=\"_blank\" rel=\"noreferrer\">https://docs.python.org/3/tutorial/errors.html</a>"
  ],
  "1.18": []
};
